# 路況播報員 — V1.8.5 正式上線總結

**上線日期：2026-08-18**
**Production main commit：`97756a8805b52acb8746aa7d14bbf89be51ee267`**
**狀態：V1.8.5 已正式上線，Production 已確認運作正常**

---

## 這個版本完成什麼

過去版本的 LINE 播報只有文字。V1.8.5 讓**國道一號的事故（accident）通報**，會在同一則 LINE 訊息裡，額外附上**事故地點附近的監視器畫面**——把周邊最多 4 支攝影機的畫面拼成一張「四宮格」照片，跟事故文字一起送出，讓司機不用自己再去查監視器，一眼就能看到現場周邊路況。

四宮格的取鏡邏輯（V1.7 訂下的規則，這一版沒有改）：
- 分成**四個象限**：南向-事故前（南前）、南向-事故後（南後）、北向-事故前（北前）、北向-事故後（北後）。
- 每個象限優先找**事故點 ±2 公里內**的攝影機；如果 ±2 公里內沒有，才放寬到 **±4 公里**；還是沒有，這個象限就**留空**，不會硬找一支很遠的攝影機充數。
- **服務區/休息站的攝影機不會被選用**於事故判斷——那些鏡頭常常對著停車場或加油站，不保證看得到主線。
- 如果四個象限全部找不到符合的鏡頭，畫面上會**明確顯示「無符合鏡頭」**，不會用假畫面魚目混珠。

---

## 安全設計

CCTV 圖片是「錦上添花」，不是「必要條件」——任何時候只要 CCTV 這邊出狀況，永遠退回純文字通報，事故本身該不該播、有沒有播，完全不受 CCTV 影響：

- 只有 `source = freeway`（國道）且 `type = accident`（事故）的事件，才會嘗試附加 CCTV 圖片。封閉、施工、其他異常、壅塞等其他類型完全不受影響。
- 目前**只支援已經確認過攝影機對應關係的國道一號**。其他國道（例如國3）暫時不支援，會直接以純文字播報，不會用猜的道路代號硬套。
- 事故公里數（KM）**只採信 TDX 官方回傳的結構化 `startKM`/`endKM` 欄位**，絕對不會從事故描述文字裡用猜的方式抓公里數。
- CCTV 圖片抓取、組圖、上傳任何一步失敗或逾時，都會**自動退回純文字通報**，不會讓司機因此收不到事故通知。

---

## 效能與 TDX 用量

- 正式播報（Production 的 Cron 排程）**不會為了 CCTV 額外呼叫 TDX API**。
- CCTV 需要的攝影機清單（哪支鏡頭在哪、對應哪個方向），是從 `TRAFFIC_KV` 裡的一份**快取**讀出來的（cache key：`cctv:freeway-metadata:v1`，效期 7 天）——這份快取是由管理端的「新竹探測工具」在人工執行一次性探測時，順便寫入的，不是播報流程自己去問 TDX。
- 監視器的**即時畫面**（不是清單，是實際照片）仍然是每次都直接向 freeway.gov.tw 官方畫面來源即時抓取，這一段本來就不消耗 TDX 額度。

---

## LINE 播報方式

- 事故文字跟 CCTV 四宮格圖片，是在**同一次 LINE 推播請求**裡一起送出，不會分兩次推播、不會讓司機收到兩則訊息。
- 圖片實際存放在 Cloudflare R2，網址**只在 15 分鐘內有效**（由程式碼強制檢查，不是單純靠瀏覽器快取）。
- 如果同一起事故要通知多個訂閱對象，大家看到的是**同一張圖片**，不會為每個人重新組圖、重複上傳。

---

## 延遲保護

CCTV 圖片的準備（抓攝影機清單、抓畫面、組圖、上傳）**整個 Cron 執行週期最多花 4 秒**，不是每一起事故各自 4 秒。如果同一個週期裡有好幾起符合條件的事故，前面的事故用掉了時間預算，後面來不及處理的事故會**直接以純文字送出**，不會讓整批通知被拖慢。

---

## Production 驗證

- 四宮格圖片在正式環境**實機測試成功**，公開圖片網址（R2）也確認可以正常存取，實測檔案大小約 200KB 左右，適合 LINE 傳輸。
- CCTV 攝影機清單快取（`cctv:freeway-metadata:v1`）已經在 Production 的 `TRAFFIC_KV` 建立完成。
- V1.8.5 已經正式啟用，開始在真實 Production 環境運作。

---

## 已知限制

1. CCTV 自動附圖目前**只支援國道一號**。國道三號等其他道路，因為還沒有從真實 Production 的 TDX 回應確認過對應的攝影機道路代號，暫時維持純文字播報。
2. 攝影機清單快取效期 **7 天**，之後需要人工重新執行一次探測工具才能更新，不會自動刷新。
3. CCTV 畫面只能反映**事故周邊路況**，不保證一定能直接拍到事故現場本身——鏡頭是固定或有限角度的監視器，不是專門追蹤事故的系統。

---

## 下一步

**這一輪之後不再主動增加新功能。** 先讓 Production 實際運行一段時間，之後只根據**真實案例**回報來修 bug（例如：某支攝影機選錯、某個公里數判斷有誤等）。不會為了測試而重複執行探測工具，也不會為了驗證而額外增加 TDX 呼叫次數。
