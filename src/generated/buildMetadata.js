// GENERATED FILE — do not edit by hand.
//
// Regenerated fresh by scripts/generateBuildMetadata.mjs every time
// `npm run deploy` runs (wired in via package.json's `predeploy` npm
// lifecycle hook, which npm runs automatically before `deploy` — see
// that script's own module comment for exactly how each field is
// determined, including its fallback order and how it stays honest
// about which source actually produced a value instead of guessing).
//
// This checked-in version is a PLACEHOLDER for local dev/test only — it
// is never what a real Cloudflare deploy actually bundles, because the
// generator overwrites this exact file, in place, as the very first step
// of every `npm run deploy` invocation, before wrangler ever reads or
// bundles source. A test that cares about specific values should inject
// its own mock rather than depend on this placeholder's content.
export const BUILD_METADATA = {
  deployedCommit: 'unknown',
  commitSource: 'not-yet-generated',
  deployedBranch: 'unknown',
  branchSource: 'not-yet-generated',
  expectedMainCommit: 'unknown',
  expectedMainCommitSource: 'not-yet-generated',
  buildTime: null,
};
