<!-- title: 版本線 -->

# 06. Version History（重要版本線，非完整 git log）

只收錄目前仍具架構意義的版本節點。commit hash 以下方列出的實際 git 證據為準（`git log --oneline`，短 SHA），非人工回憶。完整逐 commit 歷史請直接查 `git log`，不在本文件重複。

| 版本 | commit（短 SHA） | 主題 |
|---|---|---|
| V1.8.6.5 | `76a4113`→`07253f9` | KM Location Resolver（公里數 → 司機看得懂的位置 + 地圖），含官方道路位置資料整合 |
| V1.8.6.6 | `d0012e1`, `0e8349f` | Production forensic audit：非碰撞異常事件不應被誤判為事故 |
| Pipeline Trace | `3858e0a` | V1.8.6.7：24h Pipeline Trace + 人工查修頁（本專案最重要的可觀測性基礎設施） |
| V1.8.6.8 | `462e217` | Driver-Relevant Event Broadcast Time Policy |
| V1.8.6.9 | `9af8d21` | Mobile-first Deployment Guard |
| V1.8.6.9a | `5724524`, `2ae6730` | Pipeline Trace Mobile UX — Taiwan time、封閉詞彙篩選、深色模式 |
| V1.8.7.0 | `54cb6c3` | Dynamic Shoulder Broadcast + Single-CCTV Strategy（動態路肩單鏡頭策略首次引入） |
| V1.8.7.1 | `a835493` | Multi-event Single CCTV Budget / Fairness Fix（每事件獨立預算，避免先到事件餓死後到事件） |
| V1.8.7.2 | `6c109a6` | Dynamic Shoulder Message Simplification |
| V1.8.7.3 | `fa8bf22`（原始）／`62cb229`（整合進 main 後版本） | CCTV Prepare-Timeout Fix + Pipeline Trace Filter Fix（分頁 bug 修復） |
| V1.8.7.4 | `7a8ec53` | 國3 CCTV Support Audit — 唯讀稽核，**未啟用**（當時無法從 dev sandbox 確認真實 RoadID/RoadName） |
| V1.8.7.5 | `be57f88` | Enable Freeway 3 CCTV Support — 用外部唯讀查證取得的真實 Production RoadID `'000030'`/RoadName `'國道3號'` 正式啟用國3 CCTV |
| V1.8.7.6 | `40b1409` | Pipeline Trace Filter Production Investigation — 三層reproduction 皆確認 server 端無缺陷，結論為 client-side 議題 |
| V1.8.7.7 | `a3d6609`（修復）／`8e10a7a`（封版紀錄） | CCTV Gray Broken Image Fix — `extractFirstJpegFrame` JPEG marker-aware 解析修復，見 `07_KNOWN_ISSUES.md`／`02_PROJECT_HANDOFF.md` §35 完整寫法 |
| V1.8.7.8 | `f5a339a` | **TDX Quota Protection — PBS-ONLY MODE（2026-08-23，生效中）** — TDX 額度用盡，以 `TRAFFIC_SOURCE_MODE=PBS_ONLY` 單一旗標關閉 Cron 路徑上所有 TDX 呼叫（RoadEvent／OAuth token／CCTV），PBS 完全不受影響。**未刪除任何 TDX 程式碼**，還原＝改回 `ALL` 並 push。見 `07_KNOWN_ISSUES.md` 的還原程序 |
| V1.8.7.9 | `21c1a42` | Accident-Only Proactive LINE Push ＋ CCTV 影像恢復 — 主動推播收斂為重大事故限定（`LINE_PUSH_POLICY=MAJOR_ACCIDENT_ONLY`），動態路肩主動推播關閉 |
| V1.8.7.10 | `fecb818` | PBS_ONLY 下不得要求 TDX 對應 — 停用中的 TDX 不得否決 PBS 國道事故（`TDX_CORROBORATION_REQUIRED=false`） |
| V1.8.7.11 | `fc43549` | Service Area Gate — 播報邊界地理閘門（八堵事件），PBS fail-closed、TDX 僅採正面證據 |
| V1.8.7.12 | `990237c` | Location Quality Gate ＋ 播報追溯斷點修復（台68 事件）— 位置精確度閘門，且已推播事件必定可在 Pipeline Trace 查到 |
| V1.8.7.13 | `678f0bb` | PBS 國道事故 CCTV Enrichment — CCTV 資格改依「道路＋KM 可信度」，不再要求 `source==='freeway'`（國3 96K+700 事件） |
| V1.8.7.14 | `951f547` | **CCTV 官方名冊恢復＋永久保存修正（國1 93K 事件）** — 移除名冊 7 天 `expirationTtl`、拒絕以空名冊覆寫、打包官方 NFB 名冊 1943 筆為地板。四段式版本線的**最後一版** |
| **V1.9.0** | `3af5c8b` | **三段式版本治理生效後第一個正式版本** — CCTV 事故（quad）prepare-timeout 可觀測性修復（國3 96K+700 事件，2026-08-26）。真人事故：09:20 事件進 Shared Feed、無圖，且完全沒有 CCTV completion log；同一事件 09:30（下一個 Cron tick）重跑即成功四格全中。確認的 Root Cause：quad 路徑完全沒有 stage 追蹤（動態路肩單鏡頭路徑早就有），因此任何一次真實的外部延遲（frame fetch／JPEG compose／R2 publish——三者皆已證實共用同一個 4000ms budget）在 timeoutStage 上一律顯示 `null`，等於什麼都沒記錄。修復：quad 路徑比照單鏡頭路徑加上 stageTracker，在成功／失敗／timeout 任何結果都留下 metadataElapsedMs／cameraSelectionElapsedMs／frameFetchElapsedMs／collageElapsedMs／successfulFrameCount／failedFrameCount／r2PublishElapsedMs／timeoutStage。**未新增任何 retry、未新增第二輪 fetch、未調整 4000ms 數字、未加 single-camera fallback**——RETRY_REQUIRED = NO。7 項新測試（`test/cctvQuadPrepareForensics.test.js`）以決定性方式證實：FRAME_FETCH_MODE=PARALLEL、單一慢/卡住的鏡頭會拖住整批 compose、compose 與 R2 publish 都算進同一個 budget、timeout 不會中止背景中的既有工作。09:20 當下具體是哪一段外部 I/O 慢，因為當時沒有 stage 級別紀錄，**已誠實列為無法從現有證據回溯**——這正是本次修復要根除的缺口，若再發生將完整可查。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |
| **V1.9.1** | `917b8ab` | **Pipeline Trace 查修頁篩選失效修正＋預設 60 筆（2026-08-26）** — 真人回報：`/admin/pipeline-trace-view` 篩選（來源／關鍵字／道路／rawId／狀態／筆數）點了「篩選」畫面不動。確認的 Root Cause（以真實瀏覽器重現，非猜測）：`applyAdminSecurityHeaders` 的 CSP 帶 `form-action 'none'`，任何有強制 CSP 的瀏覽器（含真人回報所用的 iOS Safari）都會**完全拒絕**這個頁面（乃至任何 admin 頁面）的 `<form>` 送出——伺服器端的篩選邏輯（表單參數、`listPipelineTrace` 的 predicate、分頁）在前一輪（V1.8.7.6）就已被完整查證過是對的；那一輪自己的 headless 瀏覽器重現「不在本 repo 的自動化測試套件內」，顯然從未真正對著這個 Worker 自己的安全標頭送出過請求。本輪用真的 Chromium 對著真的回應（含真的標頭）重現：點下「篩選」按鈕，畫面完全不導頁，瀏覽器主控台明確寫著「Refused to send form data ... because it violates ... form-action 'none'」；只改這一個指令為 `form-action 'self'`（其餘 CSP 完全不動），同一個點擊就能正確導頁到篩選後的網址。**採方案 A**：修好既有篩選功能，UI 保留，未移除任何篩選欄位。同時 `DEFAULT_LIST_LIMIT` 30→60（`MAX_LIST_LIMIT`／`MAX_ENTRIES_SCANNED` 掃描安全上限不變）。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |
| **V1.9.2** | `f45cd5e` | **Cloudflare KV Write Optimization ＋ TDX Usage Summary 正式退休（2026-08-26）** — 真實 Cloudflare 帳號告警：Writes 749/1,000（`traffic-reporter-kv` = 733，佔帳號總寫入量 97.9%）。四項變更：(1) `traffic:shared-feed`／`line:incident-suppression-state` 改為 WRITE_ON_CHANGE（新共用原語 `src/util/contentEqual.js`），內容真的不變才跳過寫入——施工中自己的測試抓到一個真實 aliasing bug（`resolveIncidentNotifications` 就地修改比對用的舊物件，導致比較永遠「相等」），已用 `structuredClone` 在呼叫前先拍快照修正；(2) Pipeline Trace 改為每輪一把 `debug:pipeline-trace-batch:v2:*` 批次金鑰，取代舊制一筆事件一把 key——舊制 `debug:pipeline-trace:v1:*` 完全不刪除不遷移、靠 24h TTL 自然過期，`listPipelineTrace` 已能正確合併新舊兩種схема，兩個既有 admin 讀取 handler 未改一行程式碼；(3) **TDX Usage Summary 正式退休**（人類決策，非優化）：`tdx:usage:summary:v1` 與底層 `tdx:usage:entry:v1:*` 帳本皆確認除了餵已退休的儀表板外無其他讀者，改為 0 writes/day，`/health` 的用量卡片改為指向 TDX 官方後台的靜態提示，TDX 本身（API client／OAuth／RoadEvent／CCTV metadata／source mode／9-1 額度恢復路徑）完全未動；(4) 新增 `[kv-write-budget]` Cron console.log（僅 Workers Logs，未新增 KV key）。新增 38 項測試（`test/kvWriteOptimization.test.js`）。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |
| **V1.9.9** | `5d1f9fe` | **Workers AI Driver Impact Decision Integration（Phase 3B，2026-08-28）** — Phase 2 預留的 AI candidate／cache key 設計正式接上真實 Workers AI 呼叫。固定 model `@cf/zai-org/glm-4.7-flash`，透過 `env.AI.run(...)`（binding 名稱 `AI`，`wrangler.jsonc` 新增 `"ai":{"binding":"AI"}`）。固定繁中 prompt 只判斷駕駛通行影響（是否無法正常通行/需要繞路/明顯延誤/封路封閉車道交流道橋梁隧道/長時間重大管制/影響接送或營運動線），明確不依事件類型名稱（事故/施工/管制）直接決定。嚴格 JSON 輸出驗證：`{notify:boolean, impact:'HIGH'|'LOW', reason:string≤80字, confidence:0-1}`，任何欄位缺失/型別錯誤/超出範圍即 `AI_DECISION_INVALID`，絕不到達 LINE。新模組：`src/pbs/aiConfig.js`（kill switch `PBS_AI_DECISION_ENABLED`，預設 `false`）、`src/pbs/aiDecisionCache.js`（重用 Phase 2 cache key 設計，TRAFFIC_KV，48h TTL，獨立 prefix，fail-open 讀寫，corrupt JSON/KV outage 皆視為 miss 不中斷流程）、`src/pbs/aiDecisionEngine.js`（`validateAiDecisionResponse()` 純函式嚴格 schema 檢查；`resolveAiDecision()` orchestrator：cache lookup → miss 才呼叫 AI → 驗證 → 驗證通過才快取）、`src/traffic/aiApprovedPbsBroadcast.js`（`runAiApprovedPbsBroadcast()`，重用既有 subscriptions/notified/incidentSuppression（事故類型限定）/messageFormat/CCTV（事故類型限定，失敗絕不擋文字推播）/pushMessage，只保留 broadcastHours 執行安全閘門，明確不呼叫 `getBroadcastEligibility`／`getLinePushPolicyDecision`／`resolveLocationQuality`——這三個正是本輪要退休的內容判讀硬規則，AI verdict 是 Windows PBS 語意權威，也不重新呼叫 service area，因 Phase 2 的 `isWindowsPbsAiCandidateEligible` 已在 candidate 建立階段把關）。`src/pbs/debugPush.js` 的關鍵架構決策：AI 開啟時與 legacy `runLineBroadcast()`+`runSharedFeedPersist()` 路徑**互斥**，同一事件絕不同時執行兩者，避免舊硬規則與 AI 同時核准同一事件造成 LINE 重複推播（施工令本身只說結果，機制由本輪設計補齊）。Exact transport duplicate（既有 idempotency 命中）與 AI cache hit 皆是 0 次 AI 呼叫；fingerprint 改變才是新 AI 呼叫，無任何 NLP 語意 diff。AI 失敗（429/5xx/network/invalid response/binding missing）一律 0 LINE、trace 記錄（`AI_CALL_FAILED`），絕不 fallback 回 `MAJOR_ACCIDENT_ONLY` 或任何舊硬規則（避免同一事件出現兩個判官）；未加入 retry——檢視現有程式庫未找到可直接重用的 retry helper，且施工令要求第一版保持簡單、不做複雜 retry queue，失敗交由下一次 Windows PBS 推送（若 fingerprint 不變、cache 仍 miss）自然重試。notify=false：只記 audit trace（eventId/model/notify=false/impact/reason/confidence/cache 命中與否），不產生新 LINE/CCTV/proactive broadcast，不額外寫大量新 KV。notify=true：呼叫 `runAiApprovedPbsBroadcast()` 再呼叫既有 `runSharedFeedPersist()`。CLEARED 事件不呼叫 AI、不產生相關 LINE 推播，hourly reminder 僅方向性設計未實作。Kill switch 理由：Claude push 程式碼後 Cloudflare 會自動部署，但 GPT Work 尚未在 Dashboard 建立並驗證真正的 AI binding；若 AI 決策預設開啟，正式 LINE 推播可能在人類決定啟用 AI 之前就因部署而壞掉；安全 rollout 順序為程式碼部署→AI 決策關閉→GPT Work 建立 Binding→驗證→另外明確指令才開啟。Observability 最低詞彙：`AI_CANDIDATE_CREATED`/`AI_CACHE_HIT`/`AI_CACHE_MISS`/`AI_CALL_STARTED`/`AI_DECISION_VALID`/`AI_DECISION_INVALID`/`AI_CALL_FAILED`/`AI_NOTIFY_TRUE`/`AI_NOTIFY_FALSE`/`AI_LINE_ATTEMPTED`/`AI_LINE_SENT`/`AI_LINE_FAILED`，皆帶 eventId/fingerprint/model/confidence/reason/durationMs；若 Workers AI 回應內含真實 `usage` 則原樣記錄，絕不捏造 neuron/token 數字；Secrets 絕不進 log；未實作複雜 quota 引擎，只做可觀察的 AI 呼叫 counter。新增 5 個測試檔共 57 項測試（`aiConfig`4／`aiDecisionCache`9／`aiDecisionEngine`18／`aiApprovedPbsBroadcast`9／`pbsAiDecisionScenarios`17，含施工令 A-P 十六個 mocked-AI-adapter 情境＋2 個 kill switch 測試），全部第一次執行即 PASS，零 fix iteration。全量迴歸：1509 項／1476 pass／33 fail，與變更前基線（1452/1419/33）逐項比對完全相同的 33 項失敗清單，NEW FAILURES=0（以 `git stash` 基線 diff 嚴格驗證）。本輪**未觸碰**：`MAJOR_ACCIDENT_ONLY`/`getBroadcastEligibility`/`resolveLocationQuality` 函式本身（未刪除，只是 Windows PBS AI 開啟時不再被呼叫）、TDX 或其他來源既有 `runLineBroadcast` 判斷路徑、LINE quota 硬性 gate（本輪未新增）、hourly reminder 實作。APP_VERSION 維持 `V1.9.9`（同一版本線分階段施工，未升 V1.9.10）。Production 狀態誠實記錄為 `AI_INTEGRATION=CODE_READY`、`AI_BINDING=PENDING_BROWSER_SETUP`、`AI_DECISION=DISABLED`、`LINE_AI_DECISION=NOT_ACTIVE`——即使程式碼已部署，Windows PBS 事件仍走 V1.9.8 既有 legacy 路徑，對 Production LINE 推播沒有任何行為改變，直到 GPT Work 完成 AI Binding 設定且另有明確指令開啟為止。本輪未嘗試開啟 Cloudflare Dashboard、未驗證 Neurons Dashboard——依施工令分工屬 GPT Work 工作範圍。詳見 `07_KNOWN_ISSUES.md` 的完整記錄 |
| **V1.9.9** | `7acb82a` | **Windows Service Area Hsinchu Only（Phase 1，2026-08-28，完成於另一個 session，本 Cloud Session 未參與）** — Windows PBS Local Edge Filter 僅納入新竹市與新竹縣；明確排除竹南、頭份、苗栗市與其他苗栗縣區域。國1／國3使用 Windows 專用新竹公里範圍，舊 broad bounding box 不再能單獨納入苗栗事件；跨縣市道路只保留新竹段。重用既有 canonical road/service-area resolver，未建立第二套業務 service-area logic。`NEW`／`UPDATED`／`MISSING_PENDING_CLEAR`／`CLEARED` lifecycle 未修改；AI integration 未開始；LINE policy 未修改。Targeted 12/12、root invariants 73/73、PBS full 121/121 PASS，NEW FAILURES=0。純 `pbs-relay/`（Windows 端）變更，未觸碰任何 `src/` Cloudflare runtime |
| **V1.9.9** | `0f9a080` | **AI-ready Business Pipeline Simplification（Phase 2，2026-08-28）** — 為 Phase 3 Workers AI 全量判讀做準備，本階段刻意**不接AI**、**不啟用新LINE判讀政策**，只整理decision path。新模組 `src/pbs/aiCandidate.js`：從 `src/pbs/debugPush.js` 既有（完全未修改）的正規化事件建立最小 AI candidate 物件（source/eventId/lifecycle/road/direction/areaNm/comment/longitude/latitude/generatedAt，附加displayKM/eventType/sourceDetail/locationQuality，刻意不含notify/impact——那是Phase 3的工作），與既有 `runLineBroadcast()` 呼叫**並行、完全獨立**，純觀察用（log only），從未gate、delay或改變真實LINE決策。只保留兩個gate：service area（重用`traffic/serviceArea.js#resolveServiceAreaEligibility`，與`runLineBroadcast`自己用的同一個resolver）與既有V1.9.7持久冪等/重複防護（duplicate事件從不到達candidate-building呼叫點）。**刻意不套用**在candidate建構上的既有內容硬規則：`MAJOR_ACCIDENT_ONLY` policy、V1.5 type/keyword whitelist（`getBroadcastEligibility`）、location quality hard-reject——這些函式本身**完全未修改**，對真實LINE決策仍是完整生效的legacy policy，直到Phase 3 AI真正接手。location quality（`resolveLocationQuality()`）唯讀重用，只作為candidate的metadata附加欄位，從未作為是否建立candidate的gate，僅scoped在這個新模組內，不影響其他來源/既有路徑。安全過渡狀態：`PBS_AI_DECISION_MODE = PREPARED_NOT_ACTIVE`（exported const）——prepared（本模組真的執行並log一個真實candidate）但not active（candidate從未用於任何決策、從未觸及LINE/CCTV/Shared Feed、從未呼叫任何AI模型）。新增AI decision cache key設計（`computeAiDecisionCacheKeyHash({eventId,fingerprint})`，SHA-256(eventId:fingerprint)，重用Windows既有穩定fingerprint、非新NLP/semantic fingerprint；`AI_DECISION_CACHE_KV_PREFIX='debug:pbs-ai-decision-cache:v1'`獨立debug-only前綴，預留給Phase 3採用）——本輪僅schema/helper，**無任何實際KV讀寫**。新增測試：`test/pbsAiCandidate.test.js`（13項純函式單元測試）+ `test/pbsDebugPush.test.js` 新增施工令十五項最低清單（非事故類型/交通管制/施工/事故/封閉類/壅塞事件皆能成為candidate、位置品質不佳但在服務區內不hard-reject、服務區外仍不進candidate、無效auth/payload零candidate、duplicate零額外candidate、candidate schema正確觀測、AI inactive狀態下0額外LINE推播、既有LINE安全/去重機制不受影響——真實符合資格事故仍恰好1次推播、lifecycle(UPDATED)行為不變、CLEARED不進AI candidate路徑）。全量迴歸：1452項／1419 pass／33 fail，與變更前基線（1424/1391/33，V1.9.9 Phase 1後量測）逐項比對完全相同的33項失敗清單，NEW FAILURES=0（以`git stash`基線diff嚴格驗證）。本輪**未觸碰**：真實LINE決策邏輯本身（`runLineBroadcast`/`broadcastPolicy.js`/`broadcastRules.js`/`locationQuality.js`皆未修改一行）、Workers AI（未接入/未選model/未呼叫）、LINE policy擴大、CCTV/Shared Feed判斷邏輯。APP_VERSION維持`V1.9.9`（同一版本線分階段施工，未升V1.9.10）。詳見 `07_KNOWN_ISSUES.md` 的完整記錄 |
| **V1.9.8** | `6e8575d` | **Windows PBS Push → Production Business Pipeline ＋ 退休 Cloudflare 30 分鐘 PBS Polling（2026-08-28）** — V1.9.7 關閉了持久冪等風險後，本輪把 Windows Debug Push 正式接上真正的業務線：新的 Production 主線是 PBS 官方來源→Windows 本機每3分鐘抓取／服務區篩選／NEW-UPDATED-CLEARED 生命週期→`POST /internal/pbs-debug-push`→V1.9.7 持久冪等→**正式 Business Pipeline**→正式播報資格判斷→LINE，Cloudflare 自身既有 30 分鐘 PBS 輪詢**正式退休**。Ingress：`src/pbs/debugPush.js` **就地升級**（施工令自身「選改動最小、重複程式碼最少的方案」——採 Option A，非另建第二 endpoint）：一個真正被接受（非duplicate）的 NEW/UPDATED 事件，新函式 `buildRawPbsRecordFromPush()` 把 Windows payload 組成 raw-PBS-shaped record（`happendate`/`happentime`/`modDttm` 由 payload 自己的 `generatedAt` 精確反推 Asia/Taipei 本地時間字串——UTC+8 固定無 DST，往返可還原同一時刻，非近似值；`roadtype` 刻意留空，因 Windows 本機過濾器已保證轉發的 NEW/UPDATED 事件 comment 一定含事故關鍵字，comment-only 分類已足夠可信），交給 `pbs/normalize.js` 既有未修改的 `normalizePbsEvent()`，再交給 `traffic/broadcastPipeline.js` 既有未修改的 `runLineBroadcast()`——**與 Cron 輪詢路徑呼叫的同一個函式**，同一套事故／服務區／位置品質／資格／去重／CCTV／LINE Push Policy／notified-state 判斷；隨後呼叫 `traffic/sharedFeed.js` 既有未修改的 `runSharedFeedPersist()`，寫入時機與 `scheduled.js` 呼叫方式完全相同。debugPush.js 本身 0 份重複的業務邏輯。CLEARED 生命週期只 ACK／log，**刻意不**進 `runLineBroadcast`——比照既有 `pbs/pipeline.js` 的 `clearedEvents` 從不進 `broadcastEvents` 的行為，維持與輪詢路徑完全一致的語意。LINE Push Policy（`MAJOR_ACCIDENT_ONLY` 及既有每一條資格規則）**完全未變動**，只是事件來源多了 Windows 這一條路。退休：`src/pbs/pbsConfig.js` 新增 `PBS_30_MIN_POLLING_ENABLED = false`（可由 `env.PBS_30_MIN_POLLING_ENABLED` 覆寫——與既有 `TRAFFIC_SOURCE_MODE`／`LINE_PUSH_POLICY` 同一慣例，Production 完全不設此 env var，僅供本 repo 既有 PBS/CCTV 測試套件沿用輪詢入口注入 fixture），`traffic/scheduled.js` 的 `pbsFetchPerformed` 因此恆為 `false`；`pbsSchedule.js`／`pbs/pipeline.js`／`pbs/lifecycle.js` 程式碼**完整保留、一行未刪**，翻回旗標即可 rollback；同一個 Cron tick 的 TDX／health snapshot／Shared Feed／Pipeline Trace 完全不受影響。已知可接受的副作用（誠實記錄）：`pbs:lifecycle-state` 不再被寫入（Windows 已獨立追蹤 PBS 生命週期）；`GET /health` 的 `pbs` 區塊凍結在退休前最後一次真實數值。新增測試：`test/pbsDebugPush.test.js` 施工令十五項最低清單（NEW/UPDATED 進 Business Pipeline、CLEARED 不進 broadcast、duplicate 不重複處理、服務區外 0 推播、不符合 LINE 政策 0 推播、符合政策恰好 1 次 LINE 推播、duplicate 0 額外推播、canonical notified-state key 重用證明、Shared Feed 正常、CCTV canonical 邏輯（無 metadata cache 仍 text-only 成功）等），另建 `test/pbsPollingRetirementV198.test.js`（4項：旗標關閉、`getPbsScheduleState()` 本身不變、would-be-scheduled tick 確認 PBS fetch 從未被呼叫、其餘 Cron 工作不受影響）。KV 成本剖面誠實修正：genuinely accepted 事件現在真正呼叫 Business Pipeline，非僅 debug log——實測 N 筆事件 → `gets=5N`／`puts=N+2`（idempotency=N + incident-suppression-state 一次 + shared-feed 一次，皆 WRITE_ON_CHANGE 只在整輪第一筆寫入）。全量迴歸：1424 項／1391 pass／33 fail，與變更前基線（1404/1371/33）逐項比對完全相同的 33 項失敗清單，NEW FAILURES = 0（以 `git stash` 基線 diff 嚴格驗證）；9 個既有測試檔因直接透過 `runScheduledTdxSync` 行使 PBS 輪詢入口注入 fixture 而加上 `env.PBS_30_MIN_POLLING_ENABLED=true` 覆寫，其被測邏輯本身完全未變。本輪**未觸碰**：LINE policy 本身、台61/台15全線封閉產品政策、Durable Object（未新增）、服務區 canonical 邏輯本身、Windows Secret／Task Scheduler。Production 驗收因 sandbox 網路 egress 政策封鎖 `traffic-reporter.mr-happytan.workers.dev` 與 Cloudflare Dashboard，誠實標記 `NOT_OBSERVED`（施工令自身第十一節允許以確定性測試＋部署狀態作為封版依據）。詳見 `07_KNOWN_ISSUES.md` 的完整記錄 |
| **V1.9.7** | `9cfa8ef` | **Persistent PBS Debug Push Idempotency（2026-08-28）** — V1.9.6 首筆真實事件驗收成功（台68西向5K：Windows Debug Push 08:48:30，Cloudflare 30分鐘輪詢 09:00:39，Windows早發現約12.1分鐘），本輪關閉當時標記的唯一風險：V1.9.5的冪等只有per-isolate記憶體、`NOT_PERSISTENT`，isolate換掉/Worker重啟/redeploy都可能讓同一事件重新被accepted。修復：`src/pbs/debugPush.js` 新增 TRAFFIC_KV 下的持久 L2 層，獨立 debug-only 前綴 `debug:pbs-push-idempotency:v1:*`（絕不觸碰任何 business key），key 由 `source:eventId:lifecycle:fingerprint` 的 SHA-256 雜湊決定性產生（刻意不用requestId，因同一事件的重試requestId本就不同）；V1.9.5 既有記憶體 Map 保留為 L1 快取（同isolate內可跳過KV讀取），但**絕非唯一真相**——L1 miss 一律再查 L2 KV 才能決定是否 accept，故全新 isolate 的空 L1 仍能正確命中別的 isolate 寫入的 L2 紀錄。TTL 48小時。實測：duplicate 永遠 0 次額外寫入，僅真正首次的 idempotency key 花 1 次寫入——10/30/100 筆/日分別實測消耗 10/30/100 次 KV 寫入（`test/pbsDebugPush.test.js` 的KV成本量化測試，真實counting mock量測非估算），加上既有約118 writes/day 基線 → 約128/148/218 writes/day，遠低於1,000/day帳戶上限，`KV_WRITE_PRESSURE=LOW`。誠實回報 `KV_ONLY_ATOMICITY=NOT_SUFFICIENT`（Cloudflare KV的get-then-put沒有compare-and-swap，理論上兩個isolate同時收到完全相同payload仍可能雙雙accepted）——本輪依施工令自身「不要過度設計」指示**不**引入Durable Object，因為本輪真正要關閉的風險（isolate/重啟/redeploy造成的**事後**重複accept）已被持久KV層完全解決，僅存的極窄race window在這個endpoint的真實流量型態（單一Windows來源、非爆發性）下機率趨近於零，且此endpoint本身零business side effect（LINE=0/CCTV=0/Shared Feed=0），race發生的最壞後果只是一行重複的debug log與一次內容相同的冗餘KV寫入。故 `PERSISTENT_CROSS_ISOLATE_IDEMPOTENCY` 誠實標記為 **`PARTIAL`**（非ACTIVE，非NOT_SOLVED）。KV outage時 fail OPEN（事件仍被接受，不阻擋debug-only功能），never fail closed。既有Debug API JSON schema完全不變（測試證實向下相容）。新增52項測試（原33項），涵蓋施工令20項清單（首次請求、同isolate duplicate經L1、模擬新isolate/重啟duplicate經L2、同eventId不同lifecycle分開判定、無效auth/payload零KV操作、duplicate零額外寫入、accept恰好1次寫入、TTL斷言、KV前綴隔離、schema向下相容、KV outage fail-open）。NEW FAILURES=0（1404項）。本輪**未觸碰**：LINE、CCTV、Shared Feed、Business KV、正式Broadcast Pipeline、既有PBS 30分鐘輪詢閘門、Windows Prototype本身的程式碼/Secret/Task Scheduler。詳見 `07_KNOWN_ISSUES.md` 的完整記錄 |
| **V1.9.6** | `a674b47` | **PBS Windows Local Edge Debug Push Integration（治理封版，2026-08-27）** — 把 2026-08-26～2026-08-27 完成的 Windows 本機邊緣篩選＋Cloudflare Debug Push 架構正式寫入 Engineering Memory；**本輪不 merge feature branch、不新增 Cloudflare runtime 變更、不整合 LINE/CCTV/Business KV、不退休既有 PBS 輪詢**。Windows 端最新事實（本 Cloud Session 已獨立唯讀驗證）：`feature/pbs-local-edge-filter-prototype` 分支最新 commit `95ecdc4718f836ff36c974e829b549f262e6b936`，`git merge-base --is-ancestor` 確認**尚未 merge main**，`git worktree` 乾淨簽出跑 `node --test` 實測 **118 項全通過、0 失敗**（前一輪缺 `cache.js` 的既有落差已在此 commit 補上）。完整架構：PBS官方來源→Windows每3分鐘抓取→Local Edge Filter（**現直接 import 並重用 Production 自己的 `src/pbs/hsinchuFilter.js#isPbsEventHsinchuRelevant`／`src/pbs/roadName.js#normalizePbsRoad`**，修正舊版寬鬆矩形誤收鶯歌55.8K／楊梅68.1K的真實 bug）→事件生命週期比較（CLEARED 現需明確解除文字立即觸發，或連續 2 輪成功 fetch 缺席才 `CONFIRMED_CLEARED`，修正舊版單輪缺席即誤判的 bug，本 Session 已讀程式碼確認兩處邏輯皆存在）→ SHOULD_PUSH 判斷→僅 NEW/UPDATED/CLEARED 才呼叫 Windows Debug Push Client（5000ms timeout、最多2次嘗試、只重試 timeout/network/5xx）→ `POST /internal/pbs-debug-push`（V1.9.5 既有 Cloudflare Debug-only Receiver，本輪程式碼未修改）。真人已啟用 `PBS_DEBUG_PUSH_ENABLED=true`，`WINDOWS_REAL_DEBUG_PUSH=ACTIVE`，`WINDOWS_TO_CLOUDFLARE_DEBUG_CHANNEL=VERIFIED`（人類回報 NEW/UPDATED/CLEARED/DUPLICATE 四種情境 Production mock 皆 PASS，Workers Logs 交叉驗證、LINE/CCTV/R2/Shared Feed/Business KV 側效應皆為0）。記錄一次真實 Secret 治理事故並訂為永久規則：Cloudflare Secret 曾只存在於新 Version（`9ddc58ea`）而 Active Deployment 仍是舊版（`47f54b17`），導致持續 503——**Secret 在 Dashboard 存在 ≠ Active Version 已取得它**，真人 promote 後才恢復正常。標記待辦：`PERSISTENT_CROSS_ISOLATE_IDEMPOTENCY = PENDING_BEFORE_PRODUCTION`（目前僅 per-isolate 非持久判斷，正式 LINE 整合前必須解決）。`PBS_30_MIN_POLLING = PRESERVED`（Cloudflare 既有 PBS 輪詢完全保留，仍是正式路徑，退休時機在六階段路線圖 Phase 6，不得提前）。詳見 `07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md` 的完整記錄 |
| **V1.9.5** | `56b24d3` | **Windows → Cloudflare Debug-only Push Endpoint（2026-08-27）** — 新增 `POST /internal/pbs-debug-push`（`src/pbs/debugPush.js`＋`src/pbs/debugPushAuth.js`），只證明一件事：Windows PBS Local Monitor 發出最小事件 payload → Cloudflare 驗證身份 → 驗證資料格式 → 做冪等判斷 → 寫 Workers Logs → 回 ACK。本輪**不**讓 Windows 真的發送任何真實事件（`WINDOWS_PUSH_ENABLED = NO`），**不**整合進正式 Pipeline。身份驗證：獨立 Secret `PBS_DEBUG_PUSH_SECRET`（`Authorization: Bearer <secret>`，沿用既有 `TRAFFIC_FEED_SECRET` 的 header 慣例），以雜湊後常數時間比對（與 `security/adminAuth.js` 同技術）驗證，明確以測試證實不會回退到 `PBS_RELAY_TOKEN`／`ADMIN_PASSWORD`／任何 LINE·TDX secret；secret 未設定 = 503（fail closed），token 缺失或錯誤 = 401。Payload 必填 `generatedAt`／`source`（僅限 `'pbs'`）／`eventId`／`lifecycle`（僅限 `NEW`／`UPDATED`／`CLEARED`）／`fingerprint`／`requestId`；`event` 為選填、白名單讀取（`road`／`areaNm`／`direction`／`comment`／`longitude`／`latitude`／`sourceDetail`，僅供 log 使用），body 上限 16 KiB（遠大於單一事件所需，遠小於整包 PBS raw feed）。冪等判斷：Cloudflare Workers isolate 無法可靠跨 request 去重，本輪依施工令自身指示**不**為此新增任何 KV 寫入，改採 per-isolate 記憶體內 fingerprint Map 做 best-effort 判斷，誠實回報 `PBS_DEBUG_PUSH_IDEMPOTENCY_MODE = 'NOT_PERSISTENT'`，不假裝有跨 isolate 保證。結構性 Debug-only 邊界：此模組完全不 import LINE／CCTV／Shared Feed／Incident Suppression／notified-state／Broadcast Provenance／Pipeline Trace／PBS lifecycle 或 pipeline 任何一個模組，也完全不觸碰 `env.TRAFFIC_KV`（或任何其他 binding）——不是靠可能被忘記的執行期旗標，而是連 import path 都不存在。新增 33 項測試（`test/pbsDebugPush.test.js`，涵蓋施工令 CASE A–R 加上 auth／schema／冪等／secret 不外洩／routing 邊界情境），以 counting mock 直接確認 NEW／UPDATED／CLEARED 三種情境下 fetch 呼叫與 KV get／put 呼叫皆為 0 次。既有測試套件無 NEW FAILURES（1385 項／1352 pass／33 fail，與變更前基線的失敗清單逐項比對完全相同）。本輪**未觸碰**：LINE、CCTV、Shared Feed、正式 Pipeline、正式 KV business event state、既有 PBS 30 分鐘輪詢閘門（V1.9.3 不變）、TDX、Cron、Windows Prototype 與其 `feature/pbs-local-edge-filter-prototype` 分支（未 merge）。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |
| **V1.9.4** | `3075114` | **Pipeline Trace 讀取效能優化（2026-08-27）** — V1.9.3 上線後真人回報全站 `ERR_CONNECTION_TIMED_OUT`，經查修令確認與程式碼無關（sandbox 無法連 Production 驗證，詳見該輪報告）；隨後真人在 Production 實測出真正的效能問題：`/`≈0.8s／`/version`≈0.4s／`/health`≈0.75s，但 `/admin/pipeline-trace` 與 `/admin/pipeline-trace-view` TTFB 皆 ≈59.1s。確認的 Root Cause（讀程式碼得出，非猜測）：`listPipelineTrace` 的舊 `collectFlattenedTraceEntries` 一律**循序**（一次一個 `await kv.get()`）解碼到 `MAX_ENTRIES_SCANNED`（500）筆，**在**分頁 `limit`（預設 60）套用**之前**，不論有沒有設篩選條件——一個完全沒篩選、只要「最新 60 筆」的頁面，每次都要付出 500 次循序 KV 往返。修復：新函式 `scanTraceEntriesProgressively` 把工程令的三個「刀口」合併成同一個機制：(1) 提前停止——無篩選查詢一旦解碼滿 `boundedLimit` 筆就立刻停止，實測 500 把 key、預設 limit 60 時只做 60 次 `kv.get()`（原本 500 次）；(2) 漸進式掃描——有篩選時以「輪」為單位、累積目標逐輪翻倍（第一輪 = `boundedLimit + NO_FILTER_SCAN_BUFFER`(20)，之後每輪 ×`PROGRESSIVE_SCAN_GROWTH_FACTOR`(2)，上限仍是 `MAX_ENTRIES_SCANNED`），不再一開始就固定掃 500；(3) 有界並行讀取——同一輪內的 `kv.get()` 改為固定 `PARALLEL_GET_BATCH_SIZE`(20) 筆一批、以 `Promise.all` 並行送出，批與批之間循序，絕不把整輪（更別說全部 500 筆）一次性 `Promise.all`；20 是實測挑選（本專案原本沒有既有的 KV／Workers 併發上限可參考），落在施工令建議的 20–30 區間中段。V1／V2 兩種 schema 並存策略完全不變（V1 舊制 `debug:pipeline-trace:v1:*` 仍不刪除不遷移、繼續靠既有 24h TTL 自然過期），僅兩個前綴各自的 `kv.list()` 列舉改為並行（`Promise.all`）而非循序。新增可觀測性（全部讀既有已算出的數字、**未新增任何 KV 寫入**）：`GET /admin/pipeline-trace` 現在也回傳 `kvListCalls`／`kvGetCalls`／`v1KeysScanned`／`v2BatchKeysScanned`／`v1KeyCount`／`v2BatchKeyCount`／`entriesDecoded`／`entriesMatched`／`readDurationMs`，與原本就存在於函式回傳值、但先前從未進到 JSON 回應的 `scannedKeyCount`／`totalKeyCount`／`scanTruncated` 一起回傳；`/admin/pipeline-trace-view` 頁面底部新增一行純數字診斷條（同樣不含任何密鑰／token／原始 CCTV 網址）。新增 23 項測試（`test/pipelineTraceReadPerformance.test.js`，涵蓋施工令 CASE A–I 決定性 fixture），既有測試套件無 NEW FAILURES（1352 項／1319 pass／33 fail，與變更前基線的失敗清單逐項比對完全相同）。本輪**未觸碰**：Pipeline Trace 寫入路徑（`persistPipelineTraceBatch`／`recordPipelineTrace`／`hasPipelineTraceRelevantChange`）、V1.9.3 的 PBS 30 分鐘排程閘門、Health Snapshot WRITE_ON_CHANGE、Windows Prototype、Windows→Cloudflare Push、LINE、CCTV、TDX、Cron 頻率。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |
| **V1.9.3** | `93efec3` | **KV Write Optimization Phase 2（2026-08-26）** — 延續 V1.9.2 的帳號寫入壓力，本輪再關三個來源：(1) `health:snapshot:v1` 改為 WRITE_ON_CHANGE——比較時排除 `generatedAt`／`tdx`＋`pbs.lastFetchedAt`／`line.lastLinePushAt`，**以及** `tdx`／`pbs.scheduledThisRun`＋`sleeping`、整個 `broadcast` 區塊（這些都只代表「這輪又跑過一次」，不是真正健康狀態）；PBS 的 `pbs` 區塊現在也比照 TDX 做 schedule-skip 時的沿用不變。這個排除清單是本輪自己的決定性 fixture（`test/kvWriteQuantificationV193.test.js`）實測抓到的——若漏排除，一個完全沒變化的 PBS_ONLY 安靜日仍會因為排程狀態每輪翻轉而持續寫入，WRITE_ON_CHANGE 形同虛設；(2) PBS 固定抓取由每 10 分鐘改為每 30 分鐘、且僅 07:00:00–22:00:00 Asia/Taipei（新檔 `src/traffic/pbsSchedule.js`）——Cron 本身仍是每 10 分鐘不變，施工前已確認 `PBS_STALE_THRESHOLD_MS`（2 小時）／`PBS_ABSENCE_GRACE_PERIOD_MS`（24 小時）皆為 wall-clock 而非以 tick 計數，遠大於本輪引入的日間 30 分鐘與夜間約 9 小時空窗，LINE Push 本身已限定 08:00–21:59:59，07:00 恢復抓取還有整整一小時緩衝；`lifecycle.js` 的 `commitPbsLifecycleState` 同時開始回報真實的每 UID 新增／更新／新解除筆數；(3) Pipeline Trace 新增 `NO_RELEVANT_CHANGE`（`pipelineTrace.js` 的 `hasPipelineTraceRelevantChange`）——本輪若無新增／更新／解除的 TDX 或 PBS 事件、無 TDX 重複或 PBS 國道閘門排除、且無 LINE 推播嘗試，整批 Trace 寫入直接跳過，不再讓同一筆持續存在但完全沒變的事件每輪都重寫一次；TDX 重複與 PBS 閘門排除仍視為「有意義」（保留既有 V1.8.6.7 查修保證，在目前 `PBS_ONLY` 正式環境下這兩者永遠是 0，不影響本輪省下的量）。新增 `[cron][pbs-schedule]` 低成本 log（`pbsFetchPerformed`／`pbsFetchSkippedSchedule`，不新增 KV key）；`/health` 新增 PBS 對應的「本輪執行狀態」／「最近一次擷取時間」列（比照既有 TDX 顯示）。新增 4 個測試檔（`pbsSchedule.test.js`、`healthSnapshot.test.js` 新增項、`pipelineTraceNoRelevantChange.test.js`、`kvWriteQuantificationV193.test.js`——後者以決定性 fixture 跑完整天 144 次真實 Cron tick，實測 QUIET/NORMAL/HIGH EVENT DAY writes/day 分別為 5／21／27，遠低於施工令 ≤150／100–300／<500 目標），既有測試僅在其假設被本輪行為改變（PBS 不再 24/7）時更新，NEW FAILURES = 0。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |

## 版本線校正紀錄（PRODUCTION_VERSION_LINEAGE_RECONCILIATION，2026-08-25）

V1.8.7.8～V1.8.7.14 這七列是**補記**，不是新發布。七次變更早在補記之前就已 merge 進 `main`
並由 Cloudflare Workers Builds 自動部署；當時只寫了任務名稱、沒有配正式版本號。
本次校正只建立「版本號 ↔ 既有 commit ↔ 既有部署」的對照，
**沒有 rebase、沒有 amend、沒有 force push、沒有改動任何歷史 commit，也沒有假裝任何歷史版本重新部署。**

同時發現一個比漏編號更嚴重的問題：**唯一權威來源 `src/version.js` 從 2026-08-21 的
V1.8.6.9 之後就沒再更新過**——也就是說整個 V1.8.7.x 系列（不只是 V1.8.7.7 之後）
`GET /version` 都在回報 V1.8.6.9。漂移的成因是有三個地方各自以為自己知道版本：

| 來源 | 當時的值 | 現在 |
|---|---|---|
| `src/version.js` → `GET /version`／`/admin/deployment-status` | `V1.8.6.9` | **唯一權威**，已校正為 `V1.8.7.14` |
| Engineering Memory export（掃 commit message 找 `V\d+\.\d+\.\d+`） | `V1.8.7.7` | 改讀 `src/version.js`，掃描結果降級為 drift 警告 |
| `ENGINEERING_STATUS.md` 人工標記 | 各自為政 | 僅供交叉檢查，不具權威 |

**從 commit message 掃出來的版本號，是沒有人負責的版本號**——有人剛好打了就會動，沒人打就停住。
`test/versionLineage.test.js` 七項測試鎖住的是這條規則的**形狀**（單一來源、其他一律衍生），
刻意不鎖當下的數字，否則只會多出第四個要同步的地方。

**永久規則**：任何進 Production 且改變 runtime 行為的變更，必須在同一個 commit 內 bump
`src/version.js` 的 `APP_VERSION`。任務名稱（`CCTV_METADATA_RECOVERY`、
`PBS_ACCIDENT_CCTV_ENRICHMENT_FIX` …）是工程標籤，**永遠不能取代版本號**。
純文件／治理／Drive sync 工具／測試整理不 bump 版本，但仍須有 commit。

## 版本編號格式切換｜三段式版本治理（2026-08-25 起生效）

四段式 `V1.8.7.x` 版本線於 **V1.8.7.14 正式停止（`LAST_FOUR_PART_VERSION`）**，
`FOUR_PART_VERSIONING = RETIRED`。自此起 `THREE_PART_VERSIONING = ACTIVE`，
下一個正式版本號是 **`V1.9.0`（`NEXT_RELEASE_VERSION`）**。

**`src/version.js` 目前仍是 `V1.8.7.14`，本次切換沒有提前 bump。**
只有下一次真正改變 Production runtime 行為的 release，才會在**同一個
commit 內**把 `APP_VERSION` 換成 `V1.9.0`——治理決議本身不觸發版本號變動、
不觸發 deploy。

三段式規則：

| 變更類型 | 動哪一段 | 例 |
|---|---|---|
| Bug fix | patch | `V1.9.0 → V1.9.1 → V1.9.2` |
| 明顯新功能／架構階段 | minor | `V1.9.x → V1.10.0` |
| 大型不相容版本 | major | `→ V2.0.0` |
| 純文件／治理／Engineering Memory／測試整理 | 不升版本 | 仍須有 commit |

V1.8.7.8～V1.8.7.14 這既有七列**不重寫**，四段式版本線就此封存於歷史。

## 版本線之外的重要里程碑

- **V57 系列**：Shared Traffic Feed 首次建立（`completedProducts` 持久化機制），V57.1（Shared-Feed-only CCTV top-up）、V57.3（分頁修復）陸續強化。詳見 `02_PROJECT_HANDOFF.md` §30。
- **四部門聯合治理**（2026-08-22）：非程式版本，但是本專案第一次正式治理框架定案，見 `01_FOUR_DEPARTMENT_GOVERNANCE.md`。
- **Meeting Room Export / Engineering Memory v1**（本輪）：本 export 系統本身的建立版本。

## 目前 main 最新狀態

Export 產生時的即時 git 狀態見 `SYSTEM_STATE.json`；此表格為人工整理的「重要節點」，不會每次 export 自動改寫——只在真的有新的架構意義版本產生時才手動追加一列。
