{
  "schemaVersion": 1,
  "project": "traffic-reporter",
  "verifiedFromRepoAt": "2026-09-04T11:00:00.000Z",
  "verifiedFromCommit": "96b5f44 (V2_4_8_TDX_KM_FALLBACK_PRODUCTION_RUNTIME_DIAGNOSIS; this snapshot precedes the V2.4.10 commit it ships in)",
  "note": "Everything in this file is derived from wrangler.jsonc/src/index.js in THIS repo checkout, verifiable by any future reader by re-running the export script. Fields the repo cannot itself verify (real Cloudflare Dashboard state) are explicitly marked unverified, never guessed.",
  "worker": {
    "name": "traffic-reporter",
    "entryPoint": "src/index.js",
    "compatibilityDate": "2026-08-15",
    "publicBaseUrl": "https://traffic-reporter.mr-happytan.workers.dev"
  },
  "cron": {
    "expression": "*/10 * * * *",
    "note": "UTC in Cloudflare config. V1.9.8: Cloudflare's own PBS 30-minute fetch is RETIRED (src/pbs/pbsConfig.js#PBS_30_MIN_POLLING_ENABLED=false) — PBS's production main line is now the Windows push ingress (POST /internal/pbs-debug-push), not this Cron tick; the code path is preserved, not deleted, for rollback. TDX remains gated inside the scheduled handler to every 2nd tick (minute 00/20/40) and 08:00-21:59:59 Asia/Taipei only (src/traffic/tdxSchedule.js) — unaffected by the PBS retirement. Declared schedule from wrangler.jsonc -- verified from repo; the REAL Cloudflare Cron Trigger config is dashboard-only and not verifiable from here."
  },
  "bindings": {
    "kv_namespaces": [
      {
        "binding": "TRAFFIC_KV",
        "id": "a763ccea75b0481aa4da99fa43f8341a",
        "source": "wrangler.jsonc"
      }
    ],
    "r2_buckets": [
      {
        "binding": "CCTV_IMAGES",
        "bucketName": "traffic-reporter-cctv-images",
        "source": "wrangler.jsonc"
      }
    ],
    "vpc_services": [
      {
        "binding": "PBS_RELAY_WINDOWS",
        "serviceId": "01a008ab-4cc4-7c22-a747-4e27cdcc83c8",
        "source": "wrangler.jsonc"
      }
    ],
    "ai": [
      {
        "binding": "AI",
        "source": "wrangler.jsonc",
        "note": "V1.9.9 Phase 3B: declared in wrangler.jsonc for @cf/zai-org/glm-4.7-flash driver-impact decisions (src/pbs/aiDecisionEngine.js). Declaring this binding does NOT activate AI decisions -- the real gate is the code-level kill switch src/pbs/aiConfig.js#PBS_AI_DECISION_ENABLED (default false). GPT Work confirmed the Dashboard-side binding is ACTIVE. V1.9.9 Phase 3D hotfix: resolvePbsAiDecisionEnabled() previously only accepted a real boolean; Cloudflare Dashboard/CLI Variables inject this kind of runtime flag as a STRING, so a Dashboard value of \"true\" silently stayed disabled. Fixed to also accept the string forms 'true'/'false' (case-insensitive, trimmed), still failing safe to false for anything else. V2.0.0 MILESTONE: GPT Work reports AI_DECISION=ACTIVE (PBS_AI_DECISION_ENABLED=\"true\" in Production) -- see v200MilestoneProductionLineage below; this repo checkout has not independently confirmed it. V2.0.2 Config Drift Hotfix: the Dashboard-side \"true\" GPT Work set was silently removed by a later Workers Builds deploy (wrangler.jsonc is authoritative on every deploy, same mechanism as TRAFFIC_SOURCE_MODE's own var) -- PBS_AI_DECISION_ENABLED is now declared canonically in wrangler.jsonc's vars block (see vars.PBS_AI_DECISION_ENABLED below), and the Dashboard is no longer used to set it long-term."
      }
    ],
    "vars": {
      "PUBLIC_BASE_URL": "https://traffic-reporter.mr-happytan.workers.dev",
      "TRAFFIC_SOURCE_MODE": "PBS_ONLY",
      "LINE_PUSH_POLICY": "MAJOR_ACCIDENT_ONLY",
      "PBS_AI_DECISION_ENABLED": "true",
      "TDX_ROADEVENT_FETCH_ENABLED": "true",
      "TDX_ROADEVENT_QUEUE_INGRESS_ENABLED": "true",
      "TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED": "true",
      "TDX_CCTV_METADATA_REFRESH_ENABLED": "false"
    },
    "varsNote": "These are the runtime flags that decide what Production actually does, so they belong in the manifest rather than only in wrangler.jsonc. TRAFFIC_SOURCE_MODE=PBS_ONLY stops every TDX API call; LINE_PUSH_POLICY=MAJOR_ACCIDENT_ONLY narrows proactive LINE push to accidents; PBS_AI_DECISION_ENABLED gates Windows PBS AI driver-impact decisions (must be the string 'true' to activate, never a bare JSON boolean -- Cloudflare injects Workers Variables as strings). All three are substituted from wrangler.jsonc by the export script, never hardcoded here. V2.0.2: PBS_AI_DECISION_ENABLED's canonical source moved from a Dashboard-only Variable (silently dropped by every Workers Builds deploy) to this wrangler.jsonc declaration -- see PRODUCT_DECISIONS.md / 07_KNOWN_ISSUES.md for the Config Drift Hotfix history. V2.4.0 added three granular switches (TDX_ROADEVENT_FETCH_ENABLED/TDX_ROADEVENT_QUEUE_INGRESS_ENABLED/TDX_CCTV_METADATA_REFRESH_ENABLED, all default \"false\"); V2.4.1 added a fourth, TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED (also default \"false\" when first built). On 2026-09-01 (V2_4_1_PRODUCTION_NOTIFY_CANONICAL_RECONCILIATION), FETCH/QUEUE_INGRESS/PRODUCTION_NOTIFY were all set \"true\" under explicit human authorization. V2.4.4 (2026-09-02, TDX_SCOPE_POLICY_AND_MESSAGE_FIDELITY_FIX): a real service-area leak (台61線 39.6K, actually Taoyuan) plus routine-road-management/message-fidelity defects were found and fixed at the code layer (see 07_KNOWN_ISSUES.md), and TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED was deliberately reverted to \"false\" as this round's own safety deliverable -- FETCH/QUEUE_INGRESS stay \"true\" so Production continues to observe real TDX data through the new gate/policy fixes; re-enabling notify requires a separate future human+Claude Browser decision after that observation. TDX_CCTV_METADATA_REFRESH_ENABLED remains \"false\" (CCTV metadata refresh stays manual/on-demand). V2.4.5 (2026-09-02, TDX_HSINCHU_GEO_RESOLVER + TDX_ROAD_MANAGEMENT_POLICY_GATE): replaced V2.4.4's denylist-only service-area patch with a genuine official-boundary-backed positive-authority resolver plus a deterministic road-management policy gate (see v245TdxHsinchuGeoResolver below) -- these three switches deliberately stay exactly as V2.4.4 left them (FETCH/QUEUE_INGRESS \"true\", PRODUCTION_NOTIFY \"false\"); this round's own deliverable is the geography/policy fix itself, not a switch flip. V2_4_5_SEAL_DEPLOY_AND_REAL_WORLD_VERIFY (2026-09-02, explicit human authorization, see sealDeployAndRealWorldVerify below) flipped TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED to \"true\" (this manifest's own vars block above was previously stale at \"false\" and is corrected in this V2.4.6 sync -- the real wrangler.jsonc value was already \"true\"). V2.4.6 (2026-09-03, TRACE_PAGE_TDX_AND_DECISION_REASON_SUMMARY) is UI/observability-only and did not touch any of these switches.",
    "secretsDeclaredExternally": [
      "TDX_CLIENT_ID",
      "TDX_CLIENT_SECRET",
      "LINE_CHANNEL_ACCESS_TOKEN",
      "LINE_CHANNEL_SECRET",
      "ADMIN_PASSWORD",
      "TRAFFIC_FEED_SECRET",
      "PBS_DEBUG_PUSH_SECRET"
    ],
    "secretsNote": "Names only -- never values. Presence-only verifiable via GET /admin/deployment-status; correctness of any value is never verifiable from code.",
    "queues": {
      "producers": [
        {
          "binding": "PBS_AI_QUEUE",
          "queue": "pbs-ai-processing-queue",
          "source": "wrangler.jsonc"
        }
      ],
      "consumers": [
        {
          "queue": "pbs-ai-processing-queue",
          "maxBatchSize": 1,
          "source": "wrangler.jsonc"
        }
      ],
      "note": "QUEUE_COUNT=1 — introduced V2.3.0 (ctx.waitUntil() -> Cloudflare Queue for AI background processing), never a second queue. V2.4.0 reuses this exact same queue/binding for TDX freeway/highway RoadEvent ingress (src/tdx/tdxQueueIngress.js) — never a parallel queue for TDX. BROWSER_ACTION_REQUIRED: the real Cloudflare Queue resource must exist via Dashboard/`wrangler queues create`; this repo checkout cannot verify or create it."
    }
  },
  "routes": {
    "public": [
      {
        "path": "/",
        "method": "GET",
        "auth": "none"
      },
      {
        "path": "/version",
        "method": "GET",
        "auth": "none",
        "note": "deployedCommit/deployedBranch/buildTime/appVersion"
      },
      {
        "path": "/health",
        "method": "GET",
        "auth": "admin-basic"
      },
      {
        "path": "/webhook",
        "method": "POST",
        "auth": "line-signature"
      },
      {
        "path": "/internal/shared-feed",
        "method": "GET",
        "auth": "bearer:TRAFFIC_FEED_SECRET",
        "note": "Shared Traffic Feed consumer read path"
      },
      {
        "path": "/internal/pbs-debug-push",
        "method": "POST",
        "auth": "bearer:PBS_DEBUG_PUSH_SECRET",
        "note": "V1.9.8: Windows PBS Local Monitor -> formal Production ingress (src/pbs/debugPush.js) — an accepted NEW/UPDATED event reaches the canonical Business Pipeline (runLineBroadcast/Shared Feed); CLEARED is acknowledged but never broadcast; duplicates are idempotent via TRAFFIC_KV under debug:pbs-push-idempotency:v1:*. V1.9.9 Phase 3B: when the code-level kill switch PBS_AI_DECISION_ENABLED is true (Production default: false), this same endpoint instead routes the event through resolveAiDecision() (src/pbs/aiDecisionEngine.js) and, on a validated notify:true verdict, runAiApprovedPbsBroadcast() (src/traffic/aiApprovedPbsBroadcast.js) — mutually exclusive with the legacy runLineBroadcast() call for that same event, never both. V2.0.1: also writes ONE thin AI Decision Observatory index record (debug:pbs-ai-observatory-index:v1:*, 48h TTL, src/pbs/aiObservatoryIndex.js) per genuinely accepted non-duplicate event — see GET /admin/pbs-ai-observatory-view below."
      },
      {
        "path": "/cctv/image/:id",
        "method": "GET",
        "auth": "none",
        "note": "opaque 128-bit id, 15-min TTL enforced in code"
      }
    ],
    "admin": [
      "/debug/tdx",
      "/debug/status",
      "/debug/pbs",
      "/debug/pbs-vpc-probe",
      "/admin/cctv-probe",
      "/admin/cctv-hsinchu-probe",
      "/admin/cctv-hsinchu-collage",
      "/admin/cctv-hsinchu-publish-test",
      "/admin/cctv-hsinchu-frame/0..3",
      "/admin/deployment-status",
      "/admin/deployment-status-view",
      "/admin/pipeline-trace",
      "/admin/pipeline-trace-view",
      "/admin/broadcast-provenance",
      "/admin/pbs-ai-observatory-view"
    ],
    "adminAuth": "HTTP Basic, fixed username 'admin', Cloudflare Secret ADMIN_PASSWORD, timing-safe SHA-256 comparison (src/security/adminAuth.js)"
  },
  "deployment": {
    "method": "push to main -> Cloudflare Workers Builds auto-deploy",
    "verificationScripts": [
      "npm run check:deployment-policy (local, 0 network)",
      "npm run verify:production (calls GET /version)"
    ],
    "buildMetadataSource": "scripts/generateBuildMetadata.mjs, run via package.json predeploy hook"
  },
  "v202ConfigDriftHotfix": {
    "note": "V2.0.2 (2026-08-29). Root cause: PBS_AI_DECISION_ENABLED lived ONLY as a Cloudflare Dashboard Variable from V1.9.9 Phase 3D through V2.0.1, never in wrangler.jsonc. Workers Builds treats wrangler.jsonc as authoritative on every deploy (same mechanism as TRAFFIC_SOURCE_MODE's own long-standing var), so GPT Work's manually-set \"true\" was silently dropped by a later deployment and AI decisions fell back to the code-level default (false) without anyone changing the switch on purpose.",
    "fix": "wrangler.jsonc's vars block now canonically declares PBS_AI_DECISION_ENABLED as the string \"true\". PBS_AI_DECISION_ENABLED_SOURCE=WRANGLER_CANONICAL_VAR, DASHBOARD_ONLY_AI_SWITCH=RETIRED, KEEP_VARS=NOT_USED.",
    "regressionGuard": "scripts/check-deployment-policy.mjs#checkPbsAiDecisionEnabledVar() now fails loudly if this var is ever removed from wrangler.jsonc.",
    "firstRealAiEventNote": "A 台68 event observed at 17:49 during the config-drift window is NOT counted as a genuine AI decision -- PBS_AI_DECISION_ENABLED had already been dropped back to false by that point, so that event was decided by the legacy path, not Workers AI. FIRST_REAL_AI_EVENT remains WAITING.",
    "knownIssueRecordedNotFixed": "PBS_PRECISE_COMMENT_LOCATION_NOT_USED_BY_LINE_FORMATTER -- the LINE message formatter does not yet surface a PBS comment's own precise interchange/ramp text (e.g. 近竹科匝道) even when present in the source comment. Unrelated to this round's config fix, deliberately not addressed here."
  },
  "v200MilestoneProductionLineage": {
    "note": "V2.0.0 (2026-08-28) is an architecture-generation milestone (Windows PBS Local Edge -> Cloudflare ingress -> Workers AI -> AI driver-impact decision -> existing LINE infrastructure), not a runtime-logic change in this round. The fields below are what GPT Work reported for the Cloudflare Dashboard side; this repo checkout cannot independently verify any of them (sandbox network egress blocks the Production domain and Dashboard) -- recorded as human-reported, never claimed as self-verified.",
    "reportedByGptWork": {
      "activeProductionVersionId": "a8e9454c-ab3f-4555-ab7e-0d8c39ecf73c",
      "activeTrafficPercent": 100,
      "aiBinding": "ACTIVE",
      "pbsAiDecisionEnabledVariable": "\"true\"",
      "productionHealth": "PASS",
      "workerErrors": 0,
      "ai429Count": 0,
      "aiInvalidResponseCount": 0
    },
    "firstRealAiEvent": "WAITING -- a real Production PBS event flowing end-to-end through Workers AI to a LINE push has not yet been observed. Next observational milestone, not a V2.0.0 seal blocker.",
    "instructionForNextAgent": "If a future repo push triggers a new Workers Builds deployment, update activeProductionVersionId here once GPT Work reports the new Version ID -- this is Dashboard-verification work, not something Claude should attempt from this sandbox."
  },
  "dashboardOnlyChecksNeverVerifiableFromRepo": [
    "Cloudflare Workers Builds 'Production branch' setting actually pointed at main",
    "Real configured Cron Trigger schedule (vs. what wrangler.jsonc declares)",
    "Real traffic percentage split (gradual/staged rollouts)",
    "Whether a Cloudflare Secret is set to the CORRECT value (only presence is checkable)",
    "Build/Deployment history, logs, failure reasons"
  ],
  "v240TdxUnifiedAiPipeline": {
    "note": "V2.4.0 (2026-09-01, Phase B). TDX freeway/highway RoadEvent rejoins the same Cloudflare Queue (PBS_AI_QUEUE) and the same one AI decision engine PBS Windows ingress already uses, coordinated across sources by a new Recent Incident Memory (src/traffic/incidentMemory.js, Cloudflare KV, 8h TTL) instead of the retired V1.5 hard-rule pipeline.",
    "phaseGate": "suppressLineNotify = source === \"freeway\" || source === \"highway\" is hardcoded at the one call site in src/pbs/debugPush.js, never a wrangler.jsonc variable. AI genuinely runs and Recent Incident Memory genuinely reads/writes for a TDX-origin event, but no real LINE push happens for it yet (Phase B). Reaching Phase C requires an actual future code change removing that hardcoded true, never a config flip.",
    "legacyRetirement": "LEGACY_TDX_LINE_PIPELINE=RETIRED_FOR_ROADEVENT -- src/traffic/scheduled.js's broadcastEvents no longer includes summary.allEvents (TDX's own fetched events); even with TDX_ROADEVENT_FETCH_ENABLED alone flipped on, a TDX RoadEvent can no longer reach the old V1.5 hard-rule LINE path.",
    "currentRuntimePhaseHistorical": "As first built (2026-09-01, this note's own original text): NOT_ACTIVATED -- all three switches then existing defaulted to \"false\"; the mechanism was built but 0 real TDX fetch/enqueue/CCTV-metadata-refresh calls happened until a separate, explicit human decision turned one on.",
    "currentRuntimePhase": "PHASE_E_TDX_NOTIFY_LIVE (2026-09-02, V2_4_5_SEAL_DEPLOY_AND_REAL_WORLD_VERIFY, explicit human authorization) -- see SYSTEM_STATE.json's own phase-history blocks (V2_4_0_PHASE_A / V2_4_0_PHASE_B / V2_4_1_PHASE_C / V2_4_1_PRODUCTION_NOTIFY_CANONICAL_RECONCILIATION / V2_4_4_TDX_SCOPE_POLICY_AND_MESSAGE_FIDELITY_FIX / V2_4_5_TDX_HSINCHU_GEO_RESOLVER / sealDeployAndRealWorldVerify below) for the full phase-by-phase history. All four TDX/AI switches now \"true\": TDX_ROADEVENT_FETCH_ENABLED, TDX_ROADEVENT_QUEUE_INGRESS_ENABLED, TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED, PBS_AI_DECISION_ENABLED. TDX_CCTV_METADATA_REFRESH_ENABLED stays \"false\". A TDX-origin AI notify:true event now reaches real LINE subscribers. This session has NO Production network access (verify:production reports PASS_NETWORK_VERIFICATION_BLOCKED) -- real-world observation must be performed by a human; the documented rollback protocol (flip PRODUCTION_NOTIFY_ENABLED back to \"false\" FIRST on any anomaly, investigate second) is in 07_KNOWN_ISSUES.md and sealDeployAndRealWorldVerify below.",
    "scopeDiscipline": "Explicitly unchanged: CCTV's entire metadata-cache -> camera-selection -> compose -> R2-put -> R2-read-back-verify -> LINE pipeline (V2.3.3, untouched); VD/CMS/other Traffic APIs (never restored); CCTV metadata refresh Cron (still MANUAL/ON-DEMAND). CCTV_RUNTIME_TDX_CALLS=0. incidentSuppression.js was revised in V2.4.1 (see SYSTEM_STATE.json's own V2_4_1_PHASE_C_PRODUCTION_NOTIFY_IMPLEMENTATION block) -- the AI-approved path now trusts the AI's own sameIncident/materialChange judgment, with only a short 10-minute near-simultaneous-duplicate collision defense remaining.",
    "testEvidence": "New test/tdxUnifiedAiPipeline.test.js (the order's own 17 CASE list) and test/tdxPhaseCProductionNotify.test.js (the order's own 20 CASE list), both fully passing, plus 8+ existing suites retrofitted for the intentional TDX-legacy-retirement. Full regression 1774/1740/34, NEW FAILURES=0 against the pre-round 34-item known-flaky baseline (as of the V2.4.1 code round; this later config-only reconciliation ran targeted verification only, per its own explicit minimal-scope authorization)."
  },
  "v242LineInformationFidelityAndPolicyFix": {
    "note": "V2.4.2 (2026-09-01). TARGETED FIX, no config/binding/var change from V2.4.1 -- bindings.vars above are byte-for-byte unchanged this round.",
    "fixScope": "src/traffic/messageFormat.js gains 3 new SOURCE-FACTS lines (PBS comment fact line, sourceDetail \"通報：XXX\" line, structured blockedLanes line) -- purely additive. src/pbs/aiDecisionEngine.js SYSTEM_PROMPT rewritten (text only, schema/cache/call-sites unchanged) to reframe the AI notify question from \"will this cause congestion\" to \"is this worth a driver knowing ahead of time\", with explicit accident/predictive-hazard/normal-congestion policy anchors.",
    "knownIssueResolved": "PBS_PRECISE_COMMENT_LOCATION_NOT_USED_BY_LINE_FORMATTER (SYSTEM_STATE.json) -- RESOLVED_V2_4_2.",
    "eventReliabilityInvestigation": "EVENT_ID 11509010029-5 (國3 81.3K multi-vehicle collision) -- LINE never sent after Queue retries exhausted. Read-only architecture investigation only (Queue retry design confirmed sound); the specific failure stage/root cause for this historical event could NOT be independently confirmed (no Cloudflare Worker Logs access from this session) -- no reliability code change made. See SYSTEM_STATE.json V2_4_2 block for the full record."
  },
  "v245TdxHsinchuGeoResolver": {
    "note": "V2.4.5 (2026-09-02, MINOR). Core acceptance: TDX must positively prove an event is inside 新竹市 or 新竹縣 before entering AI; inability to prove means no AI entry. PBS entirely out of scope.",
    "geoResolver": "New src/tdx/hsinchuGeoResolver.js#resolveTdxHsinchuGeography() -- three-state CONFIRMED_HSINCHU/OUTSIDE_HSINCHU/UNKNOWN, never a boolean. Only the coordinate tier (checked against the real official 內政部國土測繪中心 boundary polygon, data.gov.tw dataset 7442, mirrored via the taiwan-atlas npm package -- this sandbox cannot reach data.gov.tw directly, human decision recorded in 07_KNOWN_ISSUES.md) can positively confirm. The old KM heuristic table is demoted to observability-only. UNKNOWN behaves exactly like OUTSIDE_HSINCHU downstream (0 Queue/0 AI/0 LINE) -- never assumed Hsinchu.",
    "boundaryData": "SUPERSEDED same-day by V2_4_5_OFFICIAL_HSINCHU_BOUNDARY_DATA_HOTFIX_CONTINUE (see boundaryDataHotfix below) -- this entry's original taiwan-atlas-mirror values are kept in 06_VERSION_HISTORY.md's own historical record, not repeated here to avoid two conflicting 'current' statements in one file.",
    "boundaryDataHotfix": "V2_4_5_OFFICIAL_HSINCHU_BOUNDARY_DATA_HOTFIX_CONTINUE (2026-09-02, same day, no version bump). BOUNDARY_AUTHORITY=內政部國土測繪中心測繪資訊課, BOUNDARY_DATASET=直轄市、縣市界線(EPSG:3824), BOUNDARY_DATASET_ID=7442, BOUNDARY_SOURCE_CRS=EPSG:3824 (TWD97 經緯度), BOUNDARY_INCLUDED_AREAS=新竹市,新竹縣, BOUNDARY_FETCH_DATE=2026-09-02 (human downloaded directly from data.gov.tw and uploaded into this session -- this sandbox still cannot reach data.gov.tw itself), BOUNDARY_SOURCE_VERSION=COUNTY_MOI_1090820 (official ISO 19115 metadata creation/revision date 2020-08-20 -- chronologically EARLIER than the outgoing taiwan-atlas 2021.9.20 mirror; disclosed and investigated, not glossed over -- adopted anyway because it is a direct official download rather than a third-party mirror, is higher-precision unsimplified raw shapefile data [4,027/15,069 points vs the outgoing 246/965], and a systematic geometry diff found no material difference from the outgoing source, consistent with Hsinchu's county-level boundary being unchanged since Taiwan's 2014 restructuring), BOUNDARY_TRANSFORMATION=none (same WGS84/TWD97 engineering judgment as before). geometryDiffCheck: 10/10 reference points identical; Taoyuan-border grid 1/4,131 differ (0.024%); Miaoli-border grid 1/5,751 differ (0.017%); full-region grid 19/47,089 differ (0.0403%), every disagreement an isolated boundary-edge cell -- BOUNDARY_CHANGED=NO. Outgoing taiwan-atlas 2021.9.20 mirror preserved (not deleted) at data/hsinchu-boundary/raw/historical-taiwan-atlas-2021/ for historical comparison; scripts/updateHsinchuBoundaryData.mjs now reads the shapefile via the `shapefile` npm package (new devDependency) instead of topojson-client. resolveTdxHsinchuGeography() itself was NOT rewritten -- only its input dataset changed. Full provenance in data/hsinchu-boundary/raw/SOURCE_META.json.",
    "roadManagementPolicyGate": "New src/tdx/roadManagementPolicyGate.js#resolveTdxRoadManagementEligibility(), runs AFTER the geography gate, BEFORE Queue.send(). Dynamic shoulder open/close never eligible; routine construction eligible only when blockedLanes>=2 (missing/unparseable/negative/fractional -> ineligible, fail-closed). Escape valve (reusing anomalyClassification.js#detectNonCollisionAnomaly plus a narrow accident/complete-closure keyword check) prevents a genuinely major event from being falsely blocked by the routine-construction rule.",
    "gateOrdering": "tdxQueueIngress.js#enqueueTdxRoadEvents() now runs the geography gate first, then the road-management gate, before any Queue.send() -- new droppedOutsideHsinchu/droppedUnknownHsinchu/droppedRoadManagement observability counters. serviceArea.js's TDX branch delegates to the same resolveTdxHsinchuGeography(), so aiCandidate.js's Gate 2 and the existing V2.4.4 Gate 3 in aiApprovedPbsBroadcast.js automatically use the identical canonical result -- no new code needed for the pre-LINE re-check (Gate B).",
    "aiInputChange": "blockedLanes added to aiCandidate.js#buildAiCandidate()/aiDecisionEngine.js#buildAiUserPrompt() as a structured field (always present, null when absent -- same shape as the existing displayKM field). PBS candidates always carry blockedLanes:null (PBS never sets it) -- purely additive, verified byte-shape change only.",
    "preserved": "V2.4.4's resolveHsinchuOnlyProductionEligibility() denylist hard gate and the SYSTEM_PROMPT's fourth semantic anchor (routine road-management events) are both KEPT as a second-layer safety net, not removed -- the new resolver/gate are the primary authority.",
    "unchanged": "Windows PBS, pbs-relay/, PBS local filtering, PBS AI candidate path, PBS LINE path, PBS lifecycle, the Workers AI model itself, the AI prompt's road-management anchor text, Incident Memory, CCTV, LINE messageFormat, the 60-character SOURCE_FACT cap.",
    "deploymentPolicy": "TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED stays \"false\" (FETCH/QUEUE stay \"true\") -- this round's own observation period; re-enabling requires a separate future human decision after confirming Taoyuan/Miaoli/Toufen/Zhunan=0 candidates, genuine Hsinchu events pass normally, and UNKNOWN is correctly intercepted.",
    "testEvidence": "New test/tdxHsinchuGeoResolver.test.js (19 tests, CASE 1-10 plus the permanent 39.6K/桃園觀音 regression lock) and test/tdxRoadManagementPolicyGate.test.js (21 tests, CASE 1-10), both fully passing. Full regression NEW_FAILURES=0 against a git-stash-u same-commit precise baseline (52 pre-existing failures)."
  },
  "sealDeployAndRealWorldVerify": {
    "task": "V2_4_5_SEAL_DEPLOY_AND_REAL_WORLD_VERIFY",
    "completedAt": "2026-09-02",
    "authorization": "explicit human directive -- config-only change (wrangler.jsonc TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED false->true), no new code",
    "preflightConfirmed": "working tree clean, local HEAD == origin/main (31ae4ef at start), APP_VERSION=V2.4.5, Engineering Memory already synced, targeted tests 42/42 pass, check:deployment-policy PASS, wrangler --dry-run PASS",
    "featuresReconfirmed": "All 13 order-listed features verified present via code/test inspection, not re-implemented: TDX coordinate preservation, geo resolver, OUTSIDE_HSINCHU/UNKNOWN drop, 台61線39.6K/頭份/竹南 blocked, dynamic-shoulder-open/close 0 AI, routine-construction 0-1-lane 0 AI / >=2-lane AI-eligible, blockedLanes in AI input, PBS untouched.",
    "deployMechanism": "This project's sole deploy path: push to main triggers Cloudflare Workers Builds auto-deploy -- no manual `wrangler deploy` was run, consistent with 02_PROJECT_HANDOFF.md's own documented deploy flow.",
    "networkLimitationDisclosed": "npm run verify:production reports PASS_NETWORK_VERIFICATION_BLOCKED -- this sandbox has zero direct network access to the Production *.workers.dev endpoint (same constraint documented throughout this project's whole history). Real-world observation of live TDX events/LINE pushes must be performed by a human with Production access; this session cannot self-verify the effect of this flip.",
    "rollbackProtocol": "Permanent rule, takes priority over any fix attempt: on ANY observed anomaly (Taoyuan/Miaoli/Toufen/Zhunan LINE push, shoulder-open/close LINE push, 0-1-lane routine-construction LINE push, a genuine Hsinchu event never reaching AI, a malformed LINE message), the FIRST action is always TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED=false to stop the bleeding -- never fix live in Production. Only then: preserve evidence (eventId/source/road/KM/coordinates/description) -> locate the real pipeline stage -> fix -> targeted test -> regression -> commit -> push -> docs sync -> redeploy -> re-enable.",
    "unchanged": "resolver/gate code themselves, PBS, the other three wrangler.jsonc TDX/AI switches (FETCH/QUEUE_INGRESS/PBS_AI_DECISION_ENABLED all already \"true\", untouched by this round -- only PRODUCTION_NOTIFY_ENABLED's value changed).",
    "appVersion": "unchanged, still V2.4.5"
  },
  "v246TracePageTdxAndDecisionReasonSummary": {
    "task": "V2_4_6_TRACE_PAGE_TDX_AND_DECISION_REASON_SUMMARY",
    "completedAt": "2026-09-03",
    "scope": "UI/observability-only. PBS_RUNTIME_MODIFIED=NO, TDX_DECISION_LOGIC_MODIFIED=NO -- no wrangler.jsonc var changed this round.",
    "tracePageTarget": "GET /admin/pbs-ai-observatory-view (src/pbs/aiObservatoryView.js + aiObservatoryIndex.js), NOT the legacy GET /admin/pipeline-trace-view (that page's buildTraceEntry() has only 3 call sites, all PBS-driven under V2.4.0's LEGACY_TDX_LINE_PIPELINE=RETIRED_FOR_ROADEVENT architecture -- pure TDX events essentially never reach it).",
    "gapFound": "A TDX event dropped at tdx/tdxQueueIngress.js's own Gate A (geography via hsinchuGeoResolver.js, or road-management via roadManagementPolicyGate.js), BEFORE any Queue message exists, previously produced only a console.log line -- ZERO persisted Observatory record. Separately, a TDX event that DID pass Gate A and reach Queue/AI already had a correctly-sourced record, but aiObservatoryView.js#renderRow()'s collapsed <summary> hardcoded the literal string \"PBS\" regardless of record.source, mislabeling it.",
    "fix": "tdxQueueIngress.js#enqueueTdxRoadEvents() now also writes one best-effort Observatory record at each Gate A drop point (reusing aiObservatoryIndex.js's existing buildAiObservatoryRecord/recordAiObservatoryEntry, same KV prefix, no new index) via 6 new AI_OUTCOME values (GEO_EXCLUDED_OUTSIDE_HSINCHU/GEO_EXCLUDED_UNKNOWN/ROAD_POLICY_EXCLUDED_SHOULDER_OPEN/_SHOULDER_CLOSE/_INSUFFICIENT_LANES/_UNKNOWN_LANES), populated purely from the ALREADY-COMPUTED geo/policy gate results -- the drop decision itself is completely unchanged. aiObservatoryView.js#renderRow() now reads record.source through a real SOURCE_LABELS map (PBS / TDX｜高公局 / TDX｜公路局). New aiObservatoryIndex.js#deriveFinalDecisionReason() is the one canonical, pure, read-only composition of 'why sent / not sent' (outcome/eventType/blockedLanes/suppressedForPhase/lineSent only), surfaced as a new collapsed-card summary line for both PBS and TDX records. buildAiObservatoryRecord() also now persists suppressedForPhase and blockedLanes -- two fields debugPush.js/aiCandidate.js already computed but this builder previously discarded (purely additive, no new judgment).",
    "tdxFlowStrip": "aiObservatoryView.js gained a second, TDX-only pipeline-stage flow strip (SOURCE->GEO->ROAD_POLICY->QUEUE->AI->LINE) in the expanded detail view, a completely separate code path from PBS's existing ①-④ strip (untouched).",
    "traceApiUnchanged": "Still GET /admin/pbs-ai-observatory-view, still debug:pbs-ai-observatory-index:v1 KV prefix, still zero Workers AI calls and zero KV writes on open/refresh/filter.",
    "testEvidence": "New test/v246TracePageTdxAndDecisionReasonSummary.test.js (20 tests: the order's own 8 CASE acceptance scenarios plus deriveFinalDecisionReason unit coverage), all passing. Full regression 1758/1726/32, NEW_FAILURES=0 against a git-stash-u same-commit precise baseline (32 pre-existing failures, all CCTV/frame-latency timing-dependent, unrelated to this round).",
    "appVersion": "V2.4.5 -> V2.4.6 (MINOR)"
  },
  "v247TdxGeoInputMissingFix": {
    "task": "V2_4_6_TDX_GEO_INPUT_MISSING_DIAGNOSIS_AND_FIX",
    "completedAt": "2026-09-03",
    "scope": "PATCH. PBS_MODIFIED=NO, GEO_RESOLVER_MODIFIED=NO, ROAD_POLICY_MODIFIED=NO, AI_POLICY_MODIFIED=NO.",
    "triggerEvent": "Real Production event EVENT_ID=A15040100H-01-20260903103244766100023 (TDX｜高公局, 國道三號 北向 79K+000 其他異常告警-散落物) showed GEO=UNKNOWN with areaNm/displayKM/longitude/latitude all empty, even though the free-text description plainly contained '79K+000'.",
    "sectionOneFinding": "Code-path audit of tdx/normalize.js#normalizeRoadEvent()'s structured-field extraction found NO bug -- it is unconditional, every structured KM candidate field was genuinely absent from this event's raw payload. The real gap is structural: tdx/normalize.js never had a description-text KM fallback at all (pbs/normalize.js has long had an analogous one for PBS's own displayKM). NORMALIZE_BUG=NO, TEXT_KM_FALLBACK_ADDED=YES.",
    "secondaryFinding": "tdx/extract.js#firstDefined(raw, paths, undefined) does NOT actually return literal undefined when no candidate matches -- passing undefined explicitly as a JS default-parameter argument still triggers that parameter's own default (fallback=''), so a genuinely-absent startKM/endKM has always resolved to '', never undefined. Harmless everywhere else that already treats '' as absent (composeLocation's filter, parseKM()'s own '' early-return, roadManagementPolicyGate.js's blockedLanes parser) -- NOT re-fixed at its source (out of this round's scope); the new fallback's own trigger condition uses a falsy check (!startKM && !endKM) instead of a strict === undefined check to correctly account for it.",
    "fix": "src/traffic/hsinchuFilter.js gained extractKmTokenFromText(), reusing parseKM()'s own TDX-KM-token shape (never a second/independent format). tdx/normalize.js's normalizeRoadEvent() calls it against `description` ONLY when both structured startKM/endKM candidates are absent -- a genuinely present structured field is NEVER overridden. The recovered token is stored in the exact same raw-string shape ('79K+000') a structured field already carries, so composeLocation()/parseKM()/hsinchuGeoResolver.js's own Tier-2 KM-heuristic tier all keep working unchanged. A new `displayKM` (plain number, same shape pbs/normalize.js's own field already has) is derived from whichever startKM the event ends up with, for trace-page display only.",
    "safetyVerified": "CASE 7/7b in test/v247TdxGeoInputMissingFix.test.js: the recovered KM is read ONLY by hsinchuGeoResolver.js's Tier-2 KM-heuristic tier, which remains permanently OBSERVABILITY-ONLY and can never by itself produce CONFIRMED_HSINCHU. The real trigger event still correctly resolves UNKNOWN (0 Queue, 0 AI, 0 LINE) even after its KM is successfully recovered; only the trace page's own observability improves.",
    "testEvidence": "New test/v247TdxGeoInputMissingFix.test.js (12 tests: CASE 1-7 plus CASE 7b safety verification, plus extractKmTokenFromText unit coverage), all passing. Full regression 1770/1738/32, NEW_FAILURES=0 against a git-stash-u same-commit precise baseline.",
    "appVersion": "V2.4.6 -> V2.4.7 (PATCH)"
  },
  "v248AiLineMessageEditorAndUnifiedPresentation": {
    "task": "V2_4_8_AI_LINE_MESSAGE_EDITOR_AND_UNIFIED_PRESENTATION",
    "completedAt": "2026-09-04",
    "scope": "MINOR. PBS_DECISION_POLICY_MODIFIED=NO, TDX_DECISION_POLICY_MODIFIED=NO, GEO_MODIFIED=NO, ROAD_POLICY_MODIFIED=NO, CCTV_LOGIC_MODIFIED=NO -- no wrangler.jsonc var changed this round.",
    "goal": "PBS and TDX LINE messages had long diverged in layout/tone. This round unifies both into one consistent 'traffic reporter' voice via a new cleanSummary field produced by the SAME single existing Workers AI call (AI_SECOND_CALL_ADDED=NO) -- never a second model invocation.",
    "designPrinciple": "AI is a text editor, never a fact generator. src/pbs/aiDecisionEngine.js's SYSTEM_PROMPT explicitly forbids cleanSummary from inventing/restating road/direction/km/lane-count/casualty numbers. validateAiDecisionResponse() accepts cleanSummary only when it is a non-empty string <= CLEAN_SUMMARY_MAX_CHARS=100, else null. New pure function cleanSummaryContradictsFacts(cleanSummary, candidate) applies two conservative regex checks (lane-count number, direction word) against the candidate's own real blockedLanes/direction fields; any mismatch discards cleanSummary back to null. This check runs completely independently of the notify/impact/reason/confidence decision -- a cleanSummary failure can NEVER block a real notify:true broadcast (text-polish is never allowed to become a single point of notification failure).",
    "presentationFormatter": "src/traffic/messageFormat.js#formatEventMessage() gained a cleanSummary parameter. When present and valid, a new block-based unified layout is used; when absent/invalid, the function falls through to the pre-existing deterministic per-line formatter byte-for-byte unchanged (verified by full regression parity) -- this fallback path is the guaranteed-safe default for every event.",
    "reporterLineChange": "buildReporterLine() (replacing the old buildSourceDetailLine()) now unconditionally shows a source/reporter line: TDX freeway/highway events render '通報：【TDX】高公局' / '通報：【TDX】公路局' -- tdx/normalize.js had never set any sourceDetail field before this round, so TDX events previously showed no reporter line at all. PBS events render '通報：【警廣】' plus the existing reporting-unit alias table, now always shown (with the empty-alias case rendering just the bare prefix) rather than sometimes omitted.",
    "observatorySync": "src/pbs/aiObservatoryView.js gained renderAiTextEditSection(), a new static server-rendered block (no client-side JS, consistent with the existing Admin CSP default-src 'none') showing raw description -> AI-edited cleanSummary -> final rendered LINE text side by side. buildAiObservatoryRecord() gained cleanSummary/finalRenderedMessage fields, purely additive.",
    "unchanged": "PBS/TDX decision policy (the four core AI schema fields' judgment criteria), hsinchuGeoResolver.js, roadManagementPolicyGate.js, CCTV pipeline, all four TDX/AI wrangler.jsonc switches (still all \"true\").",
    "testEvidence": "New test/v248AiLineMessageEditorAndUnifiedPresentation.test.js (18 tests: the order's own CASE 1-14 plus sub-cases 6b/7b/12b/14b), all passing. 6 existing test files updated for the intentional reporter-line/source-labeling behavior change (not regressions): aiDecisionEngine.test.js, messageFormat.test.js, pbsAccidentTraceLocationQuality.test.js, v242InformationFidelityAndPolicy.test.js, dynamicShoulderMessageShort.test.js, pbsLineBroadcast.test.js. Full regression 1788/1756/32, NEW_FAILURES=0 against a git-stash-u same-commit precise baseline (32 pre-existing CCTV/frame-latency timing-dependent failures, unrelated to this round).",
    "appVersion": "V2.4.7 -> V2.4.8 (MINOR)"
  },
  "v248TdxKmFallbackProductionRuntimeDiagnosis": {
    "task": "V2_4_8_TDX_KM_FALLBACK_PRODUCTION_RUNTIME_DIAGNOSIS",
    "completedAt": "2026-09-04",
    "scope": "PATCH, P0 production-anomaly diagnosis order. GEO_RESOLVER_MODIFIED=NO, PBS_MODIFIED=NO, AI_MODIFIED=NO, LINE_MODIFIED=NO, ROAD_POLICY_MODIFIED=NO -- no wrangler.jsonc var changed this round.",
    "triggerSymptom": "Two real Production trace-page TDX events (TDX｜高公局, 國道一號 北向 '101K+300 施工事件-施工維護' and 南向 '100K+000 天候事件-天候不佳') showed displayKM/longitude/latitude as '—' with GEO=UNKNOWN/Gate A excluded, despite V2.4.7's own description-text KM fallback.",
    "rootCauseChain": "Confirmed by direct runtime trace of the real code (not assumed). extractKmTokenFromText()/parseKM() and tdx/normalize.js#normalizeRoadEvent() both correctly recover displayKM (101.3/100) onto the canonical normalized event. src/tdx/hsinchuGeoResolver.js#resolveTdxHsinchuGeography() reads that same real event object directly (never a pseudo-candidate) and correctly still resolves UNKNOWN (no coordinate/text-place evidence) -- CORRECT safety behaviour per V2.4.7's own rule, not a bug, and NOT modified this round. The actual bug: src/tdx/tdxQueueIngress.js#buildTdxPseudoCandidate() -- a LOCAL candidate builder used ONLY for the Gate-A-drop observability write, built in V2.4.6, predating V2.4.7's displayKM field and never updated to carry it forward, so aiObservatoryIndex.js#buildAiObservatoryRecord() (which reads candidate.displayKM) always wrote displayKM:null for any TDX event dropped at Gate A. Events that PASS Gate A and reach AI already showed displayKM correctly (they use the REAL candidate from aiCandidate.js#buildAiCandidate(), carrying displayKM since V2.4.5) -- confirmed unaffected by test CASE 4c.",
    "fix": "buildTdxPseudoCandidate() gained one field, propagating event.displayKM unchanged into the returned pseudo-candidate object, same presence-means-something/absence-means-nothing convention as the pre-existing longitude/latitude fields beside it. Zero change to the Gate A drop decision, the geo resolver, the road-management gate, the Queue, AI, or LINE.",
    "productionVersionDisclosure": "This sandbox has zero direct network access to the Production *.workers.dev domain (curl returns 403 via the egress proxy on every attempt), so GET /version on the live deployment could not be read. This round's diagnosis is based entirely on direct execution of the repo's own code (not assumed behaviour), independent of which commit Production currently runs; the fix reaches Production via this project's sole deploy path (push to main -> Cloudflare Workers Builds auto-deploy).",
    "testEvidence": "New test/v248TdxKmFallbackProductionRuntimeDiagnosis.test.js (8 tests: the order's own §五 CASE 1-5 plus a Gate-A-drop-with-missing-KV regression guard), all passing. Full regression 1796/1764/32, NEW_FAILURES=0 against a git-stash-u same-commit precise baseline.",
    "appVersion": "V2.4.8 -> V2.4.9 (PATCH)"
  },
  "v2410TdxFreewayKmHsinchuDeterministicGeoFallback": {
    "task": "V2_4_10_TDX_FREEWAY_KM_HSINCHU_DETERMINISTIC_GEO_FALLBACK",
    "completedAt": "2026-09-04",
    "scope": "MINOR. PBS_MODIFIED=NO, ROAD_POLICY_MODIFIED=NO, AI_POLICY_MODIFIED=NO, LINE_POLICY_MODIFIED=NO. GEO_RESOLVER_MODIFIED=YES (additive only -- Tier 1/Tier 3's own existing decision logic byte-for-byte unchanged, only one new tier added) -- no wrangler.jsonc var changed this round.",
    "goal": "A common TDX｜高公局 event shape (road+direction+KM, NO coordinates, NO areaNm) stayed permanently UNKNOWN even after V2.4.7's own description-text KM recovery, because the only existing KM tier (traffic/hsinchuConfig.js's own admittedly-unverified table) is deliberately, permanently observability-only. This round adds a SEPARATE, genuinely VERIFIED positive-authority tier for exactly 國道一號/國道三號.",
    "dataProvenance": "Cross-referenced TWO independent official government datasets already bundled in this repo -- 國道百公尺里程樁 (data.gov.tw dataset 95016, 交通部高速公路局; data/road-location/generated/freeway.js, 0.1km-resolution road-centerline coordinates) and 直轄市、縣市界線 (data.gov.tw dataset 7442, 內政部國土測繪中心; data/hsinchu-boundary/generated/hsinchuBoundary.js, the same polygon already bundled since V2.4.5) -- using the SAME isPointInRings() point-in-polygon function hsinchuGeoResolver.js's own Tier 1 coordinate check already trusts. Never a web-search summary, forum, blog, or AI memory as production authority (order section 五's own explicit prohibition).",
    "computedRanges": "Raw computed contiguous spans (every 0.1km milestone point tested, sharp noise-free transitions verified within +/-1.5km of each boundary): 國道一號 75.2K-107.3K, 國道三號 74.6K-109.4K. A 0.5km conservative safety margin (order section 六) trimmed from each end yields the shipped ranges: 國道一號 75.7K-106.8K, 國道三號 75.1K-108.9K -- this margin correctly excludes 香山交流道 (109K, a real interchange known to straddle the 新竹市/苗栗縣 boundary) even though it barely cleared the raw computed boundary, demonstrating the conservative design works as intended.",
    "newModule": "src/tdx/hsinchuFreewayKmRanges.js -- static program data table plus one pure, synchronous, zero-I/O lookup function resolveVerifiedHsinchuFreewayKm({road, displayKM}), reusing traffic/roadIdentity.js#canonicalFreewayRoad() for road normalization (never a second/conflicting parser). scripts/verifyHsinchuFreewayKmRanges.mjs provides an independent, reproducible re-derivation to check for drift whenever either source dataset is refreshed.",
    "resolverIntegration": "hsinchuGeoResolver.js gained a new Tier 4 (order section 二's own LEVEL 3), consulted ONLY when Tier 1 (coordinates) and the existing Tier 3 (explicit place-name text) both found no evidence, and ONLY for event.source==='freeway'. This new tier can NEVER return OUTSIDE_HSINCHU -- a KM outside every verified range, an unrecognized road, or a missing displayKM all mean 'no positive evidence' (null, caller falls to UNKNOWN), never a negative signal (order section 十一). Coordinate priority (order section 三) is satisfied structurally: resolveByCoordinates() already returns immediately when the event carries any coordinate, so the new tier is never even called in that case -- a coordinate/KM conflict cannot arise by construction, no separate GEO_EVIDENCE_CONFLICT arbitration needed.",
    "safetyVerified": "GEO CONFIRMED never bypasses Road Policy or AI/LINE eligibility (order section 十三/十四) -- 國1 101K+300 (routine construction, unrecoverable blockedLanes) now GEO-confirms via the new tier but roadManagementPolicyGate.js (V2.4.5, unchanged) still fail-closes it exactly as before; 國1 100K+000 (weather, not a road-management event type) GEO-confirms and clears Road Policy, reaching the Queue, but whether it actually notifies is still decided entirely by the existing, untouched AI decision engine. All three real Production trigger events from the V2.4.7-through-V2.4.10 saga (101K+300/100K+000/79K+000) now correctly resolve CONFIRMED_HSINCHU.",
    "observatorySync": "aiObservatoryIndex.js's record gained geoEvidenceType (which tier confirmed a TDX event -- OFFICIAL_COORDINATE_POLYGON / EXPLICIT_PLACE_NAME / FREEWAY_KM_VERIFIED_RANGE), stamped directly onto the normalized event object in tdxQueueIngress.js's own geo-gate loop so it rides along unchanged through both the Gate-A-drop pseudo-candidate and the real aiCandidate.js#buildAiCandidate() path -- never requiring a change to the shared buildPbsAiQueueMessage() signature, so PBS is provably unaffected. aiObservatoryView.js's GEO section now shows which evidence type confirmed the event.",
    "pbsUnaffected": "A structural test (test/v2410TdxFreewayKmHsinchuDeterministicGeoFallback.test.js's own CASE 13) scans every src/pbs/*.js file and asserts none of them ever import hsinchuFreewayKmRanges.js -- never a shared/reused geographic filter between PBS and TDX.",
    "testEvidence": "New test/v2410TdxFreewayKmHsinchuDeterministicGeoFallback.test.js (18 tests: the order's own §十九 CASE 1-16, plus structural PBS/AI/LINE-file-isolation checks and pure-function zero-I/O verification), all passing. 6 assertions across test/v247TdxGeoInputMissingFix.test.js and test/v248TdxKmFallbackProductionRuntimeDiagnosis.test.js updated for the intentional behavior change (KM that previously correctly stayed UNKNOWN now correctly resolves CONFIRMED_HSINCHU), each marked with a 'SUPERSEDED BY V2.4.10' comment explaining why -- not regressions. Full regression 1814/1782/32, NEW_FAILURES=0 against a git-stash-u same-commit precise baseline.",
    "appVersion": "V2.4.9 -> V2.4.10 (MINOR)"
  }
}
