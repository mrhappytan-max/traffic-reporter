<!-- title: 版本線 -->

# 06. Version History（重要版本線，非完整 git log）

只收錄目前仍具架構意義的版本節點。commit hash 以下方列出的實際 git 證據為準（`git log --oneline`，短 SHA），非人工回憶。完整逐 commit 歷史請直接查 `git log`，不在本文件重複。

| 版本 | commit（短 SHA） | 主題 |
|---|---|---|
| V1.8.6.5 | `76a4113`→`07253f9` | KM Location Resolver（公里數 → 司機看得懂的位置 + 地圖），含官方道路位置資料整合 |
| V1.8.6.6 | `d0012e1`, `0e8349f` | Production forensic audit：非碰撞異常事件不應被誤判為事故 |
| Pipeline Trace | `3858e0a` | V1.8.6.7：24h Pipeline Trace + 人工查修頁（本專案最重要的可觀測性基礎設施） |
| V1.8.6.8 | `462e217` | Driver-Relevant Event Broadcast Time Policy |
| V1.8.6.9 | `9af8d21` | Mobile-first Deployment Guard |
| V1.8.6.9a | `5724524`, `2ae6730` | Pipeline Trace Mobile UX — Taiwan time、封閉詞彙篩選、深色模式 |
| V1.8.7.0 | `54cb6c3` | Dynamic Shoulder Broadcast + Single-CCTV Strategy（動態路肩單鏡頭策略首次引入） |
| V1.8.7.1 | `a835493` | Multi-event Single CCTV Budget / Fairness Fix（每事件獨立預算，避免先到事件餓死後到事件） |
| V1.8.7.2 | `6c109a6` | Dynamic Shoulder Message Simplification |
| V1.8.7.3 | `fa8bf22`（原始）／`62cb229`（整合進 main 後版本） | CCTV Prepare-Timeout Fix + Pipeline Trace Filter Fix（分頁 bug 修復） |
| V1.8.7.4 | `7a8ec53` | 國3 CCTV Support Audit — 唯讀稽核，**未啟用**（當時無法從 dev sandbox 確認真實 RoadID/RoadName） |
| V1.8.7.5 | `be57f88` | Enable Freeway 3 CCTV Support — 用外部唯讀查證取得的真實 Production RoadID `'000030'`/RoadName `'國道3號'` 正式啟用國3 CCTV |
| V1.8.7.6 | `40b1409` | Pipeline Trace Filter Production Investigation — 三層reproduction 皆確認 server 端無缺陷，結論為 client-side 議題 |
| V1.8.7.7 | `a3d6609`（修復）／`8e10a7a`（封版紀錄） | CCTV Gray Broken Image Fix — `extractFirstJpegFrame` JPEG marker-aware 解析修復，見 `07_KNOWN_ISSUES.md`／`02_PROJECT_HANDOFF.md` §35 完整寫法 |
| V1.8.7.8 | `f5a339a` | **TDX Quota Protection — PBS-ONLY MODE（2026-08-23，生效中）** — TDX 額度用盡，以 `TRAFFIC_SOURCE_MODE=PBS_ONLY` 單一旗標關閉 Cron 路徑上所有 TDX 呼叫（RoadEvent／OAuth token／CCTV），PBS 完全不受影響。**未刪除任何 TDX 程式碼**，還原＝改回 `ALL` 並 push。見 `07_KNOWN_ISSUES.md` 的還原程序 |
| V1.8.7.9 | `21c1a42` | Accident-Only Proactive LINE Push ＋ CCTV 影像恢復 — 主動推播收斂為重大事故限定（`LINE_PUSH_POLICY=MAJOR_ACCIDENT_ONLY`），動態路肩主動推播關閉 |
| V1.8.7.10 | `fecb818` | PBS_ONLY 下不得要求 TDX 對應 — 停用中的 TDX 不得否決 PBS 國道事故（`TDX_CORROBORATION_REQUIRED=false`） |
| V1.8.7.11 | `fc43549` | Service Area Gate — 播報邊界地理閘門（八堵事件），PBS fail-closed、TDX 僅採正面證據 |
| V1.8.7.12 | `990237c` | Location Quality Gate ＋ 播報追溯斷點修復（台68 事件）— 位置精確度閘門，且已推播事件必定可在 Pipeline Trace 查到 |
| V1.8.7.13 | `678f0bb` | PBS 國道事故 CCTV Enrichment — CCTV 資格改依「道路＋KM 可信度」，不再要求 `source==='freeway'`（國3 96K+700 事件） |
| V1.8.7.14 | `951f547` | **CCTV 官方名冊恢復＋永久保存修正（國1 93K 事件）** — 移除名冊 7 天 `expirationTtl`、拒絕以空名冊覆寫、打包官方 NFB 名冊 1943 筆為地板。四段式版本線的**最後一版** |
| **V1.9.0** | `3af5c8b` | **三段式版本治理生效後第一個正式版本** — CCTV 事故（quad）prepare-timeout 可觀測性修復（國3 96K+700 事件，2026-08-26）。真人事故：09:20 事件進 Shared Feed、無圖，且完全沒有 CCTV completion log；同一事件 09:30（下一個 Cron tick）重跑即成功四格全中。確認的 Root Cause：quad 路徑完全沒有 stage 追蹤（動態路肩單鏡頭路徑早就有），因此任何一次真實的外部延遲（frame fetch／JPEG compose／R2 publish——三者皆已證實共用同一個 4000ms budget）在 timeoutStage 上一律顯示 `null`，等於什麼都沒記錄。修復：quad 路徑比照單鏡頭路徑加上 stageTracker，在成功／失敗／timeout 任何結果都留下 metadataElapsedMs／cameraSelectionElapsedMs／frameFetchElapsedMs／collageElapsedMs／successfulFrameCount／failedFrameCount／r2PublishElapsedMs／timeoutStage。**未新增任何 retry、未新增第二輪 fetch、未調整 4000ms 數字、未加 single-camera fallback**——RETRY_REQUIRED = NO。7 項新測試（`test/cctvQuadPrepareForensics.test.js`）以決定性方式證實：FRAME_FETCH_MODE=PARALLEL、單一慢/卡住的鏡頭會拖住整批 compose、compose 與 R2 publish 都算進同一個 budget、timeout 不會中止背景中的既有工作。09:20 當下具體是哪一段外部 I/O 慢，因為當時沒有 stage 級別紀錄，**已誠實列為無法從現有證據回溯**——這正是本次修復要根除的缺口，若再發生將完整可查。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |
| **V1.9.1** | `917b8ab` | **Pipeline Trace 查修頁篩選失效修正＋預設 60 筆（2026-08-26）** — 真人回報：`/admin/pipeline-trace-view` 篩選（來源／關鍵字／道路／rawId／狀態／筆數）點了「篩選」畫面不動。確認的 Root Cause（以真實瀏覽器重現，非猜測）：`applyAdminSecurityHeaders` 的 CSP 帶 `form-action 'none'`，任何有強制 CSP 的瀏覽器（含真人回報所用的 iOS Safari）都會**完全拒絕**這個頁面（乃至任何 admin 頁面）的 `<form>` 送出——伺服器端的篩選邏輯（表單參數、`listPipelineTrace` 的 predicate、分頁）在前一輪（V1.8.7.6）就已被完整查證過是對的；那一輪自己的 headless 瀏覽器重現「不在本 repo 的自動化測試套件內」，顯然從未真正對著這個 Worker 自己的安全標頭送出過請求。本輪用真的 Chromium 對著真的回應（含真的標頭）重現：點下「篩選」按鈕，畫面完全不導頁，瀏覽器主控台明確寫著「Refused to send form data ... because it violates ... form-action 'none'」；只改這一個指令為 `form-action 'self'`（其餘 CSP 完全不動），同一個點擊就能正確導頁到篩選後的網址。**採方案 A**：修好既有篩選功能，UI 保留，未移除任何篩選欄位。同時 `DEFAULT_LIST_LIMIT` 30→60（`MAX_LIST_LIMIT`／`MAX_ENTRIES_SCANNED` 掃描安全上限不變）。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |
| **V1.9.2** | `f45cd5e` | **Cloudflare KV Write Optimization ＋ TDX Usage Summary 正式退休（2026-08-26）** — 真實 Cloudflare 帳號告警：Writes 749/1,000（`traffic-reporter-kv` = 733，佔帳號總寫入量 97.9%）。四項變更：(1) `traffic:shared-feed`／`line:incident-suppression-state` 改為 WRITE_ON_CHANGE（新共用原語 `src/util/contentEqual.js`），內容真的不變才跳過寫入——施工中自己的測試抓到一個真實 aliasing bug（`resolveIncidentNotifications` 就地修改比對用的舊物件，導致比較永遠「相等」），已用 `structuredClone` 在呼叫前先拍快照修正；(2) Pipeline Trace 改為每輪一把 `debug:pipeline-trace-batch:v2:*` 批次金鑰，取代舊制一筆事件一把 key——舊制 `debug:pipeline-trace:v1:*` 完全不刪除不遷移、靠 24h TTL 自然過期，`listPipelineTrace` 已能正確合併新舊兩種схема，兩個既有 admin 讀取 handler 未改一行程式碼；(3) **TDX Usage Summary 正式退休**（人類決策，非優化）：`tdx:usage:summary:v1` 與底層 `tdx:usage:entry:v1:*` 帳本皆確認除了餵已退休的儀表板外無其他讀者，改為 0 writes/day，`/health` 的用量卡片改為指向 TDX 官方後台的靜態提示，TDX 本身（API client／OAuth／RoadEvent／CCTV metadata／source mode／9-1 額度恢復路徑）完全未動；(4) 新增 `[kv-write-budget]` Cron console.log（僅 Workers Logs，未新增 KV key）。新增 38 項測試（`test/kvWriteOptimization.test.js`）。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |
| **V2.4.9** | 見 `git log` 訊息 `V2_4_8_TDX_KM_FALLBACK_PRODUCTION_RUNTIME_DIAGNOSIS`（單一 commit，SHA 因自我參照而略去，同前例） | **TDX KM Fallback Production Runtime Diagnosis（P0 實機異常查修，2026-09-04）** — PATCH，GEO_RESOLVER_MODIFIED=NO、PBS_MODIFIED=NO、AI_MODIFIED=NO、LINE_MODIFIED=NO、ROAD_POLICY_MODIFIED=NO。Production查修頁兩筆真實TDX事件（國1北向101K+300施工、國1南向100K+000天候）displayKM/座標顯示為—，GEO=UNKNOWN/Gate A排除。逐層runtime trace（直接執行程式碼，非猜測）：`extractKmTokenFromText()`/`parseKM()`/`normalizeRoadEvent()`皆正確、displayKM已正確落在canonical事件物件；`resolveTdxHsinchuGeography()`讀真正event物件，正確收到KM進觀測層且正確維持UNKNOWN（無座標/地名證據，安全行為非bug）。**真正bug**：`src/tdx/tdxQueueIngress.js#buildTdxPseudoCandidate()`——Gate A排除事件專用的查修頁記錄本地candidate builder，V2.4.6建立、早於V2.4.7才加的displayKM欄位未同步更新，導致Gate A排除的TDX事件寫入查修頁時displayKM永遠null。通過Gate A進AI的事件走真正的`aiCandidate.js#buildAiCandidate()`（V2.4.5起就有displayKM），完全未受影響。**修法**：`buildTdxPseudoCandidate()`補一行，把`event.displayKM`原樣帶入，與旁邊longitude/latitude同慣例。不動Gate A決策、GEO resolver、Queue、AI、LINE、道路政策、PBS。新增`test/v248TdxKmFallbackProductionRuntimeDiagnosis.test.js`（8項，CASE1-5＋KV outage迴歸鎖）。全量迴歸1796/1764/32，`git stash -u`同commit精確基準比對NEW_FAILURES=0。`APP_VERSION` V2.4.8→V2.4.9。詳見`07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md`／`00_CURRENT_STATE.md` |
| **V2.4.8** | 見 `git log` 訊息 `V2_4_8_AI_LINE_MESSAGE_EDITOR_AND_UNIFIED_PRESENTATION`（單一 commit，SHA 因自我參照而略去，同前例） | **LINE 路況文字編輯與統一排版（2026-09-04）** — MINOR，PBS_DECISION_POLICY_MODIFIED=NO、TDX_DECISION_POLICY_MODIFIED=NO、GEO_MODIFIED=NO、ROAD_POLICY_MODIFIED=NO、CCTV_LOGIC_MODIFIED=NO。核心目標：PBS警廣與TDX高公局/公路局不論原始本文格式，最終LINE訊息統一整理成短準乾淨的同一個"路況播報員"聲音。既有單次Workers AI呼叫JSON schema新增`cleanSummary`（AI文字編輯，禁止提及道路/方向/KM/車道數，禁止捏造事實）——未新增第二次AI呼叫。`validateAiDecisionResponse()`把cleanSummary獨立驗證（缺失/超過`CLEAN_SUMMARY_MAX_CHARS=100`一律null，不影響既有notify/impact/reason/confidence驗證）。新增`cleanSummaryContradictsFacts()`——車道數字/方向詞與canonical facts矛盾時null掉cleanSummary，notify決策不受影響。`messageFormat.js#formatEventMessage()`新增cleanSummary存在時的區塊式排版（headline/road+KM+車道數/cleanSummary+行動提示/通報+地圖+更新時間，空行分隔），cleanSummary缺失時走完全未改動的舊版單行排版（零回歸風險）。新增`buildReporterLine()`取代`buildSourceDetailLine()`：TDX恆定顯示"通報：【TDX】高公局/公路局"（史上第一次，此前TDX從無通報行）；PBS恆定顯示"通報：【警廣】"+deterministic別名對照（高公局/公路局/熱心聽眾/警方，不認得則顯示原文截斷，sourceDetail為空或字面是"警廣"時只顯示裸前綴）。查修頁`aiObservatoryIndex.js`新增`cleanSummary`/`finalRenderedMessage`欄位，`View.js`新增「AI文字編輯」展開區塊顯示【原始本文】→【AI整理後】→【LINE最終內容】。因通報行格式全面改變，更新5個既有測試檔7處斷言（施工令明確要求的預期行為改變，非回歸）。新增`test/v248AiLineMessageEditorAndUnifiedPresentation.test.js`（18項，含施工令§十九全部14個CASE）。全量迴歸1788/1756/32，`git stash -u`同commit精確基準比對NEW_FAILURES=0。`APP_VERSION` V2.4.7→V2.4.8。未觸碰：PBS Windows篩選、TDX Geographic Resolver、道路管理政策、notify eligibility本身、Incident Memory、Queue、dedupe、`wrangler.jsonc`全部四個TDX/AI開關、行政區polygon、CCTV搜尋邏輯。詳見`07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md`／`00_CURRENT_STATE.md` |
| **V2.4.7** | 見 `git log` 訊息 `V2_4_6_TDX_GEO_INPUT_MISSING_DIAGNOSIS_AND_FIX`（單一 commit，SHA 因自我參照而略去，同前例） | **TDX 地理資料缺失查修：description 文字 KM 後援（2026-09-03）** — PATCH，PBS_MODIFIED=NO、GEO_RESOLVER_MODIFIED=NO、ROAD_POLICY_MODIFIED=NO、AI_POLICY_MODIFIED=NO。真實事件 A15040100H-01-20260903103244766100023（TDX｜高公局，國3北向79K+000其他異常告警-散落物）查修頁 areaNm/displayKM/座標全空，即使description明確含「79K+000」。§一稽核：`normalizeRoadEvent()`結構化KM擷取本身無bug（無條件擷取），該筆事件原始payload確實缺少所有結構化地理欄位；真正缺口是TDX normalize從未有description文字KM後援（PBS早就有）。副發現（範圍外已繞過）：`firstDefined(raw,paths,undefined)`因JS default-parameter語法，缺席欄位實際上永遠是`''`而非字面`undefined`，新後援條件改用`!startKM`（falsy）判斷。**修法**：`hsinchuFilter.js`新增`extractKmTokenFromText()`（重用`parseKM()`既有TDX KM token格式），`normalizeRoadEvent()`只在結構化startKM/endKM皆缺席時對description呼叫，結構化欄位永遠優先不被覆蓋，解析出的token以相同原始字串格式存回startKM/endKM，composeLocation/parseKM/hsinchuGeoResolver.js Tier-2皆自動吃到零新型別分支。新增`displayKM`（數字，PBS既有欄位同形狀）。**安全性（CASE 7/7b鎖住）**：新KM僅被Tier-2 KM-heuristic觀測層讀取，永遠observability-only不單獨決定CONFIRMED/OUTSIDE——上述事件即使KM成功解析仍正確維持UNKNOWN（0 Queue/0 AI/0 LINE）。新增`test/v247TdxGeoInputMissingFix.test.js`（12項）。全量迴歸1770/1738/32，`git stash -u`同commit精確基準比對NEW_FAILURES=0。`APP_VERSION` V2.4.6→V2.4.7。詳見`07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md`／`00_CURRENT_STATE.md` |
| **V2.4.6** | 見 `git log` 訊息 `V2_4_6_TRACE_PAGE_TDX_AND_DECISION_REASON_SUMMARY`（單一 commit，SHA 因自我參照而略去，同 V2.4.1-V2.4.5 條目先例） | **查修頁 TDX 顯示與最終決策原因摘要（2026-09-03）** — MINOR，UI/observability-only，PBS_RUNTIME_MODIFIED=NO、TDX_DECISION_LOGIC_MODIFIED=NO。§一唯讀調查：查修頁正確目標是 `GET /admin/pbs-ai-observatory-view`（`aiObservatoryView.js`＋`aiObservatoryIndex.js`），非舊版 `pipelineTraceView.js`（`buildTraceEntry()` 僅3呼叫點皆PBS觸發）。TDX 幾乎不出現＝(B)+(A)組合：(B多數) TDX 在 `tdxQueueIngress.js` Gate A（地理/道路管理政策）被排除時此前僅`console.log`，KV零紀錄；(A少數) 通過Gate A的TDX事件雖正確寫入`source`，但`renderRow()`收合列硬編碼字串`"PBS"`從未讀`record.source`，畫面仍誤標PBS。**修法**：`tdxQueueIngress.js`在兩個Gate A排除點後新增`recordTdxGateDrop()`，重用既有`buildAiObservatoryRecord`/`recordAiObservatoryEntry`（同KV prefix），直接用已算出的`resolveTdxHsinchuGeography()`/`resolveTdxRoadManagementEligibility()`結果寫入6個新`AI_OUTCOME`值（`GEO_EXCLUDED_OUTSIDE_HSINCHU`/`GEO_EXCLUDED_UNKNOWN`/`ROAD_POLICY_EXCLUDED_SHOULDER_OPEN`/`_SHOULDER_CLOSE`/`_INSUFFICIENT_LANES`/`_UNKNOWN_LANES`)，排除決策本身完全不變、寫入best-effort。新增純函式`deriveFinalDecisionReason(record)`——唯一權威原因組成，只讀既有欄位（outcome/eventType/blockedLanes/suppressedForPhase/lineSent），區分「施工僅封1車道」與泛用`construction`類型。`buildAiObservatoryRecord()`新增`suppressedForPhase`／`blockedLanes`兩個此前被靜默丟棄的既有欄位。`renderRow()`改讀`record.source`顯示`PBS`／`TDX｜高公局`／`TDX｜公路局`三種徽章；收合卡片新增`finalReasonLine()`（✅已發送/⏭未發送/⏱AI處理失敗/⏳處理中+原因），PBS/TDX卡片皆有；展開內容新增TDX專屬SOURCE→GEO→ROAD_POLICY→QUEUE→AI→LINE六段流程條（PBS原①-④流程條完全不變，另一段程式碼路徑）。TRACE_API不變，仍唯讀0 AI呼叫0 KV寫入於開啟/篩選路徑。新增`test/v246TracePageTdxAndDecisionReasonSummary.test.js`（20項，含施工令§十二全部8個CASE）。全量迴歸1758/1726/32，與變更前基線（`git stash -u`同commit精確基準32項）完全相同失敗清單，NEW_FAILURES=0。未觸碰PBS runtime/TDX決策邏輯/AI prompt/LINE規則/`wrangler.jsonc`任何開關（`TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED`維持`"true"`）。`APP_VERSION` V2.4.5→V2.4.6。詳見`07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md`／`00_CURRENT_STATE.md` |
| **V2.4.5** | 見 `git log` 訊息 `V2_4_5_TDX_HSINCHU_GEO_RESOLVER`（單一 commit，SHA 因自我參照而略去，同 V2.4.1/V2.4.2/V2.4.3/V2.4.4 條目先例） | **TDX_HSINCHU_GEO_RESOLVER ＋ TDX_ROAD_MANAGEMENT_POLICY_GATE（2026-09-02）** — MINOR，正式架構修正（非僅 denylist patch）。核心驗收：「TDX 必須先證明位於新竹縣/市才進AI，無法證明就不進」；PBS 完全不在範圍。**背景**：V2.4.4 唯讀 audit 發現 TDX 服務區判斷全建立在未驗證 KM 表上，且座標缺席時舊 `serviceArea.js` TDX 分支 fail-open；真實洩漏台61線39.6K(桃園觀音)曾被誤判新竹。**FIX A（地理 resolver）**：新模組 `src/tdx/hsinchuGeoResolver.js#resolveTdxHsinchuGeography()`——三態 CONFIRMED_HSINCHU/OUTSIDE_HSINCHU/UNKNOWN，證據優先序：①座標對照官方行政區界線（內政部國土測繪中心 data.gov.tw dataset 7442）②KM表僅觀察用途③明確行政區文字（含「往ＸＸ方向」與事件所在地區分）。UNKNOWN 下游等同 OUTSIDE_HSINCHU，絕不預設新竹。CRS：TDX 座標視為 WGS84，與官方資料 EPSG:3824 對台灣地區僅差公分級，沿用專案既有慣例，未做數值轉換。`normalize.js` 新增座標證據保留（`positions[]`/`longitude`/`latitude`）。`serviceArea.js` TDX 分支改委派此 resolver，`aiCandidate.js` Gate2 與既有 V2.4.4 Gate3 自動沿用同一 canonical 結果（Gate B 零新增程式碼）；`crossSourceDedup.js#buildCanonicalEvent()` 補上座標傳遞。**FIX B（道路管理政策閘門，補充令）**：新模組 `src/tdx/roadManagementPolicyGate.js#resolveTdxRoadManagementEligibility()`，跑在地理閘門之後——機動路肩開放/關閉永不進AI；一般施工需`blockedLanes>=2`才有資格，資料不足一律`UNKNOWN_BLOCKED_LANES`不進AI；escape valve（重用`detectNonCollisionAnomaly`+獨立事故/完全封閉關鍵字）防止真正重大事故被誤判為一般施工擋下。`blockedLanes`新增進AI結構化輸入（PBS恆為null）。V2.4.4 denylist與AI prompt第四類錨點均保留為第二層 safety net。**部署政策（原輪）**：`TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED`維持`"false"`(FETCH/QUEUE維持`"true"`)，進入觀察期——**同日再稍晚由V2_4_5_SEAL_DEPLOY_AND_REAL_WORLD_VERIFY改為`"true"`，見本列末段**。新增 `test/tdxHsinchuGeoResolver.test.js`(19項)／`test/tdxRoadManagementPolicyGate.test.js`(21項)。全量迴歸 `NEW_FAILURES=0`（`git stash -u` 同 commit 精確基準52項）。未觸碰：Windows PBS、pbs-relay、Workers AI 模型、AI prompt 路肩政策原文、Incident Memory、CCTV、LINE messageFormat、60字上限。`APP_VERSION` V2.4.4→V2.4.5。**補正（同日稍晚，V2_4_5_OFFICIAL_HSINCHU_BOUNDARY_DATA_HOTFIX_CONTINUE，不升版）**：邊界資料來源從 taiwan-atlas npm 鏡像換成人類直接下載並上傳的官方 shapefile（COUNTY_MOI_1090820，ISO metadata 2020-08-20），與舊鏡像幾何比對確認無實質差異（<0.05%邊界像素誤差），舊鏡像存為歷史比對用途，resolver 本身未重寫。**封版部署（同日再稍晚，V2_4_5_SEAL_DEPLOY_AND_REAL_WORLD_VERIFY，不升版）**：人類明確授權，`TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED`改回`"true"`，TDX真實LINE通知重新上線（PHASE_E_TDX_NOTIFY_LIVE）；13項既有功能逐一確認存在，未新增程式碼，僅config-only commit；本sandbox無Production網路存取，實機觀察委由人類執行，異常回報時第一動作固定改回`"false"`。詳見`07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md`／`SYSTEM_STATE.json` |
| **V2.4.4** | 見 `git log` 訊息 `V2_4_4_TDX_SCOPE_POLICY_AND_MESSAGE_FIDELITY_FIX`（單一 commit，SHA 因自我參照而略去，同 V2.4.1/V2.4.2/V2.4.3 條目先例） | **TDX_SCOPE_POLICY_AND_MESSAGE_FIDELITY_FIX（2026-09-02）** — PATCH，緊急品質修復。TDX重接AI後3問題：(A)台61線39.6K(實為桃園觀音)誤發LINE，逾出新竹市/縣範圍；(B)例行施工/機動路肩開關等一般道路管理事件誤發；(C)TDX訊息仍是通用模板，`description`/`blockedLanes`未進LINE。**FIX A**：根因是`hsinchuConfig.js`台61線KM表本身把39.6K算範圍內(該檔自承未經官方驗證)，且`aiApprovedPbsBroadcast.js`從未複查服務區域。新增`serviceArea.js#resolveHsinchuOnlyProductionEligibility()`——denylist-only二次gate，事件文字正面提及新竹以外地名(含頭份/竹南/三灣=苗栗)即擋下，即使AI判notify=true；刻意不重猜KM表(曾試收窄致~30項collateral failure後revert)。**FIX B**：`aiDecisionEngine.js` SYSTEM_PROMPT新增第四類錨點——例行施工/機動路肩開關/一般封閉維護預設notify=false，除非含事故/重大障礙/坍方落石/車道突發封閉/其他安全風險；純prompt文字，無code-level白名單。**FIX C**：`TDX_INFORMATION_LOSS_FILE=messageFormat.js`／`_FUNCTION=buildSourceFactLine`(normalize.js早已保留description，是formatter自己PBS-only gate丟棄)——放寬source gate為PBS+TDX(60字上限不變)，非第二套formatter。**部署政策**：`TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED`重設回`"false"`(FETCH/QUEUE仍`"true"`)，待Production觀察後由人類＋Claude Browser另行決定重新開啟。未觸碰：Queue架構、Incident Memory、collision window、CCTV/R2、PBS Windows polling、PBS服務區域resolver本身、LINE quota、TDX排程、V2.4.3 timeout機制。新增`test/v244TdxScopePolicyAndMessageFidelity.test.js`(CASE1-14全過)。全量迴歸`NEW_FAILURES=0`；`wrangler dry-run`確認FETCH/QUEUE=true、NOTIFY=false。`APP_VERSION` V2.4.3→V2.4.4。詳見`07_KNOWN_ISSUES.md`／`SYSTEM_STATE.json` |
| **V2.4.3** | 見 `git log` 訊息 `V2_4_3_AI_TIMEOUT_AND_STALE_RETRY_RELIABILITY_FIX`（單一 commit，SHA 因自我參照而略去，同 V2.4.1/V2.4.2 條目先例） | **AI_TIMEOUT_AND_STALE_RETRY_RELIABILITY_FIX（2026-09-01）** — PATCH，reliability修正。`EVENT_ID 11509010029-5`(國3 81.3K多車追撞)3次AI attempt各~236秒"3046逾時"，LINE未發送；attempt2執行中PBS另送CLEARED，舊NEW retry未停止。**根因**：`callWorkersAi`原無app-level timeout；236秒/"3046"為PLATFORM側行為，非repo設定；payload無異常跡象，未裁剪資料。**FIX1**：`AI_CALL_TIMEOUT_MS=45000`，caller-side`Promise.race`fail-fast(callWorkersAi零side effect故安全)；retry次數/delay不變。**FIX2**：新增`debug:pbs-event-cleared:v1`KV marker(48h TTL)，NEW/UPDATED attempt前檢查clearedAt晚於generatedAt即視為stale——0再呼叫AI/0 LINE，新outcome`STALE_AFTER_CLEARED`，沿用既有idempotency COMPLETED，不觸碰notified-state。**Observability**：新增`timedOut`欄位，查修頁區分「AI逾時」與「事件已解除」。未觸碰：LINE formatter、V2.4.2政策、TDX、Memory、CCTV、R2、NOTIFY開關(維持`"true"`)；AI failure仍fail-closed。測試CASE1-12全過，迴歸1802 tests NEW_FAILURES=0。`APP_VERSION`V2.4.2→V2.4.3。詳見`07_KNOWN_ISSUES.md`／`SYSTEM_STATE.json` |
| **V2.4.2** | 見 `git log` 訊息 `V2_4_2_PBS_AI_LINE_INFORMATION_FIDELITY_AND_POLICY_FIX`（單一 commit，SHA 因自我參照而略去，同 V2.4.1 條目先例） | **PBS_AI_LINE_INFORMATION_FIDELITY_AND_POLICY_FIX（2026-09-01）** — MINOR，LINE呈現＋AI通報政策修正。AI已理解「道路阻斷、多車受困」，LINE卻只剩「請留意路況」——`INFORMATION_LOSS_FILE=messageFormat.js`／`_FUNCTION=formatEventMessage`（固定`TYPE_IMPACT_LINES`依type選字，從未讀`event.description`/`sourceDetail`）。**FIX A**：`messageFormat.js`新增3條增量SOURCE FACTS行——PBS comment摘要(`source==='pbs'`限定,60字上限)、「通報：XXX」(`sourceDetail`)、「⚠️封閉N車道」(`blockedLanes`)；AI的`reason`不傳入此函式。**FIX B**：`aiDecisionEngine.js` SYSTEM_PROMPT重寫(schema不變)：從「會不會壅塞」改為「值不值得提前知道」——明確事故notify=true不需證壅塞／預防性風險(掉落物/坍方/落石/道路中斷)notify=true即使未壅塞／單純車多notify=false。**FIX C**：`EVENT_ID 11509010029-5`LINE未發送，唯讀確認Queue retry架構正常，本session無Worker Logs無法查證確切失敗階段，未做reliability變更(同日稍晚V2.4.3修正機制面問題)。未觸碰：TDX fetch/Queue架構/Incident Memory/collision window/CCTV/R2/Windows PBS篩選/`TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED`(當時`"true"`)/service area/AI model/schema/cache。新增`test/v242InformationFidelityAndPolicy.test.js`(CASE1-12,16項全過)；1項既有fixture更新期望值。全量迴歸1790 tests NEW_FAILURES=0。`APP_VERSION`V2.4.1→V2.4.2。詳見`07_KNOWN_ISSUES.md`／`SYSTEM_STATE.json` |
| **V2.4.1** | `d66e2cb`（機制＋修正）／同日稍晚另一次 config-only commit（Phase C 正式啟用，見 `git log` 訊息 `V2_4_1_PRODUCTION_NOTIFY_CANONICAL_RECONCILIATION`） | **V2_4_1_PHASE_C_PRODUCTION_NOTIFY_IMPLEMENTATION ＋ V2_4_1_PRODUCTION_NOTIFY_CANONICAL_RECONCILIATION（2026-09-01）** — PATCH（真正的 root-cause 修正＋新開關，非架構變更）。前置盤點發現 Phase C 唯一 BLOCKER：`src/traffic/incidentSuppression.js` 原本可在 AI 已核准 `notify=true` 之後，再用自己的 legacy escalation 判斷（type 改變／closure 關鍵字／blockedLanes 數字增加，60 分鐘視窗）獨立否決，形成「AI 說發、legacy 說不發」的雙重語意權威。修正：`resolveIncidentNotifications()` 新增 `trustCallerDecision` 選項，AI-approved 路徑（`aiApprovedPbsBroadcast.js`，PBS 與 TDX 共用）不再重新判斷是否「真的惡化」，只保留 `INCIDENT_SUPPRESSION_COLLISION_WINDOW_MS`（10 分鐘）近同步重複碰撞防護；legacy 非 AI 的 `broadcastPipeline.js` 呼叫端完全未變更，仍是原本 60 分鐘＋`isMaterialEscalation()`。同一次修正中也抓到並修好一個真實的自我參照別名 bug（在比較前就先覆寫了 `escalation` 欄位，導致惡化判斷永遠與自己比較）。新增 canonical 開關 `TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED`（`wrangler.jsonc`），取代原本 `debugPush.js` 內硬寫死的 `suppressLineNotify = source === 'freeway' || 'highway'`。新增 `test/tdxPhaseCProductionNotify.test.js`（order 自訂 20 個 CASE，含子案例共 24 項全過）。全量迴歸 1774/1740/34，與 V2.4.0 基準以 failure 名稱集合對照確認 NEW FAILURES=0，僅跑一次。`APP_VERSION` 從 `V2.4.0` bump 為 `V2.4.1`。**同一天稍晚**，在明確、直接的人類授權下，`TDX_ROADEVENT_PRODUCTION_NOTIFY_ENABLED` 正式由 `"false"` 改為 `"true"`（純 config commit，未改程式碼）——TDX 來源的 AI notify:true 事件現在會真正推播 LINE 給真實訂閱者。稍早曾有一份指示聲稱 Production 已透過 Cloudflare Dashboard 提前熱切換為 `true`，本 session 查證 git 全部分支/commit 歷史後確認從未存在對應狀態，無法獨立驗證此聲稱（Dashboard-only 變數在本專案部署模型下本來就不持久，見 V2.0.2 Config Drift Hotfix），標記為 `HUMAN_REPORTED_NOT_INDEPENDENTLY_VERIFIED`，不視為此次啟用的真正起點——本輪 config commit 才是可查證的正式啟用時刻。第一筆真實 TDX 觸發的 LINE 通知端對端成功與否，`REAL_WORLD_CONFIRMATION_PENDING`。詳見 `00_CURRENT_STATE.md`／`02_PROJECT_HANDOFF.md`／`07_KNOWN_ISSUES.md`／`SYSTEM_STATE.json` 的完整記錄 |
| **V2.4.0** | `2d23329` | **TDX_FREEWAY_PROVINCIAL_TO_UNIFIED_AI_PIPELINE（2026-09-01，Phase B）** — MINOR，架構階段：TDX 國道/省道 RoadEvent 重新加入 PBS 既有的同一條 Cloudflare Queue（`PBS_AI_QUEUE`，唯一一條）與同一個 AI 決策引擎，跨來源協調交給新的 Recent Incident Memory（`src/traffic/incidentMemory.js`，Cloudflare KV `traffic:incident-memory:v1`，8h TTL，`MEMORY_KV_GETS_PER_EVENT<=1`／`MEMORY_KV_PUTS_PER_EVENT<=1`、`WRITE_ON_CHANGE=YES`，road+direction → 1000m/1.5km 內 → 最近 8h → 最多 5 筆候選的三層 prefilter，並排除事件自己剛寫入的紀錄避免自我發現）。新模組 `src/tdx/tdxQueueIngress.js`：TDX 新／更新事件送進同一個 `PBS_AI_QUEUE`，沿用 `dedupe.js`／`debugPush.js` 既有 fingerprint／idempotency／訊息建構，絕非第二套 Queue 或第二套訊息格式。`wrangler.jsonc` 新增三個最小粒度開關（canonical，非 Dashboard-only），預設全部 `"false"`：`TDX_ROADEVENT_FETCH_ENABLED`、`TDX_ROADEVENT_QUEUE_INGRESS_ENABLED`、`TDX_CCTV_METADATA_REFRESH_ENABLED`——疊加在 `TRAFFIC_SOURCE_MODE` 之上，絕非取代（見 `sourceMode.js#isTdxTokenAccessPermitted`）。**`LEGACY_TDX_LINE_PIPELINE=RETIRED_FOR_ROADEVENT`**：`scheduled.js` 的 `broadcastEvents` 不再包含 `summary.allEvents`（TDX 自己抓到的事件），即使單獨打開 `TDX_ROADEVENT_FETCH_ENABLED` 也無法讓 TDX 事件回到舊 V1.5 硬規則 LINE 路徑。AI schema 新增 `sameIncident`／`materialChange`（僅在有 memory context 時要求），AI decision cache key 併入 `memoryContextFingerprint`，未提供時與 V2.4.0 前完全相同的 hash（向下相容）。Phase B 閘門硬寫死在單一呼叫點（`debugPush.js`：`suppressLineNotify = source === 'freeway' || source === 'highway'`），非任何 `wrangler.jsonc` 變數控制——要進 Phase C（TDX 真正推播 LINE）需要未來一次明確的程式碼變更，絕非改設定值就能達成。`incidentSuppression.js` **不大拆**：保留作為短期重複推播安全網，疊加在 AI 自己的 8h Memory 再判斷之下，非取代。本輪明確未觸碰：CCTV 整條 metadata-cache → 選鏡 → compose → R2-put → R2-read-back-verify → LINE 管線（V2.3.3 原封不動）、VD／CMS／其他 Traffic API（未復原）、CCTV metadata refresh Cron（維持 MANUAL/ON-DEMAND）、Google Maps。`APP_VERSION` 從 `V2.3.3` bump 為 `V2.4.0`。新增 `test/tdxUnifiedAiPipeline.test.js`（order 自訂 17 個 CASE 全過），另 8 個既有測試檔改寫以反映 TDX-legacy-retirement 的刻意行為變更。全量迴歸 1746/1712/34，與 V2.3.3 基準以 failure 名稱集合對照確認 NEW FAILURES=0，僅跑一次。`npm run check:deployment-policy` PASS；`wrangler deploy --dry-run` 確認 bindings 乾淨、唯一一個 Queue、三個新開關均為 `"false"`。**本輪未啟用任何真實 TDX 抓取**——三個新開關全部預設 `"false"`，本輪只建好機制，實際打開（Phase A：FETCH_ONLY）需要另一次明確的人類指示。詳見 `00_CURRENT_STATE.md`／`03_ARCHITECTURE.md`／`07_KNOWN_ISSUES.md` 的完整記錄 |
| **V2.3.3** | `b3035b2` | **CCTV_R2_READBACK_VERIFY_BEFORE_LINE（2026-08-31）** — PATCH，可靠性加強，不改架構。上一輪唯讀查核（`CCTV_IMAGE_READY_BEFORE_LINE_PUSH_AUDIT`）逐函式追蹤真實 AI-approved 廣播路徑，確認 await 鏈本身已安全（R2 put 完整 await → public URL 只在 put 成功後建構 → LINE push 一律最晚），但無法用應用層時序解釋一筆真實回報的破圖事故。本輪停止對 LINE 端行為的無止盡追查，改為新增本 codebase 真正能自己保證的一件事：新函式 `publishedImage.js#verifyPublishedImageReadable(bucket, id)`，在 `publishCollageImage()` 成功之後、imageUrl 回傳之前，做一次純內部 R2 GET（絕非對本 Worker 自己 public endpoint 發 HTTP），確認物件存在、Content-Type 確實為 `image/jpeg`、bytes 非空。接上 `dynamicCollage.js` 的兩個既有 R2 發布點——`prepareCctvImageWork`（quad／事故）與 `prepareSingleCctvImageWork`（single／動態路肩）——兩者共用同一個 publish 函式與同一套下游 LINE image 組裝，同步保護，不留半修缺口。新失敗代號 `r2-readback-failed`，與既有所有 CCTV 失敗原因採完全相同的 fail-closed 處理：文字照常送、圖片跳過、不重試、不重新 publish。`TDX_CALL_CHANGE=0`（新讀回只是一次 `bucket.get()`）。明確未觸碰：15 分鐘 TTL、previewImageUrl 架構、CCTV 選鏡策略、四象限版面、圖片尺寸/JPEG quality、LINE Push 模型、AI Prompt/Model、Queue、Windows PBS、TDX、Google Maps；既有 await 順序本身未重排，只多插入一個新的 await 步驟。新增/擴充 8 項測試（`test/dynamicCollage.test.js` CASE 1-5/7-8 六項、`test/dynamicShoulder.test.js` 19b 一項），另有 9 個既有測試檔的 `r2Bucket()` mock 補上 `httpMetadata` 傳遞（與真實 R2 行為一致，既有成功案例行為不變）。全量迴歸 1729/1695/34，與既有 34 項基準以 failure 名稱集合對照確認 NEW FAILURES=0，僅跑一次。`APP_VERSION` 從 `V2.3.2` bump 為 `V2.3.3`。詳見 `07_KNOWN_ISSUES.md`／`dynamicCollage.js`／`publishedImage.js` 的完整記錄 |
| **V2.3.2** | `4e9cce2` | **CCTV_PRODUCTION_IMAGE_DIAGNOSTIC_REPAIR — 診斷工具修復（2026-08-30）** — PATCH，診斷工具修復，非 CCTV 產品功能。真實事件 `EVENT_ID=11508310005-5`（LINE 送達的 CCTV 圖片破圖）：唯一能直接驗證「剛 publish 完的 `/cctv/image/:id` 是否立即 200+JPEG」的工具 `GET /admin/cctv-hsinchu-publish-test` 本身無法使用——依賴只有 `/admin/cctv-hsinchu-probe` 才能重建的 `CANDIDATES_KEY`，而該 probe 會發起真實 TDX 呼叫，在 `TRAFFIC_SOURCE_MODE=PBS_ONLY` 下不可為診斷消耗。修正：改從同一份 `cctv:freeway-metadata:v1` 攝影機清單快取（真實事故動態播報路徑早已 cache-only 讀取）取得候選——新函式 `composeCollageFromFreewayMetadata()` 串接 `readFreewayCctvMetadataCache()`（cache-only，空 KV 退回官方 NFB 內建清單 1943 筆，實測固定測試點涵蓋 4 象限中 3 個）→ `selectFourQuadrantCandidates()`（既有 fixed-target admin probe 同一函式同一預設值，未新增鏡頭排序政策）→ `composeCollageFromCandidates()`（本專案所有 collage 路徑共用核心，非分歧邏輯）。`TDX_CALLS_PER_TEST=0` 測試直接驗證（無 fetch 觸及 tdx.transportdata.tw）。新增 `step` 欄位區分失敗成因：`METADATA_CACHE_MISSING`／`NO_CCTV_CANDIDATES`／`SNAPSHOT_FETCH_FAILED`／`COMPOSE_FAILED`（兩者以 `composeCollageFromCandidates()` 新增純累加欄位 `anyFrameFetchSucceeded` 區分，沿用 V1.9.0 同一函式已建立的「on every outcome」慣例，對既有呼叫端零行為變化）／`R2_PUBLISH_FAILED`；成功回應補齊 `status`/`published`/`contentType`/`bytes`/`createdAt`/`expiresAt`/`imageUrl`（`createdAt` 自 V1.8.4 起就有算但從未回傳）。新增/改寫 22 項測試（`test/cctvImagePublish.test.js`，含空 KV 靠內建清單成功、SNAPSHOT_FETCH_FAILED 與 COMPOSE_FAILED 各以實測驗證過的 fixture 明確區分、驗證絕不觸發 PBS/AI/Queue/LINE）。全量迴歸 1722/1688/34，與 V2.3.1 基準以 failure 名稱集合對照確認 NEW FAILURES=0，僅跑一次。`APP_VERSION` 從 `V2.3.1` bump 為 `V2.3.2`。本輪**未觸碰**：PBS、Windows filter、Cloudflare Queue、AI 決策路徑、正式 LINE 廣播、CCTV 鏡頭排序政策、真實事故 CCTV 選鏡/計時/預算邏輯本身、Shared Feed、TDX 本身運作、Google Maps、Observatory 主流程；`/admin/cctv-hsinchu-collage`（CANDIDATES_KEY）未受影響。詳見 `07_KNOWN_ISSUES.md`／`hsinchuCctvProbe.js` 的完整記錄 |
| **V2.3.1** | `070a0f4` | **DIRECT_COORDINATE_MAP_FALLBACK — LINE 地圖座標直連 Hotfix（2026-08-30）** — PATCH，formatter 行為修正，不改架構。真實事件 `EVENT_ID=11508260158-0`（竹60線縣道，新竹縣尖石鄉坍方封路）：PBS/Windows/Cloudflare 全程保留有效座標、AI 正常完成、LINE 已發送，但無地圖連結。根因：`messageFormat.js#buildRoadLines()` 的兩層地圖連結解析（`resolveKmLocation`／`resolveCoordinateLocation`）都要求 `road` 先辨識成國道/省道格式才會用座標，縣道/鄉道（如竹60）從未被本專案僅有的國道(95016)/省道(7040)公里標資料集涵蓋，座標路徑因此在使用座標前就被 road 判斷擋下。修正：新增最後手段 `kmLocationResolver.js#buildDirectCoordinateMapUrl()`，重用既有 `buildMapUrl()` 直接以事件自身座標產生地圖連結，不辨識道路、不查資料集、不猜測任何 label/road name，只在既有兩層都失敗時才觸發；新增 `isValidRawCoordinate` 拒絕 null/NaN/Infinity/非數字/超出範圍/(0,0)。`roadName.js`／`canonicalFreewayRoad`／`canonicalProvincialRoad`／官方資料集本身**未觸碰**。新增 13 項測試（`test/pbsCoordinateDirectMapFallback.test.js`，含真實事件端對端 fixture，road 全程維持真實值未硬編碼），既有 KM/座標解析測試檔重跑不變、全數通過。全量迴歸 1718/1684/34，與 V2.3.0 基準以 failure 名稱集合對照確認 NEW FAILURES=0，僅跑一次。`APP_VERSION` 從 `V2.3.0` bump 為 `V2.3.1`。本輪**未觸碰**：AI Prompt/model、Windows PBS filter、Queue、LINE 廣播政策、Observatory 架構、TDX、CCTV，亦未開始縣道/鄉道公里標資料工程。詳見 `07_KNOWN_ISSUES.md`／`kmLocationResolver.js` 的完整記錄 |
| 補登（文件） | 見 `git log`（本列所在 commit 自身，無法自我參照，同 `SYSTEM_STATE.json` 的 `exportArtifactCommit` 慣例） | **Engineering Memory 補登：Windows PBS Geographic Filter Repair（2026-08-30）** — DOCUMENTATION ONLY，`APP_VERSION` 不變（無程式碼變更）。人類回報 Windows PBS 本機篩選（`pbs-relay/`）已移除 `isAccident()` 事故語意閘門，改用 point-in-polygon（data.gov.tw dataset 7442）取代原矩形邊界，回報 `11→29`（找回 18 筆）、`124 tests passed/0 failed`。**本 Session 獨立查證**：`pbs-relay/src/localPrototype.js` 目前仍保留並使用 `isAccident()`，`pbs-relay/` 全部 git 歷史未見對應 commit，故上述數字**未經本 Session 驗證**，記錄為 `HUMAN_REPORTED_NOT_INDEPENDENTLY_VERIFIED`。同輪一併記錄人類回報「V2.3.0 已由 Production 真實事件驗收完成」，因無可核對證據，`BROWSER_ACTION_REQUIRED` 維持 V2.3.0 原記錄不變。詳見 `00_CURRENT_STATE.md`／`03_ARCHITECTURE.md`／`07_KNOWN_ISSUES.md` 的完整記錄 |
| **V2.3.0** | `850f5fa` | **PBS AI Queue Reliability — Cloudflare Queues 取代 ctx.waitUntil（2026-08-30）** — MINOR，正式改變 AI business processing 的執行架構與可靠性模型，非單純 timeout patch。真實 Production 事故（與 V2.1.0 修的是不同一種失敗模式）：`EVENT_ID=11508290166-0` 成功啟動 Workers AI 呼叫（16:49:03.112），但呼叫本身在 `ctx.waitUntil()` 自己的背景執行時間預算到期前未回傳，16:49:32.912 平台強制取消整個 task，AI 決策永久遺失，冪等記錄卡死 `PROCESSING`。`REAL_INCIDENT_ROOT_CAUSE=WAITUNTIL_BACKGROUND_WINDOW_EXCEEDED`。修正：`ctx.waitUntil()` 全面退休做為 AI 背景執行載體（`WAITUNTIL_AI_PROCESSING=RETIRED`），改用**唯一一個** Cloudflare Queue（`pbs-ai-processing-queue`／binding `PBS_AI_QUEUE`，`wrangler.jsonc` 為唯一正典設定來源，`AI_BACKGROUND_EXECUTION=CLOUDFLARE_QUEUE`）：HTTP ingress 只驗證／寫冪等 PROCESSING／寫 Observatory `PROCESSING_STARTED`／`Queue.send()`，只有 send 成功才 ACK（send 失敗回真實 503，絕不假報已接收）；獨立 Queue Consumer（`src/index.js` 新增 `queue()` export → `handlePbsAiQueueBatch` → `processQueuedPbsEvent`）承接全部 AI／LINE／Observatory-final 工作，重用（非重造）既有 AI candidate／decision engine／cache／LINE 廣播／Observatory writer。`AI_CALL_FAILED`（呼叫本身未可靠完成）現在可 Queue 重試，`MAX_QUEUE_RETRIES=3`；既有 `AI_DECISION_INVALID` fail-closed 政策維持不變，絕不重試。新增唯一最小終態 `AI_OUTCOME.PROCESSING_FAILED`（重試耗盡後由 Consumer 自己寫入，標記冪等 `COMPLETED`，永不卡死 PROCESSING）。Queue 遞送 `AT_LEAST_ONCE`（`QUEUE_DELIVERY_MODEL`），業務結果 `EFFECTIVELY_ONCE`（`BUSINESS_OUTCOME_MODEL`）：已 `COMPLETED` 的重複遞送直接 ack 略過。開發期間發現並修正一個 Observatory KV key 重複 bug：Queue Consumer 自己的 `now` 與 HTTP ingress 原始接受時間不同，改從 queue message 的 `acceptedFirstAcceptedAt` 重建 `observatoryNow` 供兩次 Observatory 寫入共用同一把 key，真實當下 `now` 仍用於所有業務決策與 `completedAt`。`RAW_PBS_TEXT_POLICY=IMMUTABLE_END_TO_END_UNTIL_AI` 不變。KV 成本實測：`puts=4N+2`（與 V2.2.0 相同，0 額外寫入）、`gets=6N`（+1／事件，Consumer 冪等 re-check）。Queue 成本工程估算：成功 2 operations／事件，重試耗盡最差 5 operations／事件，50/100/200 events/day 最差情況 250/500/1000，遠低於官方文件 10,000/日免費額度。新增 `test/pbsAiQueueReliability.test.js`（含真實事故 `EVENT_ID=11508290166-0` 迴歸 fixture，可控制 Promise 模擬 30+ 秒延遲，非真實 sleep），改寫 `test/pbsDebugPushBackgroundProcessing.test.js` 5 項過時 `ctx.waitUntil` 測試。`APP_VERSION` 從 `V2.2.0` bump 為 `V2.3.0`。全量迴歸 1705/1671/34，與 V2.2.0 基準以 failure 名稱集合對照確認 NEW FAILURES=0，僅跑一次。本輪**未觸碰**：Windows PBS filter、Windows 自身 HTTP timeout、PBS 原始文字、AI Prompt/model/semantic policy、service area、LINE policy/formatter、Shared Feed、CCTV、TDX、driverSummary、hourly reminder、Observatory 頁面整體 UI。`BROWSER_ACTION_REQUIRED=YES`：真實 Cloudflare Queue 資源需在 Dashboard／`wrangler queues create` 建立，本 sandbox 無法驗證或建立。詳見 `07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md`／`PRODUCT_DECISIONS.md` 的完整記錄 |
| **V2.2.0** | `33ba1e6` | **AI Decision Observatory — 四層事件生命週期（2026-08-29）** — MINOR，backward-compatible observability/UI 擴充，不改 AI semantic authority、Windows PBS filter、LINE policy、V2.1.0 的 ctx.waitUntil 架構。把 `/admin/pbs-ai-observatory-view` 從單一 AI-outcome 列表升級為明確四層檢視：①PBS/Windows ②Cloudflare ③AI ④LINE，各層獨立顯示成功／未執行／失敗／未知四種狀態＋detail 區塊，頂部另有流程狀態條一眼看出卡在哪層。`RAW_PBS_TEXT_VISIBLE=YES`：`aiObservatoryIndex.js` 的 `commentSummary`（原截斷 120 字）退休為 `rawComment`／`rawSourceDetail`，完整未截斷儲存，與既有解析欄位（road/direction/areaNm/displayKM）獨立標示絕不合併。`FAILURE_EVENT_VISIBILITY` 修正本輪真正缺口：`ctx.waitUntil` 背景處理中途 crash 或未跑完的事件，原本 Observatory 完全不留紀錄；`debugPush.js` 的 `processAcceptedEvent` 現在於處理一開始即寫入 `AI_OUTCOME.PROCESSING_STARTED` 記錄（直接取自 Windows 原始 payload，寫在任何可能 throw 之前），既有的最終寫入之後原地覆寫同一把 KV key（`idempotencyKeyHash`＋`taipeiDate`＋接受當下 `now` 三者相同）——停滯事件因此仍有一張卡可查。`EXTRA_KV_WRITES_PER_EVENT=1`（實測，`test/pbsAiObservatoryFourLayer.test.js` 的 KV cost formula 測試）：`puts=4N+2`，50/100/200 events/day 分別 202/402/802 puts/day，遠低於 Workers KV Free Plan 每日 1,000 次額度。REUSE_EXISTING_DATA_FIRST 全程遵守：Cloudflare 層狀態即時讀取既有 V2.1.0 transport idempotency 記錄（`computeIdempotencyKeyHash`／`buildIdempotencyKvKey` 自 `debugPush.js` 匯出重用，非第二套 hash）；AI 層 notify/impact/reason/confidence 仍即時讀取既有 `aiDecisionCache`，未重複儲存，零新增 KV prefix。開啟／重新整理／搜尋／篩選本頁仍 0 次 Workers AI 呼叫、0 次 KV 寫入（僅新增每列 transport-idempotency-status 讀取）。`APP_VERSION` 從 `V2.1.0` bump 為 `V2.2.0`。新增 16 項測試（`test/pbsAiObservatoryFourLayer.test.js`，涵蓋 order 十二全部 20 項最低要求），既有相關測試同步更新，全部首次執行即 PASS；全量迴歸 1697/1663/34，與 V2.1.0 基準以 failure 名稱集合對照確認 NEW FAILURES=0，僅跑一次。本輪**未觸碰**：Windows PBS filter/relay transport、V2.1.0 ctx.waitUntil 架構、AI Prompt/model/semantic policy、service area、LINE policy/formatter、Shared Feed、CCTV、TDX、driverSummary、hourly reminder，以及「同一事故一小時內」AI 語意上下文功能（刻意未實作）。詳見 `07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md`／`PRODUCT_DECISIONS.md` 的完整記錄 |
| **V2.1.0** | `87aa696` | **Transport Ack Decoupled From Business Processing（2026-08-29，深度壓縮）** — MINOR，正式資料流／責任邊界調整。真實事故：2筆NEW事件走到AI_CALL_STARTED，但Windows 5秒HTTP timeout觸發時該工作從未交給`ctx.waitUntil()`，client斷線後Cloudflare直接取消仍在執行的handler，AI/LINE/Observatory全部沒完成，Windows自己的retry又被早已寫入的冪等記錄擋下，永久卡死。修正：(1) `src/index.js`轉傳`ctx`，`debugPush.js`把business processing交給`ctx.waitUntil()`，HTTP回應只代表"已持久接收"非"AI判讀完成"；`ctx`為新增第4個optional參數，缺少時fallback為直接await，行為逐位元組相同。(2) KV冪等記錄新增`status:'PROCESSING'|'COMPLETED'`兩階段標記，`PROCESSING_STALE_MS`(60秒)內視為重複，超過才真正重試（極少數isolate被回收情境，非client timeout）。刻意不用Queue/Durable Object（沿用"不要過度設計"先例）。再次驗證（非重新實作）：PBS原始comment/sourceDetail逐字元完整送進AI prompt未改一行；pbs-relay/ NEW/UPDATED/CLEARED分類純fingerprint驅動，無隱藏的"同一事故一小時內"抑制規則。新架構原則：`WINDOWS_ROLE=HSINCHU_PBS_FILTER_AND_RELAY`、`AI_ROLE=SEMANTIC_DECISION_AUTHORITY`、`RAW_PBS_TEXT_POLICY=IMMUTABLE_END_TO_END_UNTIL_AI`。`APP_VERSION` V2.0.2→V2.1.0。新增9項測試，全量迴歸1681/1647/34，NEW FAILURES=0。未觸碰：AI Prompt/model、Windows filter、service area、lifecycle、formatter、CCTV、TDX、查修頁UI。詳見`07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md` |
| **V2.0.2** | `2c784b5` | **Config Drift Hotfix — PBS_AI_DECISION_ENABLED canonical deployment（2026-08-29，深度壓縮）** — PATCH。根因：`PBS_AI_DECISION_ENABLED` 曾只存在於 Dashboard 手動設定，每次 `wrangler deploy` 悄悄覆寫回程式碼預設值 `false`（與 `TRAFFIC_SOURCE_MODE` 既有機制相同）。修正：`wrangler.jsonc` vars 正式宣告 `"true"`，新增 `check-deployment-policy.mjs` regression guard 防止未來再漂移。`DASHBOARD_ONLY_AI_SWITCH=RETIRED`。10 項新測試全過，迴歸 NEW_FAILURES=0。`APP_VERSION`V2.0.1→V2.0.2。詳見 `07_KNOWN_ISSUES.md` |
| **V2.0.1** | `7b7bd05` | **AI Decision Observatory（2026-08-29，深度壓縮）** — PATCH，唯讀 observability。新 Admin 頁 `GET /admin/pbs-ai-observatory-view`（PBS原文→AI判斷→理由→結果），開啟/搜尋 0 次 Workers AI 呼叫。新 thin index `aiObservatoryIndex.js`（48h TTL KV），每個真正被接受事件寫入1筆最小記錄，`reason`直接讀既有`aiDecisionCache`（絕不重新生成）。22項新測試全過，迴歸NEW_FAILURES=0。`APP_VERSION`V2.0.0→V2.0.1。詳見 `07_KNOWN_ISSUES.md` |
| **V2.0.0** | `f1a05d0` | **MILESTONE（2026-08-28，深度壓縮）** — 架構世代里程碑封版，非新功能：V1.9.5～V1.9.9 Phase 3D 建立的 Windows PBS 本機邊緣過濾 + Cloudflare Workers AI 判讀，正式標記為新 canonical 架構（舊：Cloudflare 30 分鐘輪詢＋硬規則，已退休可 rollback；新：PBS→Windows 邊緣過濾→lifecycle→Cloudflare ingress→持久冪等→AI candidate/cache→Workers AI 判斷駕駛通行影響→既有 LINE 基礎設施），`APP_VERSION` V1.9.9→V2.0.0，`test/versionLineage.test.js` 前綴同步改 `'V2.'`。`03_ARCHITECTURE.md`「V2.0.0 接手地圖」26 題問答＋端對端流程圖；`02_PROJECT_HANDOFF.md` 新增 Rollback/Troubleshooting Runbook。全量迴歸 1517/1484/33，NEW FAILURES=0。Production 狀態（AI_BINDING/AI_DECISION/LINE_AI_DECISION=ACTIVE）為 GPT Work 回報未獨立驗證；`FIRST_REAL_AI_EVENT=WAITING`。完整記錄見 `07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md` |
| **V1.9.9** | `61795b1` | **Cloudflare 字串布林解析 Hotfix（Phase 3D，2026-08-28）** — GPT Work 在 Dashboard 設定 `PBS_AI_DECISION_ENABLED="true"` 後，正式環境 AI 決策仍未啟用。根因：Cloudflare Dashboard/CLI Variables 一律以字串注入 Worker，從不是真正的 boolean；`src/pbs/aiConfig.js#resolvePbsAiDecisionEnabled()` 原本嚴格檢查 `typeof === 'boolean'`，字串永遠不符合，每次請求都悄悄落回安全預設值 `false`——不是 Dashboard 操作錯誤，是 resolver 本身的 bug。GPT Work 已先行 rollback（`PBS_AI_DECISION_ENABLED = FALSE`）。修正：`resolvePbsAiDecisionEnabled()` 現在同時接受真正 boolean `true`/`false`，以及 Cloudflare runtime 字串形式 `'true'`/`'false'`（不分大小寫、trim 前後空白，如 `' TRUE '`、`'True'`）；`undefined`/`null`/空字串/其他常見「真值」拼法（`'1'`/`'yes'`/`'on'`）/任何非字串非 boolean 型別，一律 fail-safe 回 `PBS_AI_DECISION_ENABLED_DEFAULT=false`，刻意不做寬鬆 truthy 判斷。`wrangler.jsonc` 檢查後確認未宣告任何 `PBS_AI_DECISION_ENABLED` 值，Production 預設安全性不受影響。新增 8 項測試：`test/aiConfig.test.js` 擴充為完整 true/false/字串/大小寫/空白/未知值矩陣（新增 6 項，1 項既有測試斷言依新預期行為反轉）；`test/pbsAiDecisionScenarios.test.js` 新增 2 項 integration-level 測試，透過真實 `handlePbsDebugPush()` 端對端證明字串 `"true"` 確實會讓 mocked AI adapter 被呼叫、字串 `"false"` 確實維持 0 次 AI 呼叫。完整迴歸 1517/1484/33，與變更前基線（1509/1476/33）以 failure 名稱集合對照確認 NEW FAILURES=0（唯一差異：先前 session 量到的一項環境敏感 git/build-metadata flaky 測試本輪未再出現，屬 sandbox 雜訊非本輪回歸）。本輪**未觸碰**：AI prompt、model ID、AI candidate schema、AI cache、cache TTL、`runAiApprovedPbsBroadcast`、LINE policy、`MAJOR_ACCIDENT_ONLY` legacy path、service area、lifecycle、idempotency、CCTV、Shared Feed、hourly reminder、TDX、Windows monitor——單點 config parsing hotfix。APP_VERSION 維持 `V1.9.9`（同一版本線分階段施工，未升 V1.9.10）。`AI_BINDING = ACTIVE`（GPT Work 已確認）、`AI_DECISION = DISABLED_PENDING_GPT_WORK_RETRY`——修正已部署，但 Dashboard 端 `PBS_AI_DECISION_ENABLED` 目前仍是 GPT Work rollback 後的 `FALSE`，尚未重新以修正後邏輯重試，是否/何時重試由 GPT Work 決定。詳見 `07_KNOWN_ISSUES.md` 的完整記錄 |
| **V1.9.9** | `5d1f9fe` | **Workers AI Driver Impact Decision Integration（Phase 3B，2026-08-28，深度壓縮）** — Phase 2 的 AI candidate/cache key 正式接上真實 Workers AI 呼叫（`@cf/zai-org/glm-4.7-flash`，`env.AI.run(...)`，binding `AI`）。固定繁中 prompt 只判斷駕駛通行影響，不依事件類型名稱直接決定；嚴格 JSON schema 驗證，任何缺失/型別錯誤即 `AI_DECISION_INVALID`，絕不到 LINE。新模組：`aiConfig.js`（kill switch，預設 false）、`aiDecisionCache.js`（48h TTL，fail-open）、`aiDecisionEngine.js`（cache→AI→驗證 orchestrator）、`aiApprovedPbsBroadcast.js`（重用既有 subscriptions/notified/incidentSuppression/messageFormat/CCTV/pushMessage，明確不呼叫 `getBroadcastEligibility`／`getLinePushPolicyDecision`／`resolveLocationQuality`——AI verdict 是新語意權威）。`debugPush.js`：AI 開啟時與 legacy `runLineBroadcast()` 路徑互斥，避免雙判官重複推播。AI 失敗一律 0 LINE、`AI_CALL_FAILED`，不 fallback 舊硬規則，未加 retry（下次 Windows 推送自然重試）。新增 5 測試檔共 57 項（含施工令 A-P 十六情境），全數一次 PASS。全量迴歸 1509/1476/33，NEW FAILURES=0。`APP_VERSION` 維持 `V1.9.9`。Production 誠實記錄為 `AI_BINDING=PENDING_BROWSER_SETUP`／`AI_DECISION=DISABLED`——程式碼已部署但行為未變，待 GPT Work 完成 Binding 設定＋另有明確指令才開啟。詳見 `07_KNOWN_ISSUES.md` |
| **V1.9.9** | `7acb82a` | **Windows Service Area Hsinchu Only（Phase 1，2026-08-28，完成於另一個 session，本 Cloud Session 未參與）** — Windows PBS Local Edge Filter 僅納入新竹市與新竹縣；明確排除竹南、頭份、苗栗市與其他苗栗縣區域。國1／國3使用 Windows 專用新竹公里範圍，舊 broad bounding box 不再能單獨納入苗栗事件；跨縣市道路只保留新竹段。重用既有 canonical road/service-area resolver，未建立第二套業務 service-area logic。`NEW`／`UPDATED`／`MISSING_PENDING_CLEAR`／`CLEARED` lifecycle 未修改；AI integration 未開始；LINE policy 未修改。Targeted 12/12、root invariants 73/73、PBS full 121/121 PASS，NEW FAILURES=0。純 `pbs-relay/`（Windows 端）變更，未觸碰任何 `src/` Cloudflare runtime |
| **V1.9.9** | `0f9a080` | **AI-ready Business Pipeline Simplification（Phase 2，2026-08-28）** — 為 Phase 3 Workers AI 全量判讀做準備，本階段刻意**不接AI**、**不啟用新LINE判讀政策**，只整理decision path。新模組 `src/pbs/aiCandidate.js`：從 `src/pbs/debugPush.js` 既有（完全未修改）的正規化事件建立最小 AI candidate 物件（source/eventId/lifecycle/road/direction/areaNm/comment/longitude/latitude/generatedAt，附加displayKM/eventType/sourceDetail/locationQuality，刻意不含notify/impact——那是Phase 3的工作），與既有 `runLineBroadcast()` 呼叫**並行、完全獨立**，純觀察用（log only），從未gate、delay或改變真實LINE決策。只保留兩個gate：service area（重用`traffic/serviceArea.js#resolveServiceAreaEligibility`，與`runLineBroadcast`自己用的同一個resolver）與既有V1.9.7持久冪等/重複防護（duplicate事件從不到達candidate-building呼叫點）。**刻意不套用**在candidate建構上的既有內容硬規則：`MAJOR_ACCIDENT_ONLY` policy、V1.5 type/keyword whitelist（`getBroadcastEligibility`）、location quality hard-reject——這些函式本身**完全未修改**，對真實LINE決策仍是完整生效的legacy policy，直到Phase 3 AI真正接手。location quality（`resolveLocationQuality()`）唯讀重用，只作為candidate的metadata附加欄位，從未作為是否建立candidate的gate，僅scoped在這個新模組內，不影響其他來源/既有路徑。安全過渡狀態：`PBS_AI_DECISION_MODE = PREPARED_NOT_ACTIVE`（exported const）——prepared（本模組真的執行並log一個真實candidate）但not active（candidate從未用於任何決策、從未觸及LINE/CCTV/Shared Feed、從未呼叫任何AI模型）。新增AI decision cache key設計（`computeAiDecisionCacheKeyHash({eventId,fingerprint})`，SHA-256(eventId:fingerprint)，重用Windows既有穩定fingerprint、非新NLP/semantic fingerprint；`AI_DECISION_CACHE_KV_PREFIX='debug:pbs-ai-decision-cache:v1'`獨立debug-only前綴，預留給Phase 3採用）——本輪僅schema/helper，**無任何實際KV讀寫**。新增測試：`test/pbsAiCandidate.test.js`（13項純函式單元測試）+ `test/pbsDebugPush.test.js` 新增施工令十五項最低清單（非事故類型/交通管制/施工/事故/封閉類/壅塞事件皆能成為candidate、位置品質不佳但在服務區內不hard-reject、服務區外仍不進candidate、無效auth/payload零candidate、duplicate零額外candidate、candidate schema正確觀測、AI inactive狀態下0額外LINE推播、既有LINE安全/去重機制不受影響——真實符合資格事故仍恰好1次推播、lifecycle(UPDATED)行為不變、CLEARED不進AI candidate路徑）。全量迴歸：1452項／1419 pass／33 fail，與變更前基線（1424/1391/33，V1.9.9 Phase 1後量測）逐項比對完全相同的33項失敗清單，NEW FAILURES=0（以`git stash`基線diff嚴格驗證）。本輪**未觸碰**：真實LINE決策邏輯本身（`runLineBroadcast`/`broadcastPolicy.js`/`broadcastRules.js`/`locationQuality.js`皆未修改一行）、Workers AI（未接入/未選model/未呼叫）、LINE policy擴大、CCTV/Shared Feed判斷邏輯。APP_VERSION維持`V1.9.9`（同一版本線分階段施工，未升V1.9.10）。詳見 `07_KNOWN_ISSUES.md` 的完整記錄 |
| **V1.9.8** | `6e8575d` | **Windows PBS Push → Production Business Pipeline ＋ 退休 Cloudflare 30 分鐘 PBS Polling（2026-08-28）** — V1.9.7 關閉了持久冪等風險後，本輪把 Windows Debug Push 正式接上真正的業務線：新的 Production 主線是 PBS 官方來源→Windows 本機每3分鐘抓取／服務區篩選／NEW-UPDATED-CLEARED 生命週期→`POST /internal/pbs-debug-push`→V1.9.7 持久冪等→**正式 Business Pipeline**→正式播報資格判斷→LINE，Cloudflare 自身既有 30 分鐘 PBS 輪詢**正式退休**。Ingress：`src/pbs/debugPush.js` **就地升級**（施工令自身「選改動最小、重複程式碼最少的方案」——採 Option A，非另建第二 endpoint）：一個真正被接受（非duplicate）的 NEW/UPDATED 事件，新函式 `buildRawPbsRecordFromPush()` 把 Windows payload 組成 raw-PBS-shaped record（`happendate`/`happentime`/`modDttm` 由 payload 自己的 `generatedAt` 精確反推 Asia/Taipei 本地時間字串——UTC+8 固定無 DST，往返可還原同一時刻，非近似值；`roadtype` 刻意留空，因 Windows 本機過濾器已保證轉發的 NEW/UPDATED 事件 comment 一定含事故關鍵字，comment-only 分類已足夠可信），交給 `pbs/normalize.js` 既有未修改的 `normalizePbsEvent()`，再交給 `traffic/broadcastPipeline.js` 既有未修改的 `runLineBroadcast()`——**與 Cron 輪詢路徑呼叫的同一個函式**，同一套事故／服務區／位置品質／資格／去重／CCTV／LINE Push Policy／notified-state 判斷；隨後呼叫 `traffic/sharedFeed.js` 既有未修改的 `runSharedFeedPersist()`，寫入時機與 `scheduled.js` 呼叫方式完全相同。debugPush.js 本身 0 份重複的業務邏輯。CLEARED 生命週期只 ACK／log，**刻意不**進 `runLineBroadcast`——比照既有 `pbs/pipeline.js` 的 `clearedEvents` 從不進 `broadcastEvents` 的行為，維持與輪詢路徑完全一致的語意。LINE Push Policy（`MAJOR_ACCIDENT_ONLY` 及既有每一條資格規則）**完全未變動**，只是事件來源多了 Windows 這一條路。退休：`src/pbs/pbsConfig.js` 新增 `PBS_30_MIN_POLLING_ENABLED = false`（可由 `env.PBS_30_MIN_POLLING_ENABLED` 覆寫——與既有 `TRAFFIC_SOURCE_MODE`／`LINE_PUSH_POLICY` 同一慣例，Production 完全不設此 env var，僅供本 repo 既有 PBS/CCTV 測試套件沿用輪詢入口注入 fixture），`traffic/scheduled.js` 的 `pbsFetchPerformed` 因此恆為 `false`；`pbsSchedule.js`／`pbs/pipeline.js`／`pbs/lifecycle.js` 程式碼**完整保留、一行未刪**，翻回旗標即可 rollback；同一個 Cron tick 的 TDX／health snapshot／Shared Feed／Pipeline Trace 完全不受影響。已知可接受的副作用（誠實記錄）：`pbs:lifecycle-state` 不再被寫入（Windows 已獨立追蹤 PBS 生命週期）；`GET /health` 的 `pbs` 區塊凍結在退休前最後一次真實數值。新增測試：`test/pbsDebugPush.test.js` 施工令十五項最低清單（NEW/UPDATED 進 Business Pipeline、CLEARED 不進 broadcast、duplicate 不重複處理、服務區外 0 推播、不符合 LINE 政策 0 推播、符合政策恰好 1 次 LINE 推播、duplicate 0 額外推播、canonical notified-state key 重用證明、Shared Feed 正常、CCTV canonical 邏輯（無 metadata cache 仍 text-only 成功）等），另建 `test/pbsPollingRetirementV198.test.js`（4項：旗標關閉、`getPbsScheduleState()` 本身不變、would-be-scheduled tick 確認 PBS fetch 從未被呼叫、其餘 Cron 工作不受影響）。KV 成本剖面誠實修正：genuinely accepted 事件現在真正呼叫 Business Pipeline，非僅 debug log——實測 N 筆事件 → `gets=5N`／`puts=N+2`（idempotency=N + incident-suppression-state 一次 + shared-feed 一次，皆 WRITE_ON_CHANGE 只在整輪第一筆寫入）。全量迴歸：1424 項／1391 pass／33 fail，與變更前基線（1404/1371/33）逐項比對完全相同的 33 項失敗清單，NEW FAILURES = 0（以 `git stash` 基線 diff 嚴格驗證）；9 個既有測試檔因直接透過 `runScheduledTdxSync` 行使 PBS 輪詢入口注入 fixture 而加上 `env.PBS_30_MIN_POLLING_ENABLED=true` 覆寫，其被測邏輯本身完全未變。本輪**未觸碰**：LINE policy 本身、台61/台15全線封閉產品政策、Durable Object（未新增）、服務區 canonical 邏輯本身、Windows Secret／Task Scheduler。Production 驗收因 sandbox 網路 egress 政策封鎖 `traffic-reporter.mr-happytan.workers.dev` 與 Cloudflare Dashboard，誠實標記 `NOT_OBSERVED`（施工令自身第十一節允許以確定性測試＋部署狀態作為封版依據）。詳見 `07_KNOWN_ISSUES.md` 的完整記錄 |
| **V1.9.7** | `9cfa8ef` | **Persistent PBS Debug Push Idempotency（2026-08-28）** — V1.9.6 首筆真實事件驗收成功（台68西向5K：Windows Debug Push 08:48:30，Cloudflare 30分鐘輪詢 09:00:39，Windows早發現約12.1分鐘），本輪關閉當時標記的唯一風險：V1.9.5的冪等只有per-isolate記憶體、`NOT_PERSISTENT`，isolate換掉/Worker重啟/redeploy都可能讓同一事件重新被accepted。修復：`src/pbs/debugPush.js` 新增 TRAFFIC_KV 下的持久 L2 層，獨立 debug-only 前綴 `debug:pbs-push-idempotency:v1:*`（絕不觸碰任何 business key），key 由 `source:eventId:lifecycle:fingerprint` 的 SHA-256 雜湊決定性產生（刻意不用requestId，因同一事件的重試requestId本就不同）；V1.9.5 既有記憶體 Map 保留為 L1 快取（同isolate內可跳過KV讀取），但**絕非唯一真相**——L1 miss 一律再查 L2 KV 才能決定是否 accept，故全新 isolate 的空 L1 仍能正確命中別的 isolate 寫入的 L2 紀錄。TTL 48小時。實測：duplicate 永遠 0 次額外寫入，僅真正首次的 idempotency key 花 1 次寫入——10/30/100 筆/日分別實測消耗 10/30/100 次 KV 寫入（`test/pbsDebugPush.test.js` 的KV成本量化測試，真實counting mock量測非估算），加上既有約118 writes/day 基線 → 約128/148/218 writes/day，遠低於1,000/day帳戶上限，`KV_WRITE_PRESSURE=LOW`。誠實回報 `KV_ONLY_ATOMICITY=NOT_SUFFICIENT`（Cloudflare KV的get-then-put沒有compare-and-swap，理論上兩個isolate同時收到完全相同payload仍可能雙雙accepted）——本輪依施工令自身「不要過度設計」指示**不**引入Durable Object，因為本輪真正要關閉的風險（isolate/重啟/redeploy造成的**事後**重複accept）已被持久KV層完全解決，僅存的極窄race window在這個endpoint的真實流量型態（單一Windows來源、非爆發性）下機率趨近於零，且此endpoint本身零business side effect（LINE=0/CCTV=0/Shared Feed=0），race發生的最壞後果只是一行重複的debug log與一次內容相同的冗餘KV寫入。故 `PERSISTENT_CROSS_ISOLATE_IDEMPOTENCY` 誠實標記為 **`PARTIAL`**（非ACTIVE，非NOT_SOLVED）。KV outage時 fail OPEN（事件仍被接受，不阻擋debug-only功能），never fail closed。既有Debug API JSON schema完全不變（測試證實向下相容）。新增52項測試（原33項），涵蓋施工令20項清單（首次請求、同isolate duplicate經L1、模擬新isolate/重啟duplicate經L2、同eventId不同lifecycle分開判定、無效auth/payload零KV操作、duplicate零額外寫入、accept恰好1次寫入、TTL斷言、KV前綴隔離、schema向下相容、KV outage fail-open）。NEW FAILURES=0（1404項）。本輪**未觸碰**：LINE、CCTV、Shared Feed、Business KV、正式Broadcast Pipeline、既有PBS 30分鐘輪詢閘門、Windows Prototype本身的程式碼/Secret/Task Scheduler。詳見 `07_KNOWN_ISSUES.md` 的完整記錄 |
| **V1.9.6** | `a674b47` | **PBS Windows Local Edge Debug Push Integration（治理封版，2026-08-27）** — 把 2026-08-26～2026-08-27 完成的 Windows 本機邊緣篩選＋Cloudflare Debug Push 架構正式寫入 Engineering Memory；**本輪不 merge feature branch、不新增 Cloudflare runtime 變更、不整合 LINE/CCTV/Business KV、不退休既有 PBS 輪詢**。Windows 端最新事實（本 Cloud Session 已獨立唯讀驗證）：`feature/pbs-local-edge-filter-prototype` 分支最新 commit `95ecdc4718f836ff36c974e829b549f262e6b936`，`git merge-base --is-ancestor` 確認**尚未 merge main**，`git worktree` 乾淨簽出跑 `node --test` 實測 **118 項全通過、0 失敗**（前一輪缺 `cache.js` 的既有落差已在此 commit 補上）。完整架構：PBS官方來源→Windows每3分鐘抓取→Local Edge Filter（**現直接 import 並重用 Production 自己的 `src/pbs/hsinchuFilter.js#isPbsEventHsinchuRelevant`／`src/pbs/roadName.js#normalizePbsRoad`**，修正舊版寬鬆矩形誤收鶯歌55.8K／楊梅68.1K的真實 bug）→事件生命週期比較（CLEARED 現需明確解除文字立即觸發，或連續 2 輪成功 fetch 缺席才 `CONFIRMED_CLEARED`，修正舊版單輪缺席即誤判的 bug，本 Session 已讀程式碼確認兩處邏輯皆存在）→ SHOULD_PUSH 判斷→僅 NEW/UPDATED/CLEARED 才呼叫 Windows Debug Push Client（5000ms timeout、最多2次嘗試、只重試 timeout/network/5xx）→ `POST /internal/pbs-debug-push`（V1.9.5 既有 Cloudflare Debug-only Receiver，本輪程式碼未修改）。真人已啟用 `PBS_DEBUG_PUSH_ENABLED=true`，`WINDOWS_REAL_DEBUG_PUSH=ACTIVE`，`WINDOWS_TO_CLOUDFLARE_DEBUG_CHANNEL=VERIFIED`（人類回報 NEW/UPDATED/CLEARED/DUPLICATE 四種情境 Production mock 皆 PASS，Workers Logs 交叉驗證、LINE/CCTV/R2/Shared Feed/Business KV 側效應皆為0）。記錄一次真實 Secret 治理事故並訂為永久規則：Cloudflare Secret 曾只存在於新 Version（`9ddc58ea`）而 Active Deployment 仍是舊版（`47f54b17`），導致持續 503——**Secret 在 Dashboard 存在 ≠ Active Version 已取得它**，真人 promote 後才恢復正常。標記待辦：`PERSISTENT_CROSS_ISOLATE_IDEMPOTENCY = PENDING_BEFORE_PRODUCTION`（目前僅 per-isolate 非持久判斷，正式 LINE 整合前必須解決）。`PBS_30_MIN_POLLING = PRESERVED`（Cloudflare 既有 PBS 輪詢完全保留，仍是正式路徑，退休時機在六階段路線圖 Phase 6，不得提前）。詳見 `07_KNOWN_ISSUES.md`／`03_ARCHITECTURE.md` 的完整記錄 |
| **V1.9.5** | `56b24d3` | **Windows → Cloudflare Debug-only Push Endpoint（2026-08-27）** — 新增 `POST /internal/pbs-debug-push`（`src/pbs/debugPush.js`＋`src/pbs/debugPushAuth.js`），只證明一件事：Windows PBS Local Monitor 發出最小事件 payload → Cloudflare 驗證身份 → 驗證資料格式 → 做冪等判斷 → 寫 Workers Logs → 回 ACK。本輪**不**讓 Windows 真的發送任何真實事件（`WINDOWS_PUSH_ENABLED = NO`），**不**整合進正式 Pipeline。身份驗證：獨立 Secret `PBS_DEBUG_PUSH_SECRET`（`Authorization: Bearer <secret>`，沿用既有 `TRAFFIC_FEED_SECRET` 的 header 慣例），以雜湊後常數時間比對（與 `security/adminAuth.js` 同技術）驗證，明確以測試證實不會回退到 `PBS_RELAY_TOKEN`／`ADMIN_PASSWORD`／任何 LINE·TDX secret；secret 未設定 = 503（fail closed），token 缺失或錯誤 = 401。Payload 必填 `generatedAt`／`source`（僅限 `'pbs'`）／`eventId`／`lifecycle`（僅限 `NEW`／`UPDATED`／`CLEARED`）／`fingerprint`／`requestId`；`event` 為選填、白名單讀取（`road`／`areaNm`／`direction`／`comment`／`longitude`／`latitude`／`sourceDetail`，僅供 log 使用），body 上限 16 KiB（遠大於單一事件所需，遠小於整包 PBS raw feed）。冪等判斷：Cloudflare Workers isolate 無法可靠跨 request 去重，本輪依施工令自身指示**不**為此新增任何 KV 寫入，改採 per-isolate 記憶體內 fingerprint Map 做 best-effort 判斷，誠實回報 `PBS_DEBUG_PUSH_IDEMPOTENCY_MODE = 'NOT_PERSISTENT'`，不假裝有跨 isolate 保證。結構性 Debug-only 邊界：此模組完全不 import LINE／CCTV／Shared Feed／Incident Suppression／notified-state／Broadcast Provenance／Pipeline Trace／PBS lifecycle 或 pipeline 任何一個模組，也完全不觸碰 `env.TRAFFIC_KV`（或任何其他 binding）——不是靠可能被忘記的執行期旗標，而是連 import path 都不存在。新增 33 項測試（`test/pbsDebugPush.test.js`，涵蓋施工令 CASE A–R 加上 auth／schema／冪等／secret 不外洩／routing 邊界情境），以 counting mock 直接確認 NEW／UPDATED／CLEARED 三種情境下 fetch 呼叫與 KV get／put 呼叫皆為 0 次。既有測試套件無 NEW FAILURES（1385 項／1352 pass／33 fail，與變更前基線的失敗清單逐項比對完全相同）。本輪**未觸碰**：LINE、CCTV、Shared Feed、正式 Pipeline、正式 KV business event state、既有 PBS 30 分鐘輪詢閘門（V1.9.3 不變）、TDX、Cron、Windows Prototype 與其 `feature/pbs-local-edge-filter-prototype` 分支（未 merge）。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |
| **V1.9.4** | `3075114` | **Pipeline Trace 讀取效能優化（2026-08-27）** — V1.9.3 上線後真人回報全站 `ERR_CONNECTION_TIMED_OUT`，經查修令確認與程式碼無關（sandbox 無法連 Production 驗證，詳見該輪報告）；隨後真人在 Production 實測出真正的效能問題：`/`≈0.8s／`/version`≈0.4s／`/health`≈0.75s，但 `/admin/pipeline-trace` 與 `/admin/pipeline-trace-view` TTFB 皆 ≈59.1s。確認的 Root Cause（讀程式碼得出，非猜測）：`listPipelineTrace` 的舊 `collectFlattenedTraceEntries` 一律**循序**（一次一個 `await kv.get()`）解碼到 `MAX_ENTRIES_SCANNED`（500）筆，**在**分頁 `limit`（預設 60）套用**之前**，不論有沒有設篩選條件——一個完全沒篩選、只要「最新 60 筆」的頁面，每次都要付出 500 次循序 KV 往返。修復：新函式 `scanTraceEntriesProgressively` 把工程令的三個「刀口」合併成同一個機制：(1) 提前停止——無篩選查詢一旦解碼滿 `boundedLimit` 筆就立刻停止，實測 500 把 key、預設 limit 60 時只做 60 次 `kv.get()`（原本 500 次）；(2) 漸進式掃描——有篩選時以「輪」為單位、累積目標逐輪翻倍（第一輪 = `boundedLimit + NO_FILTER_SCAN_BUFFER`(20)，之後每輪 ×`PROGRESSIVE_SCAN_GROWTH_FACTOR`(2)，上限仍是 `MAX_ENTRIES_SCANNED`），不再一開始就固定掃 500；(3) 有界並行讀取——同一輪內的 `kv.get()` 改為固定 `PARALLEL_GET_BATCH_SIZE`(20) 筆一批、以 `Promise.all` 並行送出，批與批之間循序，絕不把整輪（更別說全部 500 筆）一次性 `Promise.all`；20 是實測挑選（本專案原本沒有既有的 KV／Workers 併發上限可參考），落在施工令建議的 20–30 區間中段。V1／V2 兩種 schema 並存策略完全不變（V1 舊制 `debug:pipeline-trace:v1:*` 仍不刪除不遷移、繼續靠既有 24h TTL 自然過期），僅兩個前綴各自的 `kv.list()` 列舉改為並行（`Promise.all`）而非循序。新增可觀測性（全部讀既有已算出的數字、**未新增任何 KV 寫入**）：`GET /admin/pipeline-trace` 現在也回傳 `kvListCalls`／`kvGetCalls`／`v1KeysScanned`／`v2BatchKeysScanned`／`v1KeyCount`／`v2BatchKeyCount`／`entriesDecoded`／`entriesMatched`／`readDurationMs`，與原本就存在於函式回傳值、但先前從未進到 JSON 回應的 `scannedKeyCount`／`totalKeyCount`／`scanTruncated` 一起回傳；`/admin/pipeline-trace-view` 頁面底部新增一行純數字診斷條（同樣不含任何密鑰／token／原始 CCTV 網址）。新增 23 項測試（`test/pipelineTraceReadPerformance.test.js`，涵蓋施工令 CASE A–I 決定性 fixture），既有測試套件無 NEW FAILURES（1352 項／1319 pass／33 fail，與變更前基線的失敗清單逐項比對完全相同）。本輪**未觸碰**：Pipeline Trace 寫入路徑（`persistPipelineTraceBatch`／`recordPipelineTrace`／`hasPipelineTraceRelevantChange`）、V1.9.3 的 PBS 30 分鐘排程閘門、Health Snapshot WRITE_ON_CHANGE、Windows Prototype、Windows→Cloudflare Push、LINE、CCTV、TDX、Cron 頻率。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |
| **V1.9.3** | `93efec3` | **KV Write Optimization Phase 2（2026-08-26）** — 延續 V1.9.2 的帳號寫入壓力，本輪再關三個來源：(1) `health:snapshot:v1` 改為 WRITE_ON_CHANGE——比較時排除 `generatedAt`／`tdx`＋`pbs.lastFetchedAt`／`line.lastLinePushAt`，**以及** `tdx`／`pbs.scheduledThisRun`＋`sleeping`、整個 `broadcast` 區塊（這些都只代表「這輪又跑過一次」，不是真正健康狀態）；PBS 的 `pbs` 區塊現在也比照 TDX 做 schedule-skip 時的沿用不變。這個排除清單是本輪自己的決定性 fixture（`test/kvWriteQuantificationV193.test.js`）實測抓到的——若漏排除，一個完全沒變化的 PBS_ONLY 安靜日仍會因為排程狀態每輪翻轉而持續寫入，WRITE_ON_CHANGE 形同虛設；(2) PBS 固定抓取由每 10 分鐘改為每 30 分鐘、且僅 07:00:00–22:00:00 Asia/Taipei（新檔 `src/traffic/pbsSchedule.js`）——Cron 本身仍是每 10 分鐘不變，施工前已確認 `PBS_STALE_THRESHOLD_MS`（2 小時）／`PBS_ABSENCE_GRACE_PERIOD_MS`（24 小時）皆為 wall-clock 而非以 tick 計數，遠大於本輪引入的日間 30 分鐘與夜間約 9 小時空窗，LINE Push 本身已限定 08:00–21:59:59，07:00 恢復抓取還有整整一小時緩衝；`lifecycle.js` 的 `commitPbsLifecycleState` 同時開始回報真實的每 UID 新增／更新／新解除筆數；(3) Pipeline Trace 新增 `NO_RELEVANT_CHANGE`（`pipelineTrace.js` 的 `hasPipelineTraceRelevantChange`）——本輪若無新增／更新／解除的 TDX 或 PBS 事件、無 TDX 重複或 PBS 國道閘門排除、且無 LINE 推播嘗試，整批 Trace 寫入直接跳過，不再讓同一筆持續存在但完全沒變的事件每輪都重寫一次；TDX 重複與 PBS 閘門排除仍視為「有意義」（保留既有 V1.8.6.7 查修保證，在目前 `PBS_ONLY` 正式環境下這兩者永遠是 0，不影響本輪省下的量）。新增 `[cron][pbs-schedule]` 低成本 log（`pbsFetchPerformed`／`pbsFetchSkippedSchedule`，不新增 KV key）；`/health` 新增 PBS 對應的「本輪執行狀態」／「最近一次擷取時間」列（比照既有 TDX 顯示）。新增 4 個測試檔（`pbsSchedule.test.js`、`healthSnapshot.test.js` 新增項、`pipelineTraceNoRelevantChange.test.js`、`kvWriteQuantificationV193.test.js`——後者以決定性 fixture 跑完整天 144 次真實 Cron tick，實測 QUIET/NORMAL/HIGH EVENT DAY writes/day 分別為 5／21／27，遠低於施工令 ≤150／100–300／<500 目標），既有測試僅在其假設被本輪行為改變（PBS 不再 24/7）時更新，NEW FAILURES = 0。詳見 `07_KNOWN_ISSUES.md` 的完整修正紀錄 |

## 版本線校正紀錄（PRODUCTION_VERSION_LINEAGE_RECONCILIATION，2026-08-25）

V1.8.7.8～V1.8.7.14 這七列是**補記**，不是新發布。七次變更早在補記之前就已 merge 進 `main`
並由 Cloudflare Workers Builds 自動部署；當時只寫了任務名稱、沒有配正式版本號。
本次校正只建立「版本號 ↔ 既有 commit ↔ 既有部署」的對照，
**沒有 rebase、沒有 amend、沒有 force push、沒有改動任何歷史 commit，也沒有假裝任何歷史版本重新部署。**

同時發現一個比漏編號更嚴重的問題：**唯一權威來源 `src/version.js` 從 2026-08-21 的
V1.8.6.9 之後就沒再更新過**——也就是說整個 V1.8.7.x 系列（不只是 V1.8.7.7 之後）
`GET /version` 都在回報 V1.8.6.9。漂移的成因是有三個地方各自以為自己知道版本：

| 來源 | 當時的值 | 現在 |
|---|---|---|
| `src/version.js` → `GET /version`／`/admin/deployment-status` | `V1.8.6.9` | **唯一權威**，已校正為 `V1.8.7.14` |
| Engineering Memory export（掃 commit message 找 `V\d+\.\d+\.\d+`） | `V1.8.7.7` | 改讀 `src/version.js`，掃描結果降級為 drift 警告 |
| `ENGINEERING_STATUS.md` 人工標記 | 各自為政 | 僅供交叉檢查，不具權威 |

**從 commit message 掃出來的版本號，是沒有人負責的版本號**——有人剛好打了就會動，沒人打就停住。
`test/versionLineage.test.js` 七項測試鎖住的是這條規則的**形狀**（單一來源、其他一律衍生），
刻意不鎖當下的數字，否則只會多出第四個要同步的地方。

**永久規則**：任何進 Production 且改變 runtime 行為的變更，必須在同一個 commit 內 bump
`src/version.js` 的 `APP_VERSION`。任務名稱（`CCTV_METADATA_RECOVERY`、
`PBS_ACCIDENT_CCTV_ENRICHMENT_FIX` …）是工程標籤，**永遠不能取代版本號**。
純文件／治理／Drive sync 工具／測試整理不 bump 版本，但仍須有 commit。

## 版本編號格式切換｜三段式版本治理（2026-08-25 起生效）

四段式 `V1.8.7.x` 版本線於 **V1.8.7.14 正式停止（`LAST_FOUR_PART_VERSION`）**，
`FOUR_PART_VERSIONING = RETIRED`。自此起 `THREE_PART_VERSIONING = ACTIVE`，
下一個正式版本號是 **`V1.9.0`（`NEXT_RELEASE_VERSION`）**。

**`src/version.js` 目前仍是 `V1.8.7.14`，本次切換沒有提前 bump。**
只有下一次真正改變 Production runtime 行為的 release，才會在**同一個
commit 內**把 `APP_VERSION` 換成 `V1.9.0`——治理決議本身不觸發版本號變動、
不觸發 deploy。

三段式規則：

| 變更類型 | 動哪一段 | 例 |
|---|---|---|
| Bug fix | patch | `V1.9.0 → V1.9.1 → V1.9.2` |
| 明顯新功能／架構階段 | minor | `V1.9.x → V1.10.0` |
| 大型不相容版本 | major | `→ V2.0.0` |
| 純文件／治理／Engineering Memory／測試整理 | 不升版本 | 仍須有 commit |

V1.8.7.8～V1.8.7.14 這既有七列**不重寫**，四段式版本線就此封存於歷史。

## 版本線之外的重要里程碑

- **V57 系列**：Shared Traffic Feed 首次建立（`completedProducts` 持久化機制），V57.1（Shared-Feed-only CCTV top-up）、V57.3（分頁修復）陸續強化。詳見 `02_PROJECT_HANDOFF.md` §30。
- **四部門聯合治理**（2026-08-22）：非程式版本，但是本專案第一次正式治理框架定案，見 `01_FOUR_DEPARTMENT_GOVERNANCE.md`。
- **Meeting Room Export / Engineering Memory v1**（本輪）：本 export 系統本身的建立版本。

## 目前 main 最新狀態

Export 產生時的即時 git 狀態見 `SYSTEM_STATE.json`；此表格為人工整理的「重要節點」，不會每次 export 自動改寫——只在真的有新的架構意義版本產生時才手動追加一列。
