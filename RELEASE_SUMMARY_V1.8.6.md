# RELEASE_SUMMARY_V1.8.6.md — TDX 用量對帳 + /health 額度儀表板

涵蓋版本：**V1.8.6 → V1.8.6.1 → V1.8.6.2 → V1.8.6.3**（本輪最終 production commit：見文末）。

給專案 owner／人類快速理解「這一輪做了什麼、現在能看到什麼、有什麼限制」——不是工程實作細節（那些在 `PROJECT_HANDOFF.md` §18–§21）。

---

## 1. 這輪解決什麼問題

- 建立了路況播報員自己的 **TDX 本機用量帳本（Usage Ledger）**：每一次真正打到 TDX 的 API 呼叫都會被記錄下來。
- 可以在 `/health` 上**對帳**：今天／本月到底打了多少次 TDX、傳了多少流量、換算成多少「點數」。
- `/health` 的 TDX 區塊整個改版成**手機優先的額度儀表板**——打開手機一眼就看得懂，不必再讀工程 debug 資料。
- 可以直接看到「這個月 TDX 基礎服務方案還剩多少點數」以及「照目前速度，月底會不會爆」。

全程 **零額外 TDX／PBS／LINE 呼叫**——`/health` 從頭到尾只讀 KV 裡已經寫好的資料，看再多次、看再多遍都不會多花一分 TDX 額度。

---

## 2. 正式 TDX 資料來源

路況播報員目前只會用到 **3 種** TDX API 資料來源：

- **國道 RoadEvent**（即時道路事件）
- **省道 RoadEvent**（即時道路事件）
- **CCTV metadata**（高速公路閉路電視攝影機資料——僅限中繼資料查詢，不是影像本身）

**CMS（可變資訊看板）／新竹市公車／新竹縣公車**這三個來源，早在 V1.6.1 就已經退出正式 Production——程式裡還留著定義，但正式排程與 Debug 端點都不會再打到它們。若哪天 `/health` 顯示這三者非 0，代表有不該發生的程式路徑打到了已停用來源（會有專屬警告卡片提示）。

---

## 3. CCTV 必須明確區分兩件事

這是這輪特別澄清、也是 V1.8.6.2 修正的重點：

| | TDX CCTV metadata | freeway.gov.tw MJPEG 即時影像 |
|---|---|---|
| 是什麼 | 查詢「哪裡有哪些攝影機」的 TDX API | 直接向公路局伺服器要一張即時影像截圖 |
| 算不算 TDX 額度 | **算**——計入呼叫次數／流量／點數 | **不算**——根本不是 TDX API，是另一台伺服器 |
| 什麼時候發生 | 只有 Admin 手動重新整理攝影機清單時 | 每次真的要傳 CCTV 圖片給 LINE 使用者時 |

兩者絕對不能混在一起算——混算會讓「TDX 點數用了多少」失真。

---

## 4. Production 排程現況

- Cloudflare Cron **每 10 分鐘**跑一次。
- TDX 只在 **08:00–22:00**（台灣時間）之間抓取，PBS 全天 24 小時都會跑。
- TDX 每 **20 分鐘**才真正打一次（不是每 10 分鐘）。
- 每次只打 **國道 + 省道** 兩個來源。
- 正常、完整一天的 Production TDX 資料呼叫理論值是 **84 次**（42 個時段 × 2 個來源）。
- PBS（警廣）**完全不佔用 TDX 額度**——PBS 是另一套獨立的資料來源。

---

## 5. `/health` 新版主要畫面（由上到下）

1. **TDX 今日** — 今天總呼叫次數、流量、估算點數、Production／人工額外次數。
2. **TDX 本月** — 這個月累積到目前為止的呼叫次數／流量／估算點數。
3. **剩餘額度** — 本月 3 點額度還剩多少，附進度條與燈號（充足／注意／接近上限）。
4. **月底預估** — 依照目前的使用速度，預測月底總點數會不會超過額度。
5. **TDX 來源（今日）** — **國道／省道／CCTV 固定三列**，即使是 0 也照樣顯示，不會被隱藏。
6. **每日對帳（近 7 天）** — 每天的呼叫次數、流量、估算點數、與理論值的差額，一眼看出有沒有異常。
7. **進階資訊**（收合區） — 給工程排查用的細節：呼叫情境拆解、OAuth 刷新次數、TDX 官方歷史參考數字等，平常不會佔畫面。

---

## 6. Source（來源）與 Context（情境）是兩件不同的事

這是這輪反覆強調、也是 V1.8.6.2 專門修正的分類原則：

**Source（打了哪個 TDX 資料）：**
- freeway（國道）
- highway（省道）
- cctv（CCTV metadata）

**Context（是哪個程式路徑打的）：**
- production-cron（正式排程）
- debug-status（除錯頁）
- debug-tdx（除錯頁）
- admin-cctv（管理員 CCTV 探測）

「TDX 來源（今日）」卡片只看 Source，不分 Context——不管是正式排程、除錯、還是管理員手動觸發，只要打到國道/省道/CCTV，都算進同一列。Context 的細節則收在「進階資訊」裡分開呈現，兩者絕不混在同一張卡片裡。

---

## 7. 2026 年 8 月官方 baseline（人工比對，非本機統計）

TDX 官方後台人工確認的實際用量：

**2026-08-16**：1490 次 / 17016 KB / 1.106 點
**2026-08-17**：704 次 / 10534 KB / 0.537 點
**2026-08-18**：89 次 / 3177 KB / 0.080 點（其中：國道 42、省道 42、CCTV 5）

**累積至 2026-08-18（含當日）：2283 次 / 30727 KB / 1.723 點**

**為什麼要這樣算：** 本機用量帳本是 2026-08-18 當天才正式上線開始累積的，所以 8/16～8/18 的真實用量沒辦法由本機系統回推——這三天的數字是人工從 TDX 官方後台核對確認的，不是程式猜出來的。8/18 官方日結資料已經涵蓋完整的 8/18 一整天，所以本月額度計算改用「baseline 累積至 8/18」，本機帳本從 **8/19 起**才開始貢獻月額度，這樣才不會把 8/18 的用量算兩次。

---

## 8. 目前限制

- 本機估算的流量（KB/MB）是**估算值**，不保證與 TDX 官方帳務系統的計算方式完全一致（壓縮、計費口徑可能不同）。
- `/health` 的用途是**對帳與提早示警**，不是取代 TDX 官方帳務系統——真正的額度以 TDX 官方後台為準。
- 2026 年 9 月起沒有官方 baseline 可比對——之後每個月都完全由本機用量帳本從頭累積，不需要、也不會再有人工回填的動作。

---

## 9. 最終 production commit

```
main HEAD: ba74d48f2c98b41f1d5b2db8a60639001ecd1109
```

（V1.8.6.3：August TDX baseline 校正至 2026-08-18 官方日結，已 merge 到 `main`，Cloudflare 已自動部署上線。）
