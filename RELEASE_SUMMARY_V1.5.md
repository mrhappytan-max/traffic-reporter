# 路況播報員 — V1.5 Alpha 封頂總結

**封頂日期：2026-08-16**
**main commit：`4efb9e5`**
**狀態：V1.5 Alpha 已上線，進入實車觀察期，暫不開發新功能**

---

## 這是什麼專案

「路況播報員」是一個跑在 Cloudflare Worker 上的服務，目標是給**營業車司機**（計程車/客運為主）用 LINE 推播新竹縣市周邊「可能需要立刻改道」的路況異常。

每 5 分鐘自動抓一次官方路況資料、整理、去重，符合條件才推播到 LINE。設計原則是「寧可少播，不要洗版」——司機的注意力很寶貴，不重要的訊息比沒有訊息更糟。

---

## 資料從哪裡來

兩個官方資料來源，各自獨立蒐集，最後合併：

1. **TDX**（交通部運輸資料流通服務平臺）：國道/省道即時路況事件、市區看板（CMS）、公車動態通阻。
2. **PBS**（警察廣播電臺）：全國即時路況通報。因為官方 PBS 網站無法被 Cloudflare 直接連線（會 timeout），改用**架在 Windows 主機上的中繼站（Relay）**，透過 Cloudflare Tunnel + Workers VPC 私下把資料送進 Worker，不走公開網路。

**PBS + TDX 若描述的是同一起事件，只會播報一次**——系統會比對道路、方向、公里數/座標、時間，判斷是不是同一件事，是的話合併成一則訊息（用資訊比較完整的那一份呈現），不會讓司機收到兩則重複通知。

---

## 現在 LINE 會播什麼、不播什麼

這是 V1.5 這一輪最大的改動。過去版本什麼類型都播，V1.5 收斂成：**只播「突發、司機可能需要立刻改道」的事件**，因為一般塞車司機自己看 Google Map / 1968 就知道了，不需要我們再推播干擾。

**一定播：**
- 事故（accident）
- 道路封閉（closure）
- 臨時交通管制（control）

**有條件才播：**
- **施工（construction）**：只有內容真的提到「封閉/車道封閉/占用車道/禁止通行/無法通行/改道/交通管制」等實際影響通行的字眼才播，單純的一般施工公告不播。
- **其他異常（other）**：只有命中明確的異常關鍵字（淹水、積水、涵洞、落石、坍方、路基流失、樹倒、電線掉落、電線桿倒、掉落物、貨物散落、火災、橋梁封閉、橋梁異常、河川暴漲、溪水暴漲、道路中斷、無法通行）才播，無法辨識的一般 other 一律不播。

**永遠不播：**
- **一般壅塞/車多/回堵/尖峰車流**（congestion）——不管是關鍵字判斷出來的，甚至是後來用即時車速資料（VD）二次確認過的「嚴重壅塞」，一律不進 LINE。壅塞資料本身還是持續在收集、分類、比對，只是不再拿來推播。
- **公車動態異動（alert）**——預設不播。

如果同一件事同時被回報成「壅塞」又被回報成「事故/封閉」（例如兩筆不同來源記錄），壅塞那筆會被擋掉，事故/封閉那筆照常播——事件還是只播一次，用比較有意義的說法呈現。

---

## Alpha 現況

- 目前只有**你本人一個訂閱者**，作為 Alpha 測試對象，沒有對外開放。
- Cron 每 5 分鐘跑一次；LINE 只在台北時間 08:00–21:59 之間實際推播（Cron 本身照跑，只是不推）。
- `PBS_BROADCAST_ENABLED = true`：PBS 的資料已經會參與播報判斷，不只是觀察用。

---

## 主要操作與依賴

- **開發框架**：Cloudflare Workers（`wrangler.jsonc`）
- **儲存**：Cloudflare KV（`TRAFFIC_KV`，一個 namespace 裝所有狀態）
- **對外服務**：TDX API、PBS Windows Relay（透過 Workers VPC）、LINE Messaging API
- **除錯用網址**（都是唯讀，不會寫入任何狀態、不會真的推播）：
  - `GET /debug/status`：整體流水線現況，含播報判斷結果
  - `GET /debug/tdx`：TDX 五個資料源各自狀況
  - `GET /debug/pbs`：PBS 資料狀況與 VPC Relay 診斷
  - `GET /debug/pbs-vpc-probe`：單純測試 Relay 連線通不通
- 部署：main branch push 後 Cloudflare 會自動觸發正式環境部署。

---

## 已知限制

1. **VD（即時車速）資料的欄位格式沒有在真實環境驗證過。** 這個開發環境連不到 TDX 官方網站，VD 二次驗證的程式碼是照 TDX 一貫的命名慣例寫的，且做了防禦性處理（欄位對不上不會壞掉，只是「查不到車速」），但實際格式要等部署後用 debug 資料確認。
2. **construction/other 的關鍵字清單是這一輪列出來的初版**，可能不夠完整或有誤判，需要靠實車測試慢慢修正。
3. **congestion 的 VD 二次驗證邏輯目前形同虛設**——因為壅塞已經完全不播了，即使 VD 確認是「嚴重壅塞」也一樣不播。這段程式碼還在（沒有刪除，未來若產品方向改變可以重新啟用），只是目前不會被使用者看到任何效果。
4. **目前只有一個訂閱者**，多使用者情境下的行為還沒有實測過。

---

## 接下來

**這一輪之後不再主動開發新功能。** 之後的調整只根據**實車測試的真實回報**來修，例如：某個真實事件沒播到、某個關鍵字誤判、某個路段標示不對等。有問題請直接回報實際案例（road/方向/公里數/當時 debug 內容），會比憑空討論更快定位問題。
